# D-SIDLE 固定延迟实现

本文是源码、配置和测试的当前权威说明。D-SIDLE 只保留一套硬件模拟机制：
按真实 HWCC/SWCC 访问覆盖的 cache line 累加固定延迟。

## 配置契约

配置路径是 dsidle.latency_inject.fixed_latency，且对象只能包含以下六个字段：

| 字段 | 语义 |
| --- | --- |
| enabled | 是否启用固定延迟 |
| cache_line_bytes | cache line 大小，必须是正的 2 的幂 |
| swcc_fixed_ns_per_line | 每条 SWCC 覆盖线的纳秒延迟 |
| hwcc_fixed_ns_per_line | 每条 HWCC 覆盖线的纳秒延迟 |
| foreground_enabled | 是否对前台 scope 收费 |
| background_enabled | 是否对后台 scope 收费 |

解析器对未知、重复、缺失、负数、非有限值和错误布局 hard fail。延迟启用时
只允许 RelWithDebInfo，且 verbose 和 extra_check 必须关闭。默认配置关闭延迟。

## 唯一计费公式

~~~text
pending_delay_ps += touched_swcc_lines * swcc_fixed_ps_per_line
pending_delay_ps += touched_hwcc_lines * hwcc_fixed_ps_per_line
delay_ns          = round(pending_delay_ps / 1000)   // 仅作用域退出时一次
~~~

每行延迟在配置时转换为定点（皮秒），整个 scope 用 checked 定点累计，只在安全
出口统一取整：`0.4ns/line × 5` 精确等于 `2ns`、`1.25ns/line × 4` 精确等于
`5ns`；乘法/加法/地址范围溢出 hard fail，不饱和到 `UINT64_MAX`。进程初始化时
注册不可变的 HWCC/SWCC 映射边界（`ConfigureLatencySimulatorForPool`），启用后
每次收费先校验为 pool-local offset；错误 domain、越界和无 scope 的真实共享访问
都 hard fail。

每次真实访问独立计算起始地址和结束地址覆盖的 cache line；同一行的后续真实访问
仍然再次收费。访问不保存历史，不维护 cache 状态，不产生访问序号或共享输出。

普通内存 wrapper 先执行真实 load/store，原子 wrapper 先执行真实
load/store/exchange/fetch/CAS，再对该次实际地址范围收费。CAS 的 expected、
成功/失败结果及两种 memory order 完整保留。fence 只保留真实排序语义，因为它
没有可计费地址。

当前入口包括：

- HWCC：PoolHeader、RootControl、NodeControl、shard、epoch 和 phase 控制；
- SWCC：规范 Masstree 节点、key suffix、value 和 free-list 对象；
- owner-private SWCC：本地 owner 仍位于 SWCC 的对象访问；
- local DRAM：副本目录、策略数据和 heartbeat，不产生 HWCC/SWCC 延迟。

批量 `memcpy`/`memmove`/`memset` 的真实读写按实际覆盖范围在操作完成处收费一次；
flush/invalidate 标签不收费，真实 `clflush`/`clwb`/invalidate/writeback/fence 及其
顺序原样保留。整节点 envelope 粗略收费已删除：每个真实字段访问只在各自的 typed
load/store 层收费一次，leaf-link 比较直接使用持久 NodeRef，不再为计费额外解析
canonical node。

replica `Create` 在复制前执行真实 SWCC invalidate（可见性），复制时按实际读取
范围收费（计费），两者分离；不再把后缀/value 的陈旧缓存发布到本地 replica。

## scope 和安全点

每个前台操作使用 foreground scope；replica、trigger、promotion、demotion、
cooler 等后台 worker 使用独立 background scope。TLS pending delay 以线程和
simulator generation 隔离，前后台不共享未结算延迟；嵌套 scope 只在最外层
安全出口结算。

不使用覆盖整个 runner 或整个 replay 的粗粒度 scope。主线程的共享内存操作按
语义拆成短 scope，各自在安全出口补齐：

- table bootstrap/attach；
- 每次 phase barrier；
- epoch-slot/layout 读取（含 `SharedExperimentPhaseBarrier` 的构造读取）；
- 最终 HWCC/SWCC 容量读取（先读入本地变量再输出）。

`PinRoot` 自带 merge scope，保持为该线程的顶层 scope。每个 E2E 前台 worker
在启动握手之前用自己的短 foreground scope 完成 SWCC 绑定（绑定读取的
HWCC layout 字段在此时收费并补齐），之后每个 Put/Get/Delete/Scan 在各自
顶层 foreground scope 内结算，末尾的 `rcu_drain()` 单独用短 foreground
scope 覆盖并在 drain 返回后补齐。replica/trigger/promotion/demotion/cooler/
adjuster 等后台 worker 的 `BindCurrentThread` 同样在进入 mutex/条件变量等待
或长期 worker loop 之前，用自己的短 merge scope 完成绑定并补齐；每个后台
任务（`TriggerOnce`/`ExecuteOnce`/`CoolOnce`/`AdjustOnce`）再建立自己的
scope。

SWCC 范围分类只读取绑定阶段写入的线程本地不可变边界缓存（swcc_begin/
swcc_end/有效标志），value 热路径不再解引用 PoolHeader 或 PoolStaticLayout，
避免在分类入口引入隐藏 HWCC 访问或递归收费。启用固定延迟时，没有合法绑定
的线程调用分类器会 hard fail，而不是把潜在 SWCC 访问静默当成本地 DRAM；
完全位于 SWCC 内的范围返回 true、完全位于池外的本地 DRAM 返回 false；左/右
边界部分跨越或 uintptr_t 地址加法溢出同样 hard fail，禁止把跨边界访问静默
按本地内存处理。`SharedPool::Close` 会清空当前线程缓存，顺序重映射后只有新
绑定范围被识别。

`SharedPool` 的 move 只允许在没有并发使用者（worker 全部停止并 join）时发生：
move 构造与 move 赋值会把当前线程的绑定（pool 指针、SharedPoolBase 与 SWCC
范围缓存）从源对象迁移到目标对象，move 赋值先关闭目标旧映射；moved-from
空壳的析构/Close 不会破坏 moved-to 的有效绑定，moved-to 的 Close 会清空
当前线程全部 TLS 状态。

busy-wait 使用一次校准的 x86 TSC。校准失败 hard fail，禁止睡眠调用和调度器
等待。结算点位于业务锁、RCU/epoch guard、allocator 锁、发布中间态和阻塞
队列/RPC 状态之外；析构、异常和早返回通过 RAII scope 清空 pending delay。

关闭时 wrapper 只读取一个进程本地不可变 feature gate，不访问 TLS、TSC、clock、
地址换算、锁或动态共享状态。编译期关闭路径同样不构造额外设备、线程或映射。

## 本地 CTest 临时目录

所有触碰 pool backing、生成配置、trace、日志或结果目录的 CTest 都通过
`scripts/dsidle_ctest_wrapper.sh` 运行：每次测试使用唯一临时根目录
（/tmp/dsidle-ctest.XXXXXX），`--config-file` 的 `shared_memory.path` 在临时
配置副本中替换为根目录内的 pool 文件，父级 wrapper 在 EXIT/INT/TERM/HUP 时
清理（即使子进程 assert/abort 也能回收），成功与失败都默认清理并把诊断输出
透传给 CTest；`DSIDLE_KEEP_TEST_ARTIFACTS=1`（或 `--keep`）时保留并打印唯一
路径。YCSB trace 生成器的小型 build 默认落在 gitignored 的
`.dsidle-ycsb-build/`（CTest 下落在临时根内），不会让 third_party 子模块变
dirty。C++ 单元测试的 backing 使用 mkstemp 唯一名并在成功路径 unlink。

## 共享池和容量

共享池只有业务 ivshmem /dev/ivpci0。PoolStaticLayout 的 coordination 区只
保存 epoch/phase 控制；HWCC 不再为模拟器事件、序号或诊断日志预留空间。
因此配置报告的 HWCC/SWCC 容量与 allocator 可用容量一致，重建 pool 时必须使用
新 backing。

## 验证

tests/latency_simulator_tests.cc 覆盖零字节、单线、跨线、非对齐、重复访问、
两域不同延迟、嵌套和 worker 隔离；还覆盖原子返回值、CAS 成功/失败、内存顺序、
RAII 结算、关闭快速门和 TSC busy-wait。

dsidle_config_tests 覆盖 JSONC 的严格字段契约和遗留配置拒绝。
dsidle_latency_access_audit_tests 在文件级 allowlist 之外执行站点级危险模式
检查（分类器不得解引用 pool header、allocator generation 必须走 wrapper、
runner 不得有 replay/phase 级粗 scope、启用后 worker 绑定必须带短 scope）；
运行时定向测试进一步证明绑定无 scope 会 hard fail、绑定产生预期 HWCC 延迟、
remap 后只识别新 SWCC 范围、HarvestRemote 的 generation 读取恰好收费一次。
完整 CTest 还覆盖共享池、allocator、epoch、replica、Masstree、runner 和脚本。
VM 验证使用本仓镜像、配置、trace 和 backing，先无延迟再用小型非零 canary。
