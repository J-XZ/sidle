# D-SIDLE 硬件模拟当前实现

本文是当前 D-SIDLE 路径的实现说明。旧的 cache-hit/cache-miss 延迟模型不再是
运行时模型；历史记录中的旧字段只用于解释旧实验，不能用于生成当前配置或解释
当前统计。

## 四个独立模块

配置位于 `dsidle.latency_inject`，必须同时出现并严格解析以下四个 section：

| section | 作用 | 关闭时是否影响其它模块 |
| --- | --- | --- |
| `fixed_latency` | 按配置的 SWCC/HWCC 域和覆盖 cache line 数累加固定延迟 | 否 |
| `hwcc_access_count` | 统一 HWCC ordinary read/write 的 operation、line、可选 byte 统计 | 否 |
| `atomic_count` | 对实际执行的 load/store/CAS/exchange/fetch/fence/wait-notify 统计 | 否 |
| `remote_cache_invalidation` | 全局有序事件上的 HWCC coherence 与显式 SWCC visibility 统计 | 否 |

parser 拒绝未知、重复、缺失、负数、非法范围和旧的 `cache_model`/
`cache_hits_enabled` 字段；启动摘要、配置 hash 和测试使用同一严格布局。运行中
不替换配置。四模块全关时业务 wrapper 只有一个 process-local relaxed feature
bit 分支，不触碰 TLS、时钟、地址换算、缓存状态、锁、统计原子或动态内存。

固定延迟模块没有命中率、LRU、per-thread cache 或 cache-hit 免延迟逻辑。访问只
累加线程本地 pending delay，并在 `ScopeGuard` 释放业务锁、RCU、pin、ring 和
发布状态后补齐；flush/invalidate 是协议事件，不会再次按普通写收费。

## 访问与内存域矩阵

| 生命周期/对象 | 内存域 | 当前入口 | 计数规则 |
| --- | --- | --- | --- |
| pool header、RootControl、NodeControl、shard/epoch/phase 控制 | HWCC | `CountedMemory*`、`CountedAtomic*` | 实际执行的普通访问和原子访问分别计数；原子不进入 ordinary read/write |
| canonical Masstree node body | SWCC | `nodeversion::stable` 的 read envelope；`unlock_impl`/publish 的 write envelope | 按 NodeControl 保存的实际 allocation bytes，一次覆盖节点体；不再对同一叶槽重复计数 |
| canonical value、value metadata、value bytes、external suffix | SWCC | value row 执行型 helper、`leafvalue`、suffix helper | 真实 load/store/memcpy 紧邻观测；副本 sizing pass 不观测，唯一 source copy 观测一次 |
| SWCC free-list link/header | SWCC + HWCC | `FixedBlockShardAllocator` | link/header 的真实 SWCC 访问和 free-list head/bump/lock 的 HWCC 原子分别计数 |
| replica body、directory、策略 histogram、队列、trace/JSON buffer | local DRAM | 本地数据结构；replica worker 原子使用 `kLocalDram` wrapper | 不产生 SWCC/HWCC/remote 事件；启用 atomic 模块时可进入 local-DRAM breakdown |
| simulator feature bit、TLS registry、sequencer、统计结构、共享 event log | instrumentation | simulator 内部 | 排除自计数；event log 只存放诊断记录，不占业务 allocator |

节点体仍使用 Masstree 的原地更新语义。因为上游 inline 字段访问很多，稳定读和
发布写使用一个有界、按实际分配大小的节点 envelope；value/suffix 则有独立的
执行型访问封装。这样不会为了模拟器重写树的锁、版本或发布协议，也不会把同一
个 value row 由 node envelope 和 value helper 重复收费。

## 原子和远程可见性

`CountedAtomic*` wrapper 先执行真实原子，再记录结果，保留 memory order、CAS
failure order、weak 伪失败、`expected` 更新、返回值和 lock-free 语义。CAS 每次
attempt 都计数，并分解 success/failure；HWCC、owner-private SWCC 和 local DRAM
使用不同 domain。replica worker 与 e2e heartbeat 的本地控制原子也使用
`kLocalDram` wrapper；模拟器自身的 feature/registry/counter 原子不计入。

remote 模块使用共享 instrumentation-only sequence 和固定容量 event log。事件
按稳定 `(domain, pool_id, pool_offset/cache_line_bytes)` 标识，不使用虚拟地址。
缺口、重复、溢出、非法状态和跨映射不一致都会 hard fail。HWCC 至少区分 dirty
owner→reader、dirty owner→writer/RMW、clean-copy invalidation 和 dirty-capacity
eviction；支持 per-node/total/explicit capacity、cache instance、associativity、
LRU 及带 aging/tie-break 的 LFU。

普通 SWCC read/write/atomic 不进入这套自动 coherence 状态机。只有真实
`clflush`/`sfence` 后的显式 flush/handoff 协议才会产生 SWCC visibility 事件；
模型状态不会成为业务可见性的证明。

## 线程与阶段覆盖

池初始化/attach、warmup、foreground replay、Masstree background trigger、
promotion、demotion、cooler、adjuster、RPC/完成路径、RCU drain、recovery 和
shutdown 都在各自实际执行线程绑定 allocator/directory 后进入统一入口。统计在
generation/phase 边界取样，不在线程活跃时清零。E2E 在前台和 replica worker
停止后再通过一次全局 drain barrier，之后才验证有序 event log，避免把其它 VM
已预留但未提交的记录误判为丢失。

## 覆盖审计与验证

`tests/test_latency_access_audit.py` 对 `dsidle/` 和 D-SIDLE 修改过的 Masstree
文件建立共享地址/offset/NodeRef allowlist；allowlist 只表示已人工逐行审查，
不替代动态 canary。当前审查明确将 JSON、trace buffer、字符串工具、扫描输出和
本地副本 memcpy 归入 local DRAM，将节点 envelope、value row、suffix、allocator
link、HWCC controls 和所有业务原子列入 wrapper 覆盖矩阵。

确定性验证包括：四 section 配置组合和旧字段拒绝、固定延迟结算点、HWCC
operation/line/byte、CAS attempt/result/memory-order、跨 VA stable offset、全局
sequence replay、LRU/LFU/capacity、SWCC explicit handoff、四模块分别启用、全启用、
全关闭，以及编译关闭和 RelWithDebInfo disabled benchmark/反汇编检查。功能 E2E
使用全关闭配置；硬件模拟 smoke 使用固定延迟 0，不能混入尾延迟性能实验。
