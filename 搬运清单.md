# 搬运清单

## M7：VM 镜像制作（与 cxlkv 完全对齐）

| 来源 | 固定版本 | 目标 | 处理 |
| --- | --- | --- | --- |
| `../cxlkv/thirdparty_libs/tigon/emulation/image/make_vm_img.sh` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `image/make_vm_img.sh` | 原样拷贝；宿主机代理、SSH 密钥、apt 源、swap、chrony/ptp、pinned 内核依赖的构建前 `mkosi.extra` 预置与 `sudo mkosi -f build`。 |
| `../cxlkv/thirdparty_libs/tigon/emulation/image/mkosi.default` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `image/mkosi.default` | 原样拷贝；Jammy、BIOS、`RootSize=38G`、完整 Packages 列表、`PostInstallationScript=mkosi_postinst.sh`。 |
| `../cxlkv/thirdparty_libs/tigon/emulation/image/mkosi_postinst.sh` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `image/mkosi_postinst.sh` | 原样拷贝；钉死 `5.15.0-173-generic`、Java/Rust/LLVM/gh 等 postinst 收尾。 |
| `../cxlkv/thirdparty_libs/tigon/emulation/image/ubuntu_rootfs.sh` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `image/ubuntu_rootfs.sh` | 原样拷贝；可选复制宿主机 APT 配置进 `mkosi.extra`。 |
| `../cxlkv/xz_scripts/init_scripts_env_2_make_vm_img.fish` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `dsidle_make_vm_img.sh` | 语义对齐为 bash 包装：已存在则跳过、`--force` 重建、调用本树 `image/make_vm_img.sh`；额外保留 `--dry-run` / `--config`（`--config` 仅为与其它脚本 API 兼容，镜像 SSH 仍按 cxlkv 使用宿主机 `~/.ssh`）。 |

## M7：VM 启动与 ivshmem 数据面

| 来源 | 固定版本 | 目标 | 处理 |
| --- | --- | --- | --- |
| `../cxlkv/xz_scripts/init_scripts_env_99_clear_vm_data.fish` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `dsidle_kill_vms.sh` | bash 对齐：危险路径拒绝、pidfile + `pgrep -f` 双轮 `kill -9`、清空 storage 内容、删除 ivshmem 产物、卸载共享 tmpfs；额外清理遗留 `dsidle_shared.pool`。 |
| `../cxlkv/xz_scripts/rust_utils/init_vm/src/resources/{etc_hosts_template,gai.conf}` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `scripts/cxlkv_vm_assets/` | 原样拷贝；guestmount 注入网络与 SSH 时使用。 |
| `../cxlkv/thirdparty_libs/tigon/emulation/ivshmem/ivshmem-kernel/` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `third_party/ivshmem-kernel`（init 时 rsync 进 guest） | 不再在宿主预编译 `.ko` 分发；与 cxlkv 一样在每台 guest 内构建并 `modprobe ivshmem_driver`（设备节点 `/dev/ivpci0`）。 |

## M7：YCSB trace 生成器

| 来源 | 固定版本 | 目标 | 处理 |
| --- | --- | --- | --- |
| `https://github.com/J-XZ/YCSB-cpp.git` | `746415127173e7711f134944dbcd92b8216c47e7` (`cxlkv_trace_ycsb_5m_20260621`) | `third_party/YCSB-cpp` | 本仓 git submodule；使用其 `scripts/generate_cxlkv_trace.sh` 生成独立的 CXLKV 格式 trace，不在构建或运行时调用兄弟仓。 |

## M6：四模块硬件模拟器（当前实现）

| 来源 | 固定版本 | 目标 | 处理 |
| --- | --- | --- | --- |
| `../cxlkv/src/utils/include/latency_simulator.h` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `dsidle/latency_simulator.h` | 保留 `latency_sim` 配置、统计与作用域 API；增加 Sidle 插桩封装。 |
| `../cxlkv/src/utils/src/latency_simulator.cc` | `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` | `dsidle/latency_simulator.cc` | 仅借鉴 TSC 校准与线程本地延迟结算结构；当前实现删除固定命中率/cache-hit/per-thread LRU 近似，独立实现四模块、真实原子 wrapper、全局有序 remote event log。 |
