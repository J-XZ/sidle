# D-SIDLE 组件维护边界

本仓库的运行时、配置、脚本、测试和 guest image 均由 D-SIDLE 独立维护。
任何通用固定延迟辅助代码复制进入本仓库后都视为本仓源码，不通过其它项目的
include、link、import、symlink、配置、二进制、trace、backing 或 build 目录
运行。

## 当前 owned components

| 组件 | 责任 |
| --- | --- |
| dsidle/latency_simulator | fixed latency、access wrapper、scope 和 TSC busy-wait |
| dsidle/shared_pool、shard_allocator、epoch | 业务 HWCC/SWCC 布局和生命周期 |
| third_party/masstree-beta 适配 | canonical SWCC、visibility 和 replica 访问 |
| scripts、根目录 VM/YCSB 入口 | 本仓配置、trace、镜像和运行目录 |
| tests | parser、访问覆盖、协议、关闭快速门和 E2E smoke |

共享池只包含业务 ivshmem /dev/ivpci0。普通 coordination metadata 用于
epoch/phase 控制，不能重新扩展为延迟模拟状态。
