# D-SIDLE 延迟与硬件模拟审计报告

本文只描述当前实现。旧版本的 `cache_model`、cache-hit/cache-miss、hit/miss
统计和 detached `Record*` API 已从当前路径移除；历史提交中的同名文字不属于
当前配置或运行时语义。四模块的完整说明见 [硬件模拟当前实现.md](硬件模拟当前实现.md)。

## 访问覆盖矩阵

| 访问类别 | 当前执行入口 | 域/结算 | 审计结论 |
| --- | --- | --- | --- |
| NodeControl、RootControl、共享 epoch/phase、shard control | `CountedMemory*`、`CountedAtomic*` | HWCC；原子不进入 ordinary read/write | 已覆盖 init、attach、foreground、background、recovery、drain、shutdown |
| canonical node body | `nodeversion::stable`、`unlock_impl`、`publish_body_before_edge` | SWCC；按 NodeControl 的实际 allocation envelope | 上游裸字段访问由单节点范围一次有界覆盖，避免逐字段重复收费 |
| value row metadata/bytes | `third_party/masstree-beta/dsidle_value_access.hh` 与 value row helpers | SWCC；实际执行后观测 | Bag 当前为编译生效 row；Array/VersionedArray/String 源文件也使用同一 helper |
| external suffix、leaf link | `readable_external_ksuf`、`assign_ksuf`、`CountedMemory*` | SWCC；suffix 按实际字节 | sizing pass 不观测；唯一 source copy 观测一次 |
| SWCC free-list link/header | `FixedBlockShardAllocator::Pop/Push/Allocate` | SWCC 数据 + HWCC head/bump/pop-lock 原子 | 普通 link、清零、flush 和控制原子不重复收费 |
| local replica/directory/worker/heartbeat | `CountedAtomic*` with `kLocalDram` | local DRAM | 不生成 SWCC/HWCC/remote 事件；atomic 模块可独立统计 |
| trace、JSON、扫描临时 buffer、上游工具字符串 | 无共享域 wrapper | local DRAM/transient | allowlist 中逐项分类，不把业务共享对象隐藏在这里 |

节点体的 stable read 和 unlock/publish write 是协议级 envelope，而不是逐字段
模型；它保留原 Masstree 锁/版本/发布语义。value row 的 typed load/store 与
bulk copy 紧邻真实操作；副本布局计算用不观测 sizing pass，复制时只记录一次
canonical source read，process-local destination 不计 SWCC。

## 作用域与安全结算

- foreground trace op 使用 `ScopeGuard(kForeground)`；merge、trigger、promotion、
  demotion、cooler、adjuster 使用 `ScopeGuard(kMerge)`。
- pending delay 只在线程本地累加，并在业务锁、RCU、pin、ring ownership 和发布
  状态释放后 busy-spin 补齐；关键区内没有 busy-wait，也没有 `sleep_for` 延迟
  fallback。
- flush/invalidate 执行真实 `clflush`/`sfence` 后作为协议事件记录；固定延迟模块
  不把它们再当作第二次普通写。
- generation/phase 只在 startup/quiescent 边界重置；E2E 停止 foreground 和
  replica workers 后通过最终 drain barrier，随后才验证共享有序 event log。

## 远程失效与 SWCC 边界

remote 统计使用共享 instrumentation-only sequencer/event log，事件 identity 是
`(memory_domain, pool_id, pool_offset/cache_line_bytes)`，不使用进程虚拟地址。
sequence 缺口、重复、溢出、非法状态和跨 VA replay 不一致都会 hard fail。HWCC
状态区分 dirty owner→reader、dirty owner→writer/RMW、clean-copy invalidation 和
dirty-capacity eviction；容量支持 per-node/total/explicit、cache instance、
associativity、LRU 和带 aging/tie-break 的 LFU。

普通 SWCC read/write/atomic 不自动改变其他 VM 的 cache 状态。SWCC 只有真实
flush/writeback/invalidate 协议触发的 explicit handoff 才进入 remote visibility
统计；模拟器状态不能被业务当作可见性证书。

## 禁用路径与配置审计

四个 section 都有独立 `enabled` 和内部维度开关。parser 拒绝未知、重复、缺失、
非法范围、负数以及遗留 cache-hit 字段；完整 JSONC、启动摘要、policy/layout
hash 和配置测试保持一致。全关路径只有一个 relaxed process-local feature gate；
不访问 TLS、时钟、地址换算、cache state、全局锁、统计原子、IPC 或动态内存。

`tests/test_latency_access_audit.py` 的 allowlist 仅表示人工审查过共享地址获得点；
它与 direct bulk/atomic 扫描、确定性状态机测试、跨映射 canary、四模块单独/全启用
smoke、全关闭功能 E2E、compile-off disabled benchmark 和反汇编共同构成三层审计。

## 当前验证入口

- `dsidle_config_tests`：完整四 section、组合开关、旧字段拒绝和 RelWithDebInfo 合同。
- `dsidle_latency_simulator_tests`：固定延迟行数/结算、HWCC op/line/byte、原子
  attempt/result/order、global sequence、LRU/LFU、SWCC explicit handoff。
- `dsidle_shared_pool_tests`、`dsidle_shard_allocator_tests`、`dsidle_epoch_tests`、
  `dsidle_node_control_tests`：布局、稳定 offset、阶段/回收和域分类。
- `dsidle_masstree_single_process_tests`：canonical value/suffix、leaf replica、
  scan、generation reuse 和副本命中不读取 SWCC。
- `dsidle_latency_access_audit_tests` 与 `dsidle_disabled_overhead_tests`：静态
  allowlist、编译关闭和关闭路径运行/反汇编检查。

功能 E2E 使用全关闭配置；硬件 smoke 固定延迟为 0，不能混入尾延迟实验。
