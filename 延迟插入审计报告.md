# D-SIDLE 延迟插入审计

## 审计结论

当前生产路径只保留 fixed latency。访问按真实地址覆盖的 cache line 收费，
pending delay 在线程本地累积，并在业务安全出口使用校准 TSC busy-wait 结算。
源码、配置和测试不依赖第二块共享设备或共享模拟日志。

## 覆盖矩阵

| 访问区域 | 入口 | scope |
| --- | --- | --- |
| HWCC 控制项、epoch、phase、allocator head | FixedLatencyMemory/Atomic wrapper | 前台或后台 |
| SWCC canonical node、suffix、value、free-list object | Masstree access wrapper 和真实 flush/invalidate | 前台或后台 |
| owner-private SWCC | 同一 SWCC wrapper，使用 owner-private 域 | 对应 worker |
| replica directory、策略状态、heartbeat | local-DRAM atomic wrapper | 对应 worker |

每个 wrapper 只在自己执行的真实操作之后调用一次访问记录接口。Masstree 的
上层 query 不会再次对同一地址收费；flush/invalidate 是独立的真实可见性操作，
只记录其自身覆盖范围。

## 生命周期审查

- foreground trace runner 和 suite runner 在单个逻辑操作外层建立 scope；
- replica worker 的 trigger、promotion、demotion 和 cooler 在各自 worker 中建立
  background scope；
- scope 析构发生在 query/RCU/latch 逻辑退出之后，最外层才 busy-wait；
- worker 的 pending delay 位于 thread-local state，不能泄漏到另一个 worker；
- disabled、异常和早返回路径不会初始化延迟 TLS 或保留 pending delay。

## 配置和布局

parser 只接受 fixed_latency 六字段对象，并验证几何、非负有限数值、构建类型和
安全检查开关。共享池初始化只分配业务 metadata、epoch 和 phase coordination；
HWCC 容量描述与 allocator 实际可用空间一致。启动命令行只传递
/dev/ivpci0，不创建第二设备。

## 测试证据

Debug 和 RelWithDebInfo 均构建完整测试目标。定向测试覆盖 line coverage、重复
访问、不同域延迟、原子语义、CAS 失败 expected、内存顺序、嵌套 scope、后台隔离、
RAII、关闭快速门、TSC 校准和遗留 JSONC 字段拒绝。静态测试检查 production access
allowlist；共享池和 Masstree 测试检查协议行为没有被删除。

VM 证据必须记录本仓 image/root.img、独立生成的 trace、pool 初始化输出、无延迟
回放和非零固定延迟 canary；canary 只检查受控 wall-time 和成功退出，不依赖
访问次数输出。
