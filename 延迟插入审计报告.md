# 延迟插入审计报告

本报告对应 PLAN §4.6；状态基于当前工作树的静态审计与 CTest。

| 访问类别 | 统一 wrapper | 已接入位置 | 状态 |
| --- | --- | --- | --- |
| 规范节点体读 | `RecordSwccRead` | `ResolveCanonicalNode` 首行 + `NodeVersionAccessor` 实际分配字节 | 规范路径已覆盖；NodeControl 保存真实节点字节数，避免按 512B 上限多计相邻对象。节点内字段沿用原 Masstree 裸访问，因此以单节点实际分配范围作为有界上限。 |
| 值对象读/写 | `RecordSwccRead/Write` | `leafvalue::value/set_value` | 已覆盖。 |
| 外部 key suffix | `RecordSwccRead/Write` | `readable_external_ksuf/assign_ksuf` | 已覆盖；已发布 bag 只做 COW，不原地更新。物理 invalidate 仍保守覆盖 bag 容量，延迟计费只覆盖描述符元数据和实际访问的 suffix 字节。 |
| SWCC flush | `RecordSwccFlush` | `FlushSwccRange`、allocator free-list publish | 已覆盖且每个实际 flush 只计一次；read-side invalidate 不误计为写回。 |
| 分配器自由链 | `RecordSwccRead/Write` + `RecordHwccAtomic*` | `FixedBlockShardAllocator::Pop/Push/Allocate` | SWCC link、HWCC head/bump/limit 均覆盖。 |
| 节点控制/版本 | `RecordHwccRead/Write/Atomic*` | `NodeControlSlab`、`NodeVersionAccessor` | allocation state、generation、offset、size、version、free head 均覆盖。 |
| RootControl / epoch | `RecordHwccAtomic*` | Root accessor、共享 epoch clock/slots | 池内共享对象已覆盖；仅供单进程测试的本地 `EpochTable` 是普通 DRAM，不计 HWCC。 |
| 本地副本 / 队列 / 直方图 | 无 | process-local data | 明确不计费。 |

## 作用域与统计

- 前台 runner 每个 trace op 使用 `ScopeGuard(kForeground)`；query 返回后其 RCU
  scope 与节点锁已释放，析构时才 busy-spin。
- SIDLE trigger/promotion/demotion/cooler 使用 `ScopeGuard(kMerge)`；延迟同样只在
  本轮锁和 RCU 区间退出后补齐。adjuster 只访问进程本地策略状态，不计池延迟。
- 完整的本地副本 GET 从 HWCC `RootControl`/`NodeControl`
  `{NodeRef,generation,version}` 验证不可变快照；命中值后不 resolve、invalidate
  或读取规范 SWCC 叶。单测要求这条路径的 `swcc_raw_line_accesses == 0`。
- 叶副本构建在持有规范叶锁时单次读取 suffix/value，并把已验证的本地描述符
  用于布局和拷贝；不因两遍布局计算重复计算 SWCC 读流量。
- scan 进入某个规范叶后可使用版本匹配的本地 value；检查目录时不再次
  invalidate/计费同一规范叶，版本失配仍回退规范路径并惰性清理旧副本。
- `PrintAndResetLatencySimulatorStats` 输出 raw/hit/miss/delayed_ns；单元测试
  验证 `raw=hits+misses` 与 SWCC/HWCC 延迟聚合。
- 当前每个 runner 进程只在结束时打印并 reset 一次，所以带 phase tag 的
  `LATENCY_SIM_STATS` 是该进程的生命周期统计：包含计时前 root pin/后台启动，
  以及计时结束后的 worker Stop 收尾访问。`E2E_TRACE_TIME_US` 本身在起 barrier
  后开始、止 barrier 前结束，不包含这些工作。cxlkv 当前也采用生命周期近似；
  若未来要求严格相位统计，应双方同时增加后台启动门，不能在后台 scope 执行中
  直接清零。
- `enabled=false` 时 wrapper 只执行一个 relaxed 全局 gate；`ScopeGuard` 的
  gate 强制内联，enabled 分支才调用 noinline slow helper。RelWithDebInfo+LTO
  反汇编确认 hot path 不再调用 guard 构造/析构、不会建立原先约 256B 的函数
  栈帧，也不会建立 TLS cache、统计、TSC 校准或 busy-spin。SWCC 与 HWCC 的
  读、写、flush、atomic 延迟字段保持独立配置。
- `latency_inject.enabled=true` 按 CMake 的精确配置名只接受
  `RelWithDebInfo`，并继续要求 `verbose=false`、`extra_check=false`；
  `Release` 不能因同样定义了 `NDEBUG` 而绕过合同。

## 已知边界

- 模型不模拟预取、MLP 和跨节点 invalidation 流量（与 PLAN 的精度声明一致）。
- Masstree 仍沿用原始原地节点更新。unlock 按实际节点分配范围计一次 SWCC
  publication write 与 flush；它是协议级 cacheline 模型，不声称区分该范围内
  每个 C++ 字段的 dirty byte，也没有为追求模拟精度重写原始 Masstree 更新流。
- 规范节点读同样按实际分配范围作为保守上限：原 Masstree 会直接读取节点字段
  并预取节点内多条 cacheline，所选 child/value/link 也可能位于分配范围末行。
  value body 与外部 ksuf 仍单独按实际访问字节计费。
- `SharedPhaseBarrier` 虽位于 HWCC，但只承担 phase 编排；正式 runner 把起止
  barrier 放在逐操作 latency scope 和吞吐计时窗口之外并单独报告，因此它不计入
  workload 的 HWCC 延迟统计。池初始化、attach 和布局校验同样属于 setup，
  不纳入逐操作访问模型。

## 聚焦验证

- `dsidle_latency_access_audit_tests`：扫描 D-SIDLE 与就地修改的 Masstree
  源文件；任何新出现共享地址获得点的文件必须先逐行审查 wrapper/scope，再显式
  加入 allowlist。
- `dsidle_latency_simulator_tests`：cache 模型、池独立配置、raw/hit/miss
  恒等式与后台 scope 开关。
- `dsidle_config_tests`：精确 `RelWithDebInfo` 启用合同。
- `dsidle_epoch_tests`：本地 EpochTable 的 SWCC/HWCC raw 均为零；共享
  epoch/barrier 的跨进程功能保持成立。
- `dsidle_node_control_tests`：规范节点按实际分配范围计费。
- `dsidle_masstree_single_process_tests`：外部 ksuf 按实际长度计费、叶副本
  构建只读一遍、完整副本 GET 不访问 SWCC、scan 副本减少 SWCC 读取且拒绝旧版本。
