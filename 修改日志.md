# D-SIDLE 当前变更摘要

本文件只保留当前交付摘要；详细施工过程和旧实验记录由 Git 历史保存。

## 2026-08

- 延迟模拟收敛为 fixed-latency-only：按真实 HWCC/SWCC 访问覆盖的 cache line
  累加 pending delay，使用线程 scope 和校准 TSC 在安全点结算。
- 删除访问统计、原子操作累计、远程可见性模拟、共享事件日志、额外设备和
  对应共享池预留；保留真实 SWCC flush/invalidate、epoch、replica 和
  owner-free 协议。
- parser 收紧为 fixed_latency 六字段，未知和遗留配置 hard fail。
- runner、Masstree、allocator、共享池、配置 fixture、定向测试和静态审计同步；
  disabled benchmark 保留运行时快速门与编译期关闭对照。
- R6525 VM 验证使用本仓 image/root.img、业务 ivshmem、独立 trace 和新 backing；
  测试后停止 QEMU 并清理运行数据。
- 历史实验日志、可重建 trace、旧 build 和缓存不属于最终仓库状态。
