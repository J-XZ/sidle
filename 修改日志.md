# D-SIDLE 修改日志

基线：`1f1fa8ae9459935963a67a9beef6f30683c3c47b`（final release）。

本日志从 M0 开始维护；每个里程碑和架构检查点均记录提交、测试与差异。

## 2026-07-24 — M0：回滚与 D-SIDLE 骨架

- 代码检查点：`7c8f4b0378e655df3acb6bb6d26e1544eca98820`。
- 已建立 `dsidle/` 配置骨架、根 `experiment_config.jsonc` 与 CTest 配置契约；
  根 CMake 仅构建 D-SIDLE 骨架，已从构建图摘除基线 DAX/memkind、ART 与旧 benchmark。
- 验证：`cmake -S . -B /tmp/dsidle-m0-clang-build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build /tmp/dsidle-m0-clang-build --parallel && ctest --test-dir /tmp/dsidle-m0-clang-build --output-on-failure`（1/1 通过）。
- 环境说明：验收配置使用 Clang 18；默认 GCC 11 不支持合同要求的 `-flto=full`，因此 CMake 明确要求 Clang，未降低优化或 LTO 级别。

## 2026-07-24 — M1 架构检查点：共享池首次 attach

- 代码检查点：`fe3da1cd064d64fa0a24519aee0744aa5f5d6e04`。
- 新增双区域 `SharedPool`：PoolHeader/RootControl 固定于 HWCC 起点，布局仅允许 `hwcc.offset=0`、相接 SWCC，并在 create/attach 时校验 ABI、容量和跨进程 64 位 lock-free 原子。
- 新增 64B 对齐的 `NodeControl`/`RootControl` 与 64 位带类型 offset 基元；未接入基线 Masstree 前不改动其行为。
- 验证：`cmake -S . -B /tmp/dsidle-m1-build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`（2/2 通过；共享池测试含 fork 后 attach、SWCC 载荷与 HWCC 根版本可见性）。

## 2026-07-24 — M1 架构检查点：SWCC shard 分配器

- 代码检查点：`f74df50d344d6eed51379eab5a2e27d7dbe70e7d`。
- 已实现固定 size-class 的每 VM SWCC shard bump 区、HWCC local/remote-free 栈头及 offset 空闲链；remote-free 发布会逐行 flush 并 fence，单次分配最多收割 64 个对象。
- 验证：RelWithDebInfo 构建与 CTest（3/3 通过）。

## 2026-07-24 — M1：池与分配器完成

- 代码检查点：`aa41e30f51189bc67f74a1dff7a24b074f1c928a`。
- 新增 `dsidle_shared_pool --init-pool`：对已由宿主创建/清零的 backing 写入
  PoolHeader、RootControl、NodeControl slab、每 VM shard 控制区、共享 epoch 槽和
  诊断区；按配置打印 HWCC 预算并在容量不足时 hard fail。该路径只改固定元数据，
  不会误触完整 SWCC 区而重复预填充。
- NodeControl slab 使用 HWCC 空闲链，发布前写 canonical SWCC offset、递增
  generation，且要求 RETIRING 后才可归还；shard allocator 改为使用该固定布局。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（4/4 通过）；其中 shard 专项由子进程
  attach 后 remote-free，父进程 harvest 并复用同一 SWCC offset。另以 16MiB
  临时 layout 执行 `dsidle_shared_pool --init-pool --node-control-capacity 64
  --max-threads-per-vm 2`，成功输出 HWCC 预算。

## 2026-07-24 — M2 架构检查点：HWCC 版本与根访问器

- 代码检查点：`49ceccec1286abc21b296610455b2fdeb44ea760`。
- `NodeVersionAccessor` 将基线 32-bit Masstree nodeversion 的锁、插入、分裂、
  删除、根和 migration 位布局原样映射到 `NodeControl.version_and_state`；稳定
  读取同时取得 generation 与 canonical SWCC offset。新增 `RootControlAccessor`
  执行版本化 root-ref/root-generation 发布与双检读取。
- CTest target 均显式以 `-UNDEBUG` 编译，使 RelWithDebInfo 下的断言真实执行；
  新增 NodeControl/RootControl 单测，覆盖 slab generation 复用、root 发布以及
  插入/分裂版本跃迁。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（5/5 通过）。

## 2026-07-24 — M2 架构检查点：按进程解析共享 offset

- 代码检查点：`a07a3d78765bcd155dd080811c121f0d0e0db08f`。
- SharedPool create/attach 现在登记本进程（线程）的映射基址；该基址不进入共享
  backing，仅为后续 Masstree `NodeRef`/`SwccOffset` 解引用提供不同基址安全的
  本地解析点。close 会清除对应登记，未 attach 即解析会 hard fail。
- 验证：RelWithDebInfo 构建与 CTest（5/5 通过）。

## 2026-07-24 — M2 架构检查点：SWCC size-class slab

- 代码检查点：`fc9030281ab5b8ddb4f615d585b05f8d997fdd0f`。
- shard 分配器扩展为 64B 到 2MiB 的 16 个 2 的幂 size class；每 VM、每 class
  有独立 HWCC 控制行与 SWCC bump 区/本地 free 链/remote-free 链。`SwccShardAllocator`
  按对象大小选择 class，保持对象引用为 offset；生产 `--init-pool` 初始化全部
  class，单 class 初始化仅保留给专项测试。
- 验证：RelWithDebInfo CTest（5/5 通过）；分配器专项覆盖 97B、17KB、900KB
  三类分配，以及 remote-free harvest 后按原 offset 复用。

## 2026-07-24 — M2 架构检查点：原生 Masstree 接入 SWCC

- 代码检查点：`3dd6c5530e90a154ea507be5c20eaaed4dbdbd66`。
- 新增 `dsidle_masstree`，直接编译仓库内的 Masstree 源码；旧 CXL/memkind
  allocator 实现不进入构建图。运行时将当前进程绑定到一个 SWCC shard，Masstree
  canonical node、value 和 key-suffix 分配均经 D-SIDLE SWCC size-class 分配器，
  不存在 DAX/memkind 回退。SIDLE 前端的 canonical 分配也固定为 SWCC，策略选择
  留待 M5 用于副本。
- 新增原生 Masstree 单进程 PUT/GET CTest，验证其在共享池上完成创建、插入和读取。
  此检查点仍保留基线的进程内裸指针字段及临时本地 RCU epoch 兼容层；NodeRef
  偏移化、NodeControl 实际接管 nodeversion 和跨进程重挂载尚未完成，不能视作 M2
  出口。
- 验证：RelWithDebInfo CTest（6/6 通过）。

## 2026-07-24 — M2 测试检查点：Masstree 单进程对拍

- 代码检查点：`2dd49a36af6fdebf7c3c82e7cd4623b3b30c26d2`。
- 原生 Masstree 测试扩大为 64 个固定 8B key 的乱序写入、覆盖写及逐项 GET，
  使用 `std::map` 保存期望值；该规模会经过叶/内部节点分裂，验证值内容而非
  基线 `run_replace` 的返回枚举。SWCC OOM 诊断现在带 block、offset 和 limit，
  便于定位实际 shard/class 耗尽。
- 验证：RelWithDebInfo CTest（6/6 通过）。仍缺 offset/NodeRef 重写与重挂载，
  因此未达到 M2 退出条件。

## 2026-07-24 — M2 架构检查点：NodeControl 发布序

- 代码检查点：`9ca7a8b733850d6b0ce05a3379a7fd78a756b26c`。
- NodeControl slab 不再把刚取得的槽直接标成 PUBLISHED。调用方必须先 `Reserve`
  （ALLOCATING，写 canonical offset / generation）、初始化并 flush 配对 SWCC
  节点，再以 `Publish(initial_version)` 进行 release 发布；`Retire` 后才允许
  `Release` 回空闲链。`NodeVersionAccessor` 拒绝读取非 PUBLISHED 槽。
- 单测覆盖 leaf 初始位、版本稳定读取、insert/split 位跃迁、retire/release 及槽
  generation 复用；RelWithDebInfo CTest（6/6 通过）。此为接入 Masstree 构造器的
  必要前置，尚未使其节点字段偏移化。

## 2026-07-24 — M2 架构检查点：Masstree 节点成对控制项

- 代码检查点：`6d91ff56c944ddae260c08b162561e9a945b45c1`。
- 原生 leaf/internode 工厂现在为每个 SWCC 节点体 reserve 配对 NodeControl，将
  `NodeRef` 写入节点自身，flush 节点体后以正确的 leaf/non-leaf 初始 version
  发布控制行。当前线程的 SharedPool 绑定成为这条成对分配路径的唯一来源。
- Masstree 单测验证根节点的 NodeRef 已解析至其实际 SWCC offset；RelWithDebInfo
  CTest（6/6 通过）。树边、root、value 和 ksuf 仍未改写为 offset，nodeversion
  也仍由基线字段承载，故本检查点不等同完成偏移化。

## 2026-07-24 — M2 架构检查点：NodeRef 根发布

- 代码检查点：`c0ce0dd4e84c00701864ee640916d180438ccf58`。
- `basic_table` 不再持有持久化 `root_` 裸指针，而保存 NodeRef；`root()` 仅在
  栈上解析 canonical SWCC 地址。初始化和 `fix_root()` 均以 NodeControl 的
  generation 发布 RootControl，scan/print/destroy 等原有直接 root 使用点改为
  瞬时解析。
- Masstree 单测验证 RootControl 的 `{NodeRef,generation}` 与根 NodeControl/SWCC
  offset 一致；RelWithDebInfo CTest（6/6 通过）。child/parent/leaf/value/ksuf
  及 nodeversion 仍待改造，M2 未完成。

## 2026-07-24 — M2 架构检查点：内部 child NodeRef

- 代码检查点：`d2bbd22786391662f6fd49e119068b393ff67134`。
- `internode::child_[]` 已由裸指针改为 8B NodeRef 链接；链接在 SWCC 中只保存
  HWCC control offset，读取时才解析瞬时 canonical 地址。查找、split、复制和
  统计保留原有控制流，仅在模板实参推导无法进行隐式解析的位置增加显式栈上转换。
- 验证：64-key 分裂的 Masstree `std::map` 对拍以及完整 RelWithDebInfo CTest
  （6/6 通过）。parent、leaf link、value/ksuf 和 nodeversion 尚待改造。

## 2026-07-24 — M2 架构检查点：parent NodeRef

- 代码检查点：`6d0612c81b8fa7e9fadca59f6fbc691fd7bb3edf`。
- leaf 与 internode 的 `parent_` 均改为 NodeRef 链接。基线 destroy RCU callback
  原本复用 parent 裸指针作为工作队列 next 字段，现同样复用 NodeRef，避免在销毁
  路径把瞬时虚拟地址回写到 SWCC。
- 验证：RelWithDebInfo CTest（6/6 通过）；child/parent 树边均经 NodeRef 解析，
  但 leaf link、value/ksuf 与 nodeversion 仍待改造。

## 2026-07-24 — M2 架构检查点：leaf link NodeRef

- 代码检查点：`18664f9b91963edbcf3551367c0f75936f90f8f0`。
- leaf 的 `next_`（低位保留基线标记语义）与 `prev_` 均已改为 NodeRef；split、
  remove、scan、print 与链回收路径只在使用点解析瞬时地址。SWCC 链字不再执行
  CAS：M2 的单进程锁前提下使用普通写，跨 VM 的持锁 flush/fence 发布协议留待
  M4 完整实现。
- 验证：RelWithDebInfo CTest（6/6 通过）。value/ksuf 和 nodeversion 仍待改造，
  M2 未完成。

## 2026-07-24 — M2 架构检查点：外部 ksuf SWCC offset

- 代码检查点：`ecb6d8051d83f5a3443b18185ef0e492e7f86699`。
- leaf 的外部 `stringbag` 指针 `ksuf_` 已换为 `SwccOffset<stringbag>`；分配、
  重新分配、读取、预取和 RCU 延迟释放都只在调用点解析瞬时地址。新增共同前缀的
  160B 长键对拍，覆盖 layer / 外部 suffix 容器的写入与读取。
- 验证：RelWithDebInfo CTest（6/6 通过）。leafvalue/value 与 nodeversion 仍待
  改造，M2 未完成。

## 2026-07-24 — M2 架构检查点：leafvalue offset

- 代码检查点：`215f7539679124fb8eeafddc1291670d0bece975`。
- `leafvalue` 的 64-bit 槽不再存 value/layer 虚拟地址：普通 value 解释为
  `SwccOffset<row_type>`，layer 解释为 NodeRef。`tcursor` 及 kvrow/log/checkpoint
  的更新路径改为读取临时指针、完成原有更新逻辑后以 `set_value` 写回 offset，
  消除了 `R*&` 对共享字段的裸指针回写。
- 验证：随机/分裂/覆盖写、长共同前缀键的单进程 `std::map` 对拍和完整
  RelWithDebInfo CTest（6/6 通过）。nodeversion 仍待实际迁入 NodeControl，M2
  未完成。

## 2026-07-24 — M2 架构检查点：nodeversion → NodeControl

- 代码检查点：`690a6a9632d0aac258049a04ea04c7063cc441bd`。
- 保留基线 nodeversion 的全部位布局、stable/lock/unlock/mark 接口与快照语义；
  绑定 NodeRef 的活节点将版本 load/store/CAS 重定向至配对 NodeControl 的
  `version_and_state` HWCC 原子。nodeversion 拷贝仍是栈上稳定快照，不持有第二个
  可写句柄；未绑定的 single-thread 值保持原本地行为。
- 验证：分裂、覆盖写、长共同前缀的 Masstree 对拍与完整 RelWithDebInfo CTest
  （6/6 通过）。M2 剩余重挂载验证与针对不同映射基址的端到端证明。

## 2026-07-24 — M2 完成：不同基址重挂载

- 代码检查点：`7eb683af6789d17a33011ddca0a8c25cb85ece89`。
- `basic_table::attach()` 从固定 RootControl 采用 `{NodeRef,generation}` 并验证
  对应 NodeControl。测试关闭初始映射、以匿名 guard 占住旧虚拟基址，再通过
  `AttachAt` 把同一池固定映射到另一个地址；新 table attach 后逐项读取完整
  `std::map` 期望集（含分裂、覆盖写与共同前缀长键）。
- 审计 root、child、parent、leaf link、ksuf、leafvalue 的持久化字段均不再为
  裸指针；nodeversion 活节点状态位位于 NodeControl。
- 验证：RelWithDebInfo CTest（6/6 通过）。至此满足 M2“单进程 std::map 对拍 +
  重挂载 + commit/log”退出条件；后续进入 M3 跨进程预填充树的 Get/覆盖 Put/Delete。

## 2026-07-24 — M3 检查点：跨进程预填充树读取

- 代码检查点：`3692216f8d7d371b3ef50cedd0a49e79a614b3f2`。
- Masstree 测试现 fork 子进程：子进程关闭继承映射、重新 attach backing、绑定
  SWCC shard、独立创建 threadinfo/table 并从 RootControl attach；随后读取父进程
  预填充的完整期望集（分裂、覆盖写、长共同前缀键）。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 的覆盖 Put/Delete 和 SWCC 写入
  可见性发布仍待实现。

## 2026-07-24 — M3 检查点：跨进程覆盖 PUT / DELETE

- 代码检查点：`a36c79b829806facb9ee0ce83ed39d849b66d133`。
- 子进程独立 attach 后覆盖既有 key，父进程验证新值；另一子进程删除既有 key，
  父进程验证后续 Get 不再命中。两者均在预填充树上执行，不引入 split。
- 新增 `swcc_visibility.h`：value 槽写回前 flush 新 value，nodeversion unlock
  在 HWCC release 发布前 flush 对应 canonical 节点的基线 512B 节点包络，确立
  `SWCC 写/flush → HWCC version release` 顺序。Delete 专项同时补齐测试翻译单元
  对原生 `masstree_remove.hh` 的依赖。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 仍缺更严格的多进程线性化/规范
  值契约验证，M4 前不执行 SMO。

## 2026-07-24 — M3 检查点：SWCC 读侧 invalidate

- 代码检查点：`cb1f8d32ab9f7363df15dca1d786e9aa08515d6d`。
- `NodeVersionAccessor` 驱动的 Masstree `stable()` 在 HWCC 稳定版读取后精确
  invalidate 该 canonical 节点的 512B 基线包络；leafvalue 读取 value 前先
  invalidate value 首行并据 size invalidate 完整对象。写侧已有 flush/fence 后
  才 release 版本，因此读路径按 `稳定版本 → invalidate → SWCC 读` 执行。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 仍需以并发用例验证线性化，不进入
  M4 的 SMO/scan/reclaim。

## 2026-07-24 — M3 检查点：并发跨进程覆盖写

- 代码检查点：`440c4500442c919207c1afeb36c759832bdb0c3a`。
- 两个子进程分别重新 attach、经 pipe 屏障同时对同一预填充 key 执行覆盖写；父
  进程等待两者结束后读取该 key，断言结果恰为两个完整获胜值之一。该专项保持在
  既有叶内更新，不触发 split，覆盖 NodeControl CAS 锁与 release 版本发布。
- 验证：RelWithDebInfo CTest（6/6 通过）。此为 M3 线性化基础验证；更高冲突
  SMO、scan 和回收仍属 M4。

## 2026-07-24 — M3 完成：跨进程预填充树操作

- 退出证据：子进程重新 attach 后读取预填充完整树；独立子进程覆盖 PUT、DELETE
  后父进程观察到新值/缺失；两个同步起跑子进程竞争同一既有 key 后父进程只读到
  一个完整获胜值。所有 M3 写均为既有 leaf 内更新，不触发 split。
- 协议证据：写路径为新 value flush、canonical node flush、HWCC nodeversion
  release；读路径为 stable HWCC version、SWCC invalidate、value invalidate、
  基线 Masstree 原有 has_changed 复检。引用均经 offset/NodeRef 解析。
- 验证：RelWithDebInfo CTest（6/6 通过）。后续进入 M4 的 SMO、叶链、Scan 与
  epoch/limbo 回收；M3 的预填充树范围不替代这些测试。

## 2026-07-24 — M4 检查点：跨叶 Scan 对拍

- 代码检查点：`85ce90a4a5979ff63287ac092789f3c3ab14136b`。
- 新增直接调用原生 `basic_table::scan` 的 collector，对删除后的完整树逐条收集
  key/value，并与 `std::map` 的有序结果严格对拍。该树已包含多次 split、layer/
  长 key、跨进程覆盖写和 DELETE，因此覆盖 leaf link NodeRef 行走与 value offset
  读取的跨叶路径。
- 验证：RelWithDebInfo CTest（6/6 通过）。并发 Scan∥Delete、SMO 发布顺序及
  epoch/limbo 回收仍待 M4 后续实现。

## 2026-07-24 — M4 检查点：跨进程 split 可达性

- 代码检查点：`54f3832429d039ff3ea2643f0e018aaa9fade2be`。
- 独立 attach 的子进程向既有共享树插入 48 个新 key，触发额外 leaf/internal
  split；父进程随后逐项 Get 所有新增值并对全树执行 Scan/`std::map` 对拍。
  该路径验证 NodeRef child/parent、RootControl 和 leaf link 在跨进程 SMO 后仍
  能正确解析并行走。
- 验证：RelWithDebInfo CTest（6/6 通过）。并发 Scan∥Delete 与 epoch/limbo
  回收仍待 M4 后续实现。

## 2026-07-24 — M4 检查点：跨进程 Scan ∥ Delete

- 代码检查点：`42387e8824f9c9f6cb74baf5fcf0d65d5d80eb7e`。
- 父进程的原生版本化 Scan 在首个 leaf 回调时经 pipe 放行子进程，子进程独立
  attach 并删除一个既有 split key；扫描在删除并发期间安全结束，等待子进程后
  父进程验证该 key 不再可读。测试刻意延长首叶回调以形成真实重叠窗口。
- 验证：RelWithDebInfo CTest（6/6 通过）。epoch/limbo 回收仍待实现。
