# D-SIDLE 修改日志

> 2026-08 当前实现说明：本日志中的早期 `cache_model`、cache-hit/cache-miss、
> hit/miss 统计和 detached `Record*` 记录均为历史施工记录。当前运行时和测试
> 合同以 [硬件模拟当前实现.md](硬件模拟当前实现.md) 与
> [延迟插入审计报告.md](延迟插入审计报告.md) 为准；四模块独立配置为
> `fixed_latency`、`hwcc_access_count`、`atomic_count`、
> `remote_cache_invalidation`。

## 2026-07-26 — 架构复核收口与正式验收证据冻结

- `4efbb19` 至 `79ef569` 沿用原 SIDLE 的 leaf traversal、访问计数、初始
  placement、父子 promotion/demotion 与三态 adjuster 关系，只把跨 VM 不可用的
  raw pointer 队列项替换为 `NodeRef+generation`，并把迁移动作挂接为副本
  publish/refresh/invalidate。canonical 写入只令快照 stale、不抹掉策略 intent；
  worker 据 intent 区分 cold demotion、hot refresh 与首次 promotion，避免平行
  重写策略或用劣质 cold-leaf 特例污染对比。
- `f980d96` 令 RootControl 基于 `{ref,version}` 条件发布并沿 HWCC parent 链收敛
  到真实根，统一 root/fix_root/scan/worker 入口，防止连续 split 后旧写者把根
  倒退到 nonroot；`f0256d9` 只为启动时真实锁竞争增加有界重试。root pin 不再
  淘汰 split 后仍存活的旧根，layer root 仍受原预算策略约束。
- `5ccef25` 在本 VM 节点 retire 时立即撤销本地副本；`3fdcd27` 仅在预算压力下
  清扫其他 VM 已 retire 的副本，避免 steady-state 周期扫描开销。
  `c68408d`/`b92bcfa` 让 Scan 复用原 Masstree permutation 位置并补齐正向、
  反向、跨 layer 的逐 key/value 覆盖，没有另造排序或全局 scan gate。
- `8158105` 将 `host_cpu.ivshmem_server_cores` 保留为 cxlkv 配置兼容字段，但
  ivshmem-plain 模式不再校验、预留或空占这两个核。`50e9767`、`e914316`、
  `5c99e92` 删除未引用的 pointer-migration adapter、tree iterator、偶然终端
  debug 输出并忽略 Python bytecode；旧 ART/benchmark facade 继续整体硬隔离，
  未为本轮对比“修活”第二套树。
- `b59402c` 增加显式 `--formal-acceptance` 和 fail-closed 证据生成器：只有
  精确 10 轮、4 VM×4 worker、8 vCPU、32GiB、100k 冻结计数、关闭延迟且
  tracked worktree clean 的 VM E2E/YCSB 才原子写 `acceptance.meta`；开发运行
  只写 `run_complete.meta`。清单绑定 Git、runner、pool、host/guest config、
  trace/normalization、每轮状态、所有节点日志和汇总 SHA256，并拒绝复用已完成
  输出目录。`e6f6005` 又修正从仓库外调用时默认配置和 git provenance 受 cwd
  影响的问题。
- 最终延迟反汇编审计发现 disabled `ScopeGuard` 虽然先查 relaxed gate，旧的
  out-of-line 构造/析构仍各自建立约 256B 栈帧。现将 gate 强制内联，只有
  enabled 分支调用 noinline slow helper；关闭注入时每 op 仅保留 load+branch，
  enabled 的嵌套 scope、延迟补齐与统计语义不变。SWCC-only/HWCC-only、merge
  开关、配置、epoch、NodeControl 和 Masstree 延迟专项均通过。另补上 PLAN
  要求但此前缺失的共享地址获得点 allowlist CTest；以后新文件直接接触共享
  pool/offset/NodeRef 时必须先逐行审查 wrapper 与 scope。
- 第一轮正式门槛的单测 240/240 和 e2e08 10/10 已通过，但 e2e09 第 1 轮
  update 暴露 1024B SWCC free-list 被已分配 value 覆盖，故整轮证据按合同
  作废。GDB 和逐对象 RCU 追踪确认每个 old value 只 retire/free 一次；进一步
  将 allocator 压测改为 1024B 块、分配后覆盖 payload，在 8 消费者下稳定复现。
  根因是 tagged Treiber head 只阻止陈旧 CAS，不能保护 CAS 前对 head 对象
  `next_offset` 的解引用：另一消费者可先 pop、复用并覆盖该块。现为每个
  shard/size-class 的 local/remote 链各增加一个 HWCC pop lock，只序列化必须
  保护的消费者临界区，push 仍无锁；共享池 ABI 升至 4，避免旧布局误 attach。
  修复后的覆盖式 allocator 压测已连续通过 20 轮；正式验收将从全体单测重新
  开始，不沿用此次失败前的结果。
- 针对性验证已通过：策略/目录/root/retire/Scan 单测各多轮，关键 host
  多进程 E2E smoke，验收证据、VM harness、YCSB prepare-only CTest，以及从
  仓库外执行的 metadata 回归。此处不冒充最终门槛；全体单测 10 轮、真实
  e2e08/09 各 10 轮及正式 YCSB load/A–E 各 10 轮须在延迟插入最终审计和
  文档提交冻结后重新从第 1 项连续执行。

## 2026-07-26 — 最终审计修复：策略、可见性与延迟模型

- `a1e5fd1` 恢复原 SIDLE threshold adjuster 的三态和同步 shortage 控制：
  tight 时阻止 promotion，收紧 allocation/demotion depth，最多三次唤醒现有
  trigger 并等待现有 demotion executor drain 后重查预算，退出前恢复阈值和
  promotion；仍只有原五个后台角色。握手用同一 mutex/CV 排除普通 trigger/
  demotion 与阈值改写，`Stop` 持锁发布 running=false，修复曾稳定复现为 cooler
  一小时等待的 lost wakeup。修复后并行压力共 2400/2400 次通过，长 interval
  单测要求 Stop<5s；08/09 foreground affinity 失败也改为 hard fail。
- `75fe587` 使完整本地副本 GET 从 HWCC RootControl/NodeRef 开始，以
  allocation-state、generation、version 前后双检遍历 immutable internal/leaf
  snapshot；命中时不 resolve canonical SWCC，专项严格断言
  `swcc_raw_line_accesses==0`，miss/空 child/版本变化仍回退原 Masstree 路径。
  跨 layer 同样先尝试本地副本。leaf replica 构建只读取一次 value/ksuf，并用
  定长栈数组保存描述符，避免第二次 SWCC 读取和额外 heap allocation。
- 同一提交修正 Scan 查询目录时重复整叶计费、external ksuf 按 allocation
  capacity 而非实际 suffix 计费；物理 invalidate 仍按 SWCC 可见性协议保守覆盖
  已发布 COW bag。canonical 节点保持原裸字段访问控制流，以 NodeControl 记录的
  实际 allocation envelope 作为有界计费范围，不用固定 512B，也不为逐字段精度
  重写上游 Masstree。
- `cfa347e` 令 latency injection 只接受精确 RelWithDebInfo，Release 不再因
  `NDEBUG` 绕过合同；仍要求 verbose/extra_check=false，并注册严格 VM E2E
  harness 与 YCSB metadata 测试。`178d945` 删除普通 DRAM 本地 EpochTable 的
  伪 HWCC 计费，共享池内 SharedEpochTable 计费保持不变。
- 聚焦合并验证：config/epoch/node-control/latency/replica-directory/Masstree
  7/7，通过 trace smoke/min-duration/replica-activation/2VM 与 e2e08/09
  6/6；副本/节点改动后再跑 3/3 通过。完整插入位置与保守边界记录在
  `延迟插入审计报告.md`。

## 2026-07-26 — 最终审计修复：正式 YCSB 与 VM E2E 证据

- `2db8831` 删除空壳 `--shared-reserve-mb`，继续继承 base config 或接受显式
  2 的幂 shared size，并在 run metadata 记录最终实际值。trace manifest
  增加逐 worker 和 trace-set SHA256。
- 上游 YCSB-cpp 的 operation chooser 由 `random_device` 播种，不能靠重新生成
  稳定得到冻结拆分。正式 4 VM×4 worker、100k/100k 入口因此做显式、仅此规模
  生效的 deterministic normalization：load=100000 PUT；
  A=100000 GET+50199 PUT；B=95019 GET+4981 PUT；C=100000 GET；
  D=95072 GET+4928 PUT；E=94920 SCAN+5080 PUT。归一化前后计数、修改数和
  trace-set SHA 写入 `trace_normalization.json`，正式公平比较必须把同一组
  worker 文件交给 cxlkv 与 D-SIDLE。
- 新鲜正式生成首次覆盖“A 的随机 PUT 高于目标”时，发现写盘前复核未跳过待删除
  行；`a7c0597` 修正并分别覆盖高/低于目标边界，`b3b1bcc` 又改为完整内存验证
  后才发布文件，避免失败留下部分归一化结果。随后新鲜 600k 生成、严格计数和
  manifest/provenance SHA 校验通过。
- `9c6156f` 对真实 e2e08/09 每轮执行与 cxlkv 同口径的 host+4 guest page-cache
  clear 和 4×64MiB CPU sweep；保存不可变 host/guest config、git/config SHA、
  每轮 exit evidence，严格核对每节点 TIME/ops/workers/verify marker，并按每轮
  最慢节点汇总吞吐。helper/summarizer 的成功和损坏日志拒绝测试通过。

## 2026-07-26 — 最终审计修复：配置空壳与热路径一致性

- `2a273b1` 撤销单侧 TLS heartbeat 聚合，恢复与当前 cxlkv runner 相同的逐 op
  shared atomic，避免把不对称加速包装成优化；`ef62fc8` 令 column update 在
  unlock 前 `mark_insert()`，确保 HWCC version 推进并使远端目录拒绝旧副本，
  新增远端 column-update 回归。
- `38a445c` 删除没有独立资产和断言的 replica/scan_stress/reclaim/concurrency
  伪 suite 标签；`25aeddd` 删除临时 nodeversion stderr 锁诊断但保留 invariant；
  `37ce49e` 更正文档中的真实五后台角色、heartbeat、epoch slots、A–E 和
  100k 验收合同。

## 2026-07-26 — 验收接口与历史证据更正

- 撤销 `replica`、`scan_stress`、`reclaim`、`concurrency` 四个所谓补充
  suite：旧编排除 `concurrency` 附加最小时长外只改变 phase 名称，没有各自
  的 trace、workflow 或断言，因此旧日志的“3/3 套件通过”不能作为四项独立
  集成测试证据。`run_dsidle_e2e_rounds.sh --suite` 现在只接受真实实现的
  `08|09|ycsb`；通用 `--phase` 仍可用于开发回放，但不得称为套件。
- 删除只验证伪 `concurrency` 标签的 CTest。并发、副本、Scan 与回收分别由
  真实 4 VM×4 worker YCSB A–E、replica activation/目录/Masstree、YCSB-E
  materialized Scan、epoch/allocator/node-control/Masstree 单测覆盖。
- PLAN 的当前硬门槛改为：全体单测 10 轮、真实 e2e08/09 各 10 轮，以及
  100k、4 VM×4 foreground worker、固定 32B key/value 的独立 load 与
  A/B/C/D/E 各 10 轮；任一阶段发现 bug，修复后从全体验收起点重跑。

## 2026-07-25 — kill/clear 与 cxlkv 对齐 + init 硬门收紧

- `dsidle_kill_vms.sh` 按 `../cxlkv/xz_scripts/init_scripts_env_99_clear_vm_data.fish`
  重写：pidfile/`pgrep -f` 双轮 `kill -9`、拒绝危险路径、清空 storage 内容、
  删除 `ivshmem_shared_mem`、卸载共享 tmpfs；保留 `--dry-run`/`--execute`。
- init 收紧：`modprobe tun tap`、guestmount umount 失败即退出、prepare_for_network
  与 cxlkv 同款 replace+del、空 NUMA 拓扑硬失败；`mkosi.extra` `.bash_history` 与 cxlkv 对齐。
- CMake `dsidle_vm_scripts_dry_run` 纳入 kill dry-run。

## 2026-07-25 — VM init 与 cxlkv 完全对齐

- 以 `../cxlkv`（`984ad91a614ae65b57d0fe53ccc174bb6e962bcd`）的
  `init_scripts_env_3_init_vm.fish` + `rust_utils/init_vm` 为基准，重写
  `scripts/dsidle_init_vm.py` / `dsidle_init_vms.sh`：宿主性能调优、NUMA 预检、
  共享内存 tmpfs `mpol=bind`、bridge/tap、双网卡 QEMU、guestmount 注入网络/SSH、
  guest 内 rsync/`make`/`modprobe ivshmem_driver`（`ivpci`）、BAR2/`/dev/ivpci0`
  校验、QEMU `taskset`；额外保留 D-SIDLE `dsidle_shared_pool --init-pool`。
- 对齐 cxlkv 硬门：MemAvailable（含可回收旧 QEMU RSS）、QEMU 前 SSH hostfwd 端口释放、
  挂载后 tmpfs 再校验、guestmount 后 settle。
- 驱动装载从 e2e/YCSB 编排移入 init；`run_dsidle_vm_{e2e,ycsb}_rounds.sh` 仅校验
  `ivpci`；删除 `dsidle_build_vm_artifacts.sh`；YCSB/README 改为宿主 `build/` RelWithDebInfo。
- 验证：`python3 -m py_compile`、`bash -n`、`./dsidle_init_vms.sh --dry-run` 输出
  tmpfs `mpol=bind`、guest `modprobe ivshmem_driver`、per-VM taskset。

## 2026-07-25 — VM 镜像制作与 cxlkv 完全对齐

- 从 `../cxlkv`（`984ad91a614ae65b57d0fe53ccc174bb6e962bcd`）原样拷贝
  `image/make_vm_img.sh`、`image/mkosi.default`、`image/mkosi_postinst.sh`、
  `image/ubuntu_rootfs.sh`；删除本仓旧的最小 `mkosi.postinst` / `ExtraTrees=rootfs`
  路径。
- `dsidle_make_vm_img.sh` 按 cxlkv `init_scripts_env_2_make_vm_img.fish` 语义改写：
  已存在非空 `image/root.img` 则跳过，`--force` 重建，内部调用本树
  `image/make_vm_img.sh`；保留 `--dry-run` / `--config` API。镜像 SSH 改为与
  cxlkv 相同，使用宿主机 `~/.ssh`。
- 验证：`diff` 四份拷贝文件与 cxlkv 源零差异；`bash -n`；
  `./dsidle_make_vm_img.sh --dry-run` 输出 `DRY_RUN bash .../image/make_vm_img.sh`。
  实际 `--force` 重建未在本轮执行（约 38GiB、需 sudo/网络）；重建后内核应为
  `5.15.0-173-generic`。

## 2026-07-24 — M8 正式多轮验收

- 真实四 VM、32 GiB ivshmem、宿主一次 Jammy RelWithDebInfo 构建并 rsync 分发：e2e08（100k 8B/8B）连续 10/10 轮通过，e2e09（100k 32B/1000B）连续 10/10 轮通过；日志分别在 `/tmp/dsidle-m8-e2e08`、`/tmp/dsidle-m8-e2e09`。
- YCSB-cpp 生成 100k record/100k operation、zipfian、4 VM×4 worker 的并发 load/workloada；标准 `build-jammy` 产物连续 10/10 轮通过，汇总为 `/tmp/dsidle-m8-ycsb-100k/ycsb_summary.json`。其中 workloada 的更新操作按 trace 展开为 GET+PUT，故 `ops_sum` 可大于输入 operationcount。
- 历史记录更正：当时名为 replica、scan_stress、reclaim、concurrency 的四组
  运行仅复用了同一 trace runner 并改变 phase 标签，不能视为四个独立套件；
  `/tmp/dsidle-m8-*` 只保留作旧通用回放日志。
- 首次 100k YCSB 失败使用了原子发布修复前的外部 Jammy 产物；重新执行一次外部构建后，标准产物的 10 轮复验通过。`build-jammy/` 是可再生构建目录，已忽略，不在版本库中提交。
- §7.4 差异审计：相对 `1f1fa8ae9459935963a67a9beef6f30683c3c47b` 的 89 个已提交文件逐类归属为构建/配置、共享池与偏移控制、Masstree 就地改造与副本、VM/YCSB 编排、测试/trace、镜像与交付文档；没有未归属的新增运行时文件、临时调试开关或对同级仓库的运行时引用。基线 ART/benchmark/upstream Masstree 中的既有 TODO 未触碰。
- 清扫后复验：Jammy 外部构建成功；RelWithDebInfo `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure` 为 19/19；`bash -n dsidle_*.sh scripts/*.sh`、`git diff --check` 通过；四 VM e2e08/e2e09 和 100k 并发 YCSB 各额外 1 轮通过。新增 `README_DSIDLE.md` 记录独立建镜像、一次外部构建/rsync、并发 YCSB 和精确停止流程。
- 64GiB 性能预备数据：以 4 VM×8 vCPU、4 worker/VM、5M record/operation、32B key/value、`--no-latency` 执行 A–D 五轮；汇总 `/mnt/xz_vm_storage/dsidle-m8-perf-abcd/ycsb_summary.json` 为 load 213929、A 309344、B 570174、C 684637、D 632292 ops/s。A–E 单轮冒烟（同合同）通过，E 为 91798 ops/s，汇总在 `/mnt/xz_vm_storage/dsidle-m8-perf-abcde/ycsb_summary.json`。这些早期运行未记录独立预热轮，不能作为 §6.4 正式矩阵结果。
- 后续矩阵编排已增加默认 1 个不汇总预热轮（`--warmup-rounds`），并把该数字写入 `run_meta.json` 与复现命令；正式结果仅汇总其后的 `--rounds` 轮。针对该元数据的 CTest 通过。
- 历史记录更正：提交 `533e1a9` 实现的 runner
  `--min-duration-sec` 循环重放本身保留并有专项 CTest；把它包装成
  `concurrency` suite 的编排和 `dsidle_e2e_concurrency_plan_test` 已撤销，
  因为持续时长参数不能替代独立并发 workload 与断言。
- 运行时发现 YCSB runner 仅绑定了 `ReplicaDirectory` 却未启动五个 SIDLE 副本 worker，导致长跑统计 `replica_bytes=0`。现把预算、Trigger/Promotion/Demotion/Cooler/Adjuster 接入 runner 生命周期；后台线程显式绑定同一共享池/本 VM shard，并为实际进入 epoch 的 Trigger/Promotion/Demotion/Cooler 预留四个独立 epoch slot（Adjuster 不进入 epoch），避免与前台并发写线程复用 slot。此前使用旧 runner 的 5M 输出只保留作并发回放诊断，不再作为副本性能数据。
- 真实 VM 验证（提交 `375aee5`）：64GiB、2 VM×1 前台 worker、64MiB 副本预算的 10k 并发 load→workloadb 探针通过；两个 workloadb 节点分别报告 `replica_bytes=219722`、`223146`，产物为 `/mnt/xz_vm_storage/dsidle-replica-probe-20260724`。随后已按预热 1 + 正式 5 的合同启动新的 5M 2 VM 格。
- 该接入后的 RelWithDebInfo 全量 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure` 为 22/22 通过；覆盖两 VM 并发 runner、最小时长、副本激活、rounds/YCSB/08/09 编排、配置、共享池、epoch、副本目录与单进程 Masstree。
- 新增 `dsidle_e2e_trace_runner_replica_activation`：先写入一个真实 value，再以只读 trace 持续 1 秒回放，断言 `replica_bytes > 8`；实测为 62 bytes，覆盖 value-copy 副本而非空叶头，防止副本 worker 再次只定义不启动。
- 修复后 5M 正式格的预热 workloadb 已完成：2 VM 各 2,500,000 次并发只读回放，耗时 47.196s / 47.493s，`replica_bytes=43824/43352`；日志在 `/mnt/xz_vm_storage/dsidle-formal-replica-2vm-1t-64-20260724`。
- YCSB 汇总补充每 case 的逐轮吞吐 `ops_per_sec_p50` / `ops_per_sec_p90`，同时写入 JSON、CSV 和 Markdown 报告；summarizer CTest 与 Python 语法检查通过。
- 运行时修复：重启后的 `/dev/ivpci0` 可能是 `uio_pci_generic` 的符号链接，不能把“字符设备存在”当作自定义 BAR2 driver 已绑定；YCSB 编排现检查 PCI driver 名称为 `ivpci`，否则显式 unbind、加载模块、设置 `driver_override` 并 probe。5M 多 phase trace 超出 2 GiB guest root，编排改为每 phase 清理并 rsync 当前不可变 trace，保持宿主构建/生成、VM 仅回放。
- 性能矩阵编排去除固定四节点：`--vm-count 1|2|4` 同步派生 `vm.count`、e2e worker 数、YCSB trace 数、元数据和回放 SSH 节点集；VM 初始化接受相同节点范围。1 VM×2 worker prepare-only 实测生成两份 load/A trace，配置和元数据均为 1/2。
- 真实 VM 复验：64 GiB 合同下，1 VM×2 worker 与 2 VM×2 worker 均通过 10k load/workloada 并发回放，结果分别保留在 `/mnt/xz_vm_storage/dsidle-matrix-1vm` 与 `/mnt/xz_vm_storage/dsidle-matrix-2vm`。两次均通过新配置驱动的精确 VM 启动、BAR2 绑定和逐 phase rsync。
- 新增 `--replica-budget-mb`：派生配置、`run_meta.json` 和可复现命令均记录预算。prepare-only 用 2 VM×1 worker、64 MiB 验证配置值、元数据和 worker 数一致。
- 64 GiB 正式并发格完成：2 VM×1 前台 worker/VM、每 VM 两个副本策略线程、64 MiB 副本预算，5M record + 5M operation、固定 32B key/value、`--no-latency`，先预热 1 轮再执行 A–D 各 5 轮。正式汇总位于
  `/mnt/xz_vm_storage/dsidle-formal-replica-2vm-1t-64-20260724`：load/A/B/C/D 吞吐分别为
  49,567/72,511/97,397/113,748/98,997 ops/s；p50/p90 已写入 JSON、CSV 与报告。结构化核验确认每 case 正好 5 个正式样本；每阶段 10 个节点级 `replica_bytes` 样本均大于空叶头（load 28,160–30,744，A 37,936–42,016，B 37,528–47,368，C 42,632–51,152，D 40,688–50,472）。日志未发现 error/fatal/assert/exception；本地全量 CTest 为 22/22。
- 64 GiB 主并发写格完成：4 VM×4 前台 worker/VM（16 个前台 writer）、每 VM 两个副本策略线程、64 MiB 副本预算，5M record + 5M operation、固定 32B key/value、`--no-latency`，预热 1 轮并完成 A–D 各 5 轮。正式产物为
  `/mnt/xz_vm_storage/dsidle-formal-replica-4vm-4t-64-20260724`；load/A/B/C/D 吞吐为
  156,732/220,608/432,536/526,880/446,774 ops/s，p50/p90 分别为 158,377/159,224、224,697/229,003、446,506/447,450、542,203/563,953、443,351/480,379。结构化核验确认每 case 有 5 个正式样本，且每阶段 20 个节点级副本样本均大于空叶头（load 23,824–47,936，A 41,392–47,120，B 45,680–61,016，C 60,064–66,824，D 49,936–64,680）；100 个正式节点阶段日志无 error/fatal/assert/exception。期间一次真实并发 warmup 暴露 `Masstree leaf replica size mismatch`：副本复制原先在未锁叶上先计算大小再拷贝，现使用非阻塞叶锁在锁内构建/发布、竞争时跳过 promotion（提交 `f7649a9`）。
- 以上主并发格随后以最终跨 VM 安全 runner（`82e0dc2`）重新执行，最终可引用产物改为
  `/mnt/xz_vm_storage/dsidle-formal-replica-4vm-4t-final-20260724`。该修复不再沿用跨进程无效的 raw `parent()` 指针，并在尚未 pin root 时避免对空 `NodeRef` 调用目录回收；gdb 已将原短跑异常定位到 `root_replica_pin` 析构的 `ReplicaDirectory::Invalidate`。最终 load/A/B/C/D 吞吐为 150,854/225,617/410,131/521,966/466,476 ops/s，p50/p90 为 151,108/151,671、226,594/228,074、406,693/431,740、519,203/537,398、466,488/474,993。最终五轮结构化核验：每 case 5 个样本，每阶段 20 个 `replica_bytes > 8`（load 23,736–44,200，A 29,808–34,912，B 31,504–46,984，C 38,032–56,656，D 34,696–56,416），100 个正式节点阶段无 error/fatal/assert/exception；宿主全量 CTest 22/22。
- 64 GiB 2 VM×4 writer 格完成：每 VM 四个前台 writer、两个副本策略线程、64 MiB 副本预算，5M record + 5M operation、32B key/value、`--no-latency`、预热 1 + 正式 5 轮 A–D；产物
  `/mnt/xz_vm_storage/dsidle-formal-replica-2vm-4t-final-20260724`。load/A/B/C/D 吞吐为 103,695/135,458/218,911/244,814/222,377 ops/s，p50/p90 为 104,184/106,981、134,107/142,394、214,776/241,813、243,428/251,856、222,507/225,898。核验每 case 为 5 个正式样本，阶段各有 10 个 `replica_bytes > 8`（load 23,376–25,574，A 33,872–41,832，B 35,720–54,240，C 43,584–57,352，D 37,480–54,608），日志无 error/fatal/assert/exception。
- 64 GiB 1 VM×4 writer 格完成：四个前台 writer、两个副本策略线程、64 MiB 副本预算，5M record + 5M operation、32B key/value、`--no-latency`、预热 1 + 正式 5 轮 A–D；产物
  `/mnt/xz_vm_storage/dsidle-formal-replica-1vm-4t-final-20260724`。load/A/B/C/D 吞吐为 61,343/81,241/115,326/131,881/117,523 ops/s，p50/p90 为 61,688/62,315、80,625/84,861、113,258/122,475、132,219/135,190、121,572/122,607。核验每 case 为 5 个正式样本，阶段各有 5 个 `replica_bytes > 8`（load 23,040–24,296，A 37,848–43,432，B 32,600–52,696，C 42,168–54,264，D 39,104–54,288），日志无 error/fatal/assert/exception。
- 按“更稀疏矩阵”指示，不再展开全部 1/2/4 VM×1/2/4 worker 的五轮组合：正式主表保留 1/2/4 VM×4 writer 三行；线程/预算维度以真实 VM 单轮 probe 覆盖。1 VM×1 writer、1 MiB 预算、10k load→A 通过（22,206/33,487 ops/s，`replica_bytes=1364/27982`，`/mnt/xz_vm_storage/dsidle-probe-1vm-1t-budget1-20260724`）；1 VM×2 writer、1536 MiB 预算、10k load→A 通过（44,779/66,254 ops/s，`replica_bytes=816/28144`，`/mnt/xz_vm_storage/dsidle-probe-1vm-2t-budget1536-20260724`）。两份探针日志无 error/fatal/assert/exception，且只作为正确性/覆盖证据，不并入正式性能分位数。
- 严格复核 cxlkv e2e08/09 后，废弃旧的“node0 串行 fill + delete/scan”作为对齐证据：其数据规模虽为 100k，但不具备 cxlkv 的 4 VM×4 foreground worker 并发写入，也不符合其 phase workflow。suite runner 现逐字节采用 cxlkv 的 base36 key/value（08）和 generation=0/1 的 1000B value（09）生成规则；全局 100k key 按 VM、再按 4 foreground worker 分片，读索引采用同一 SplitMix64 node/worker/sequence 派生式。08 工作流严格为 fill→随机读校验；09 严格为 fill→update→读回 generation=1 value。每个 foreground 线程都绑定自己的 SWCC allocator 与 ReplicaDirectory。
- 严格实机复验：沿用 cxlkv 同构的正式配置（4 VM×8 vCPU、每 VM 4 foreground worker、VM 在 NUMA0、32 GiB ivshmem 在 NUMA1、HWCC/SWCC=1024/31744 MiB、宿主 Jammy RelWithDebInfo 一次构建后 rsync 分发）。e2e08 100k 8B/8B 在 `/mnt/xz_vm_storage/dsidle-e2e08-4vm-strict-10r-20260724` 连续 10/10 轮通过（10×2×4=80 份节点验证日志）；e2e09 100k 32B/1000B 在 `/mnt/xz_vm_storage/dsidle-e2e09-4vm-strict-10r-20260724` 连续 10/10 轮通过（10×3×4=120 份节点验证日志）。两份日志均无 error/fatal/assert/exception；e2e09 覆盖真实 16 并发 fill、16 并发 update 和跨 VM 更新值读回。
- VM E2E 部署修复：`/dev/ivpci0` 可能由 `uio_pci_generic` 创建但仅映射 4KiB BAR0；E2E 脚本现与已验证的 YCSB 路径一致，检查驱动必须为 `ivpci`，否则解绑、加载本仓构建模块、设置 `driver_override` 并重新 probe。外部构建脚本同时产出 host 侧池初始化工具，避免任何 guest 内编译。

## 2026-07-24 — M7 VM 真实数据面冒烟

- VM 镜像启动和依赖向 cxlkv 对齐：BIOS `root.img`、Jammy Clang/rsync/gdb；镜像仅构建一次。
- 采用宿主 `systemd-nspawn --image image/root.img` 进行一次 Jammy ABI 构建，再通过 rsync 向四台 VM 分发二进制和来宾 `/dev/ivpci0` 配置；未在每台 VM 编译 D-SIDLE 可执行文件。
- 修复 `SharedPool::Attach`/`AttachAt`：字符设备 `fstat().st_size == 0` 时使用配置声明的 BAR 大小。
- 真实诊断表明 `uio_pci_generic` 只提供 4KiB BAR0；改用 cxlkv `ivshmem_driver` 后确认 BAR2 为 32GiB。四 VM e2e08、e2e09 各 1 轮 fill/read/delete/verify/scan 全通过。
- YCSB 10k record/10k operation、4 VM×4 worker 的 load/workloada 真机冒烟通过：load 与 workloada 均为四 VM、16 worker 并发回放；修复 `nodeversion::unlock()` 释放后读取 lock bit 的竞态断言，runner、模块和 trace 均由宿主一次构建/生成后 rsync 分发。
- 后续四 VM 并发 load 曾在 node 2 触发“未发布 canonical node”诊断；将跨进程共享的 `NodeControl::allocation_state` 改为 acquire/release 原子发布，并让 replica worker 以 acquire 读取。完整 `dsidle_run_ycsb_experiment.sh` 入口在 10k/10k、4 VM×4 worker 下再次通过 load 与 workloada；宿主全量 CTest 19/19 通过。
- VM e2e runner 不再在每次运行时无条件解绑已工作的 `ivpci`；这会让 udev 字符设备在重绑窗口内消失。改为仅在 `/dev/ivpci0` 缺失时安装并绑定模块；08 真机单轮回归通过。

## 2026-07-24 — M7 检查点：按 cxlkv 语义的真实四 VM 启动

- VM 镜像与 QEMU 启动路径按 cxlkv 已验证实现收敛：mkosi 使用 BIOS boot
  protocol、GRUB 和非 UKI 镜像；启动使用 cxlkv 同构的 q35/KVM、virtio-blk、
  user SSH hostfwd 和 ivshmem-plain 参数，不依赖兄弟仓的脚本或镜像产物。
- `shared_memory.path` 恢复为 cxlkv 的目录语义，D-SIDLE 将实际 backing 统一
  解析为 `<path>/ivshmem_shared_mem`；共享池、启动和检查脚本均使用相同规则。
- 镜像启用 cxlkv 同构的 `systemd-networkd`/DHCP 和 root password，四台 guest
  均获得 user-network DHCP 地址；默认配置同时包含 cxlkv 公钥与本机验收公钥。
- 实机验证：4×8 vCPU/2GiB VM 共享 32GiB backing；端口 10022–10025 均可 SSH，
  `sshd` active，且每台 guest 的 `/dev/ivpci0 -> uio0`。`dsidle_check_vms.sh`
  输出 `DSIDLE_VM_CHECK_OK`。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  'dsidle_config_tests|dsidle_vm_scripts_dry_run'`（2/2 通过）；四个 VM 脚本
  `shellcheck`（通过）。

## 2026-07-24 — M7 检查点：08/09 的 100k 跨进程数据面套件

- 新增 `dsidle_e2e_suite_runner` 与 `--suite 08|09` 编排：08 使用固定 8B key/8B
  value，09 使用固定 32B key/1000B value；两者均填充 100,000 条记录。
- 由于 Masstree writer version lock 不支持多个进程并发构建同一棵树，填充阶段由
  node 0 串行执行；所有四个进程仍通过共享 phase barrier 参与，随后跨节点分片读回
  每条记录、node 1 删除每十条、全节点验证 delete miss，并由 node 3 扫描验证余下
  90,000 条。每阶段输出时间与明确的 verify 成功标记。
- 新增 4 VM 08/09 配置及 CTest smoke；其中 09 为了容纳真实 100k×1KB 值使用
  8 GiB 稀疏共享映射。YCSB trace CTest 的断言改为跨所有 worker 文件查找 GET/PUT，
  避免随机工作分片使 worker0 偶尔只含一种操作而产生伪失败。
- 验证：RelWithDebInfo `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`
  （19/19 通过）；六个脚本 `shellcheck`（通过）。

## 2026-07-24 — M7 YCSB 两相位 rounds suite

- `run_dsidle_e2e_rounds.sh` 增加 `--suite` 接口和 `ycsb` 两相位编排：每轮只
  初始化一次池，load phase 仅首节点 bootstrap，workloada phase 全节点
  attach 已发布根；load/run 配置必须拥有相同共享池拓扑。历史补充标签后来
  被确认没有独立负载语义并于 2026-07-26 删除；真实 suite 默认 10 轮。
- 验证：原 2/4 节点 rounds CTest 通过；两节点 YCSB suite smoke 验证 load 与 workloada 两相位统计行通过。

## 2026-07-24 — M6/M7 综合回归

- 验证：RelWithDebInfo 全量构建；`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`，16/16 通过，覆盖 trace/YCSB、2/4 节点 rounds、VM dry-run、配置与延迟模拟。
- 未将此回归视为真实 QEMU 或 10 轮 e2e 验收；镜像构建仍需在持久终端中完成。

## 2026-07-24 — M7 Bash 静态检查

- 安装 shellcheck 0.8.0-2，并检查 `dsidle_make_vm_img.sh`、`dsidle_init_vms.sh`、`dsidle_kill_vms.sh`、`dsidle_check_vms.sh`、`dsidle_run_ycsb_experiment.sh` 和 `scripts/run_dsidle_e2e_rounds.sh`。
- 验证：shellcheck 零诊断。

## 2026-07-24 — M7 独立 VM 镜像配置

- 新增本仓 `image/mkosi.default` 与 post-install 脚本，定义可引导 Ubuntu jammy raw `root.img`、SSH 和 guest 构建/NUMA 依赖；镜像脚本可从配置注入 `vm.local_ssh_pub_key`，并支持 `--force` 交由 mkosi 覆盖输出。
- 验证：`bash -n`、镜像脚本 dry-run，以及 `mkosi -C image summary`；摘要确认 Ubuntu、`gpt_ext4`、`root.img`、Bootable、包集和 post-install 脚本。Debian bookworm 首次实际构建因宿主 archive keyring 无法验证 Release 签名而在安装前失败，未保留镜像；Ubuntu jammy 已通过签名和包下载阶段，但当前受控终端会在长构建会话结束时清理子进程，未形成 `root.img`，须在持久终端重跑。

## 2026-07-24 — M7 YCSB 多轮汇总字段

- 汇总器补齐 `avg_ops_sum`、`avg_duration_sec` 和基于它们的 `ops_per_sec`，CSV/Markdown 与 JSON 同步输出，避免用单轮字段隐含多轮平均。
- 验证：Python 语法检查与 YCSB 汇总 CTest 通过。

## 2026-07-24 — M6 后台副本延迟作用域

- replica worker 的 trigger、promotion/demotion executor 与 cooler 批次均增加 `ScopeGuard(kMerge)`；Trigger/Cool 的析构在 RCU stop 后发生。
- 验证：`merge_enabled=false` 时后台 scope 不产生 delayed_ns；延迟和单进程 Masstree CTest（2/2 通过）。

## 2026-07-24 — M7 配置全字段合同

- 配置解析增加所有对象层级的必填/未知键 hard fail，覆盖根、共享池、VM、host CPU、e2e、D-SIDLE 与 latency 全字段；新增未知根键拒绝测试。
- 验证：config、runner 冒烟和四节点 rounds CTest（3/3 通过）。

## 2026-07-24 — M6 TSC 注入精度与降级保护

- 启用延迟注入时要求 TSC 校准成功；移除 `sleep_for` 降级，避免在非 TSC 路径将睡眠误报为性能注入。增加测试专用 TSC 自旋入口。
- 验证：100 次 1000ns 忙等取最小样本，误差在 ±20%；仅 SWCC 与仅 HWCC profile 各自只有对应池的 delayed_ns 非零；RelWithDebInfo 延迟 CTest 通过。

## 2026-07-24 — M7 四节点 runner 冒烟

- 新增 4 VM 语义的 4 进程共享池 fixture 与 load/GET/PUT/DELETE/SCAN worker trace；统一 rounds 脚本通过单轮 `--smoke` 回放并验证 node 3 统计行。fixture 将 SWCC 设为 224MiB，以满足四 shard allocator 的容量下限。
- 这是 host 多进程冒烟，非 QEMU 4 VM 验收。

## 2026-07-24 — M7 YCSB phase 配置绑定

- YCSB 准备脚本为 load 与每个所选 workload 物化独立 `experiment_config_ycsb_<phase>.jsonc`，将 `dsidle.trace_dir` 精确绑定到该 phase 的 worker 目录，并在 `run_meta.json` 记录映射。
- 验证：YCSB trace 生成 CTest 断言 A phase 配置的 trace 路径及 GET/PUT trace 展开，通过。

## 2026-07-24 — M7 VM 启动与只读检查路径

- `dsidle_init_vms.sh --execute` 现实现共享 backing 预故障/池初始化、每 VM 独立 raw 磁盘、NUMA 绑定 QEMU、ivshmem、pidfile 与 taskset；严格预检布局、NUMA、CPU 集合和可用内存。所有破坏性步骤仍要求显式 `--execute`。
- `dsidle_check_vms.sh` 实际模式验证 pid/QEMU cmdline、ivshmem backing、CPU 亲和性、SSH guest 设备和 `numa_maps`；dry-run 只输出检查计划。
- 验证：四脚本 `bash -n`；默认配置 init/check/make 的 dry-run 通过；新增 CTest 覆盖 VM 脚本 dry-run。

## 2026-07-24 — M7 统一 e2e rounds 编排

- 新增 `scripts/run_dsidle_e2e_rounds.sh`：默认最低 10 轮；仅显式 `--smoke` 可运行开发单轮。实际路径要求 `--execute`，每轮重置精确 backing、初始化池、并发启动所有节点并写入节点日志、配置 SHA、git SHA 与退出状态；dry-run 只打印命令。
- 验证：两节点共享池、单轮 smoke CTest 通过，两个节点均输出 `E2E_TRACE_TIME_US`。

## 2026-07-24 — M7 YCSB trace 生成接入

- `--prepare-only` 现调用本仓固定子模块生成 load 和所选 A–E 的 CXLKV 格式 trace，验证 4×1 worker 的 load/A/E 文件集合和 A 的 `GET`+`PUT` 展开；新增 CTest 覆盖该路径。初始化时需 `git submodule update --init --recursive` 以取得 YCSB-cpp 的嵌套直方图依赖。
- VM 回放仍未实现，普通执行在 trace 生成后明确失败。

## 2026-07-24 — M7 YCSB-cpp 依赖固定

- 按计划 SHA 初始化本仓 `third_party/YCSB-cpp` 子模块，来源为公开 `cxlkv_trace_ycsb_5m_20260621` 分支；生成器将由本仓脚本调用，未引用兄弟仓运行时资源。

## 2026-07-24 — M7 YCSB 参数矩阵复核

- 复核 `f`、`workloada`、大写、空项、重复项和 `load` 等非法 workload：均以失败退出，且不创建请求的输出目录。
- 验证：RelWithDebInfo `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`，12/12 通过。当前环境未安装 `shellcheck`，故该外部静态检查尚未执行。

## 2026-07-24 — M7 YCSB 准备指南

- 新增 `YCSB指南.md`，仅记录已执行的帮助、准备和汇总命令，并明确 YCSB-cpp 子模块/实际 VM 回放尚未就绪，避免将准备产物误述为实验结果。

## 2026-07-24 — M7 YCSB 编排准备入口

- 新增 `dsidle_run_ycsb_experiment.sh` 的严格参数和 workload 矩阵校验，以及 `--prepare-only` 可复现元数据输出；实际 trace 生成和 VM replay 尚未启用。
- 验证：脚本 `bash -n`；YCSB 汇总器和准备模式 CTest（2/2 通过）。

## 2026-07-24 — M7 YCSB 配置物化

- 准备入口补齐计划中的超时、共享 NUMA/大小/预留、缓存冲刷及跳过阶段选项；输出派生的四 VM 配置、配置 SHA256、git SHA 和完整复现命令。派生配置固定 1024MiB HWCC、其余 SWCC，并强制关闭模拟 cache-hit 模型。
- 验证：`--prepare-only` 以显式 NUMA、4096MiB、`--no-latency` 和全部跳过开关运行；Python 断言配置布局、摘要和延迟字段；准备模式 CTest 通过。

## 2026-07-24 — M6 延迟模拟器缓存语义

- 补全 CXLKV `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` 对应的 4ms 一次性 TSC 校准、作用域末尾批量 busy-spin、固定命中率和每线程组相联 LRU。
- 保留 Sidle 的 SWCC/HWCC 插桩封装，并在 `搬运清单.md` 记录来源、版本和差异。
- 验证：RelWithDebInfo 全量构建；`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`，8/8 通过。

## 2026-07-24 — M6 SWCC 分配与值路径插桩

- 在 shard allocator 的自由链读写、零初始化，以及 `leafvalue` 的值读取/发布处使用统一的 SWCC wrapper 记账。
- 验证：RelWithDebInfo 构建；allocator 与单进程 Masstree CTest（2/2 通过）。

## 2026-07-24 — M6 缓存模型验证

- 延迟模拟器测试增加固定命中率和每线程组相联 LRU 用例，验证 raw/hit/miss/delayed 的恒等关系与命中附加延迟。
- 验证：`dsidle_latency_simulator_tests` 通过。

## 2026-07-24 — M6 CXLKV 注入对比档

- 新增 `configs/latency/cxlkv_e2e11_reference.jsonc`：25ns SWCC、117ns HWCC、每线程 LRU；该档仅用于注入对比，默认 `experiment_config.jsonc` 仍保持全零、关闭延迟。
- 验证：配置解析测试使用该文件通过。

## 2026-07-24 — M6 HWCC 控制面插桩

- RootControl、epoch 时钟/槽和 NodeControl 的插入、分裂标记均经 HWCC 原子薄封装记账。
- 验证：epoch、node-control 与单进程 Masstree CTest（3/3 通过）。

## 2026-07-24 — M6 作用域与统计输出接口

- 增加 `ScopeGuard`，供 runner 在一个逻辑操作完成且退出锁/RCU 后统一补齐延迟；增加 cxlkv 同字段的 `LATENCY_SIM_STATS` 输出和重置接口。
- 验证：延迟模拟器测试覆盖作用域守卫和统计行，CTest 通过。

## 2026-07-24 — M7 trace runner 初版

- 新增 `dsidle_e2e_trace_runner`：严格解析 cxlkv trace 行、定长 key/随机定长 value、PUT/GET/DELETE/SCAN 回放、每秒心跳、相位统计与延迟统计输出。
- runner 通过 `--bootstrap` 创建共享根，否则从 RootControl attach；多 VM 相位屏障和 VM 启动编排仍待后续阶段实现。
- 验证：RelWithDebInfo 目标构建成功；`--help` 参数契约可执行。

## 2026-07-24 — M7 trace runner 共享池冒烟

- 增加最小 128MiB 共享池配置与 PUT/GET/SCAN/DELETE trace；CTest 先精确创建 backing、初始化池，再以 `--bootstrap` 回放。
- 验证：`dsidle_e2e_trace_runner_smoke` 通过，并实际输出 `E2E_TRACE_TIME_US` 与 `DSIDLE_MEMORY_STATS`。

## 2026-07-24 — M7 ivshmem 相位屏障

- 在共享池诊断区增加可重用 HWCC phase barrier；bootstrap 节点先发布根，所有 VM 经过屏障后非 bootstrap 节点才从 RootControl attach。屏障在 replay 计时之前。
- 验证：跨 `fork` attach 的 epoch 测试覆盖两参与者屏障；runner smoke 通过。

## 2026-07-24 — M7 VM 脚本 dry-run/preflight

- 新增四个根目录 VM 脚本：镜像、初始化、精确 pid 停止和只读检查；所有会改变 VM/backing 的路径均要求 `--execute`，当前初始化脚本先交付配置 layout/CPU 预检和 QEMU dry-run。
- 验证：四脚本 `bash -n`，以及默认配置的所有 dry-run 通过。

## 2026-07-24 — M6 规范节点解引用记账

- `ResolveCanonicalNode` 在 canonical NodeRef 解引用时记录首个 SWCC cacheline 读取，覆盖规范节点遍历的统一入口。
- 验证：node-control 与单进程 Masstree CTest（2/2 通过）。

## 2026-07-24 — M6 延迟注入构建保护

- runner 在 Debug 构建中拒绝 `latency_inject.enabled=true`，防止将调试构建误用于延迟性能数据。
- 验证：RelWithDebInfo runner smoke 通过。

## 2026-07-24 — M7 两 VM runner 冒烟

- 新增两个独立 runner 进程共享同一池的 CTest；验证 bootstrap 根发布、phase barrier 与另一 VM 的 RootControl attach，两个节点都输出 `E2E_TRACE_TIME_US`。
- 验证：`dsidle_e2e_trace_runner_2vm_smoke` 通过。

## 2026-07-24 — M7 YCSB 汇总器

- 新增 `scripts/summarize_ycsb_experiment.py`，从 runner 的 `E2E_TRACE_TIME_US` 行生成 JSON、CSV 和 Markdown；吞吐以每轮节点最大耗时计算。
- 验证：Python 语法编译和双节点日志 fixture CTest 通过。

## 2026-07-24 — M6 延迟插入审计

- 新增 `延迟插入审计报告.md`，列出 wrapper、接入点、不计费的 process-local 数据与仍需逐字段扩展的边界。

基线：`1f1fa8ae9459935963a67a9beef6f30683c3c47b`（final release）。

本日志从 M0 开始维护；每个里程碑和架构检查点均记录提交、测试与差异。

## 2026-07-24 — M5 架构检查点：根副本跟随

- 代码检查点：`19e4e8bd71bcc4edb520cab9fac54e49dc253be7`。
- 新增每 VM `root_replica_pin`：从 RootControl 读取当前 root ref/version，按根
  类型 promote leaf 或 internode 副本；RootControl 变动时释放旧根槽并钉住新根。
  即使 root ref 未变，本地副本被写后失效也会按当前 version 重建。根 pin 不改
  规范 parent/link，也不持有跨进程虚拟地址。
- 测试在结构性并发插入前后刷新 pin，并断言其 ref 始终等于 RootControl 当前 ref。
- 验证：RelWithDebInfo replica-directory 与 Masstree 专项 CTest（2/2 通过）。

## 2026-07-24 — M5 架构检查点：内部节点副本命中

- 代码检查点：`303ca3d382ac9c2e95eb42edeeb06c7f1290be0d`。
- 新增紧凑 internode 副本（排序 ikey + child NodeRef），按 stable-copy/二次验版
  发布为 `kInternal`。`reach_leaf` 在 `{generation,version}` 匹配时从本地副本
  计算下一 child NodeRef，随后保持原 canonical 版本双检；不缓存子树，也不把
  本地地址写入规范树。
- 测试发布真实根副本、验证 child NodeRef 对拍，并执行查找断言内部副本命中计数
  递增；leaf 副本和值副本专项继续通过。
- 验证：RelWithDebInfo 的 replica-directory 与 Masstree 专项 CTest（2/2
  通过）。

## 2026-07-24 — M5 架构检查点：本 VM 写后副本失效

- 代码检查点：`34e2f8316b38680a0d6510187060ea937136d4a7`。
- `run_put`、`run_replace`、`run_remove` 在原有叶版本发布完成后，仅失效当前
  进程的 ReplicaDirectory 槽并在本地 reader 离开后释放旧缓冲；不会跨 VM 修改
  其他目录。远端写仍通过 canonical generation/version 失配惰性失效。
- 测试在实际副本命中后执行本地 Replace，断言旧 `{generation,version}` 无法再
  acquire，随后继续以 `std::map` 对拍。
- 验证：RelWithDebInfo 的 replica-directory 与 Masstree 专项 CTest（2/2
  通过）。

## 2026-07-24 — M5 架构检查点：Get 副本命中双检

- 代码检查点：`348c46c8713ca1aeef28a375c95039c359be1947`。
- `unlocked_tcursor` 在完整 epoch scope 内按 canonical `{NodeRef,generation,
  version}` acquire 本地副本，执行叶查找后再次验证 canonical leaf 未变化；只有
  验版成功才返回复制的 row。目录 `ReadHandle` 随 cursor 存活到 query 调用者已
  取/拷贝 value，失效不会在窗口内释放 `local_ptr`。layer 副本按 NodeRef 回到
  下层规范树。测试直接断言 query cursor 命中副本。
- `basic_table::get` 因其 ABI 返回裸 value 指针而无法延续读句柄，明确保留规范
  SWCC 路径，避免 UAF。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build -R
  'dsidle_(replica_directory|masstree_single_process)_tests' --output-on-failure`
  （2/2 通过）。

## 2026-07-24 — M5 架构检查点：稳定 leaf promote

- 代码检查点：`c86dd1b3c5baba3f31033ab8f5125c6dade9e569`。
- `leaf_replica::Promote` 实现 canonical leaf 的 stable-copy protocol：取得
  `{NodeRef,generation,version}` 快照，构造自包含副本，二次验版后才以
  `{generation,cached_version,bytes,kind}` 发布到本地 ReplicaDirectory；版本
  变化则丢弃副本。替换旧缓冲仅在目录读者退出后发生。
- 单测从真实 leaf promote，按相同 generation/version acquire，使用本地 row
  bytes 查值，并安全 invalidate。Get 热路径命中尚待接入。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build -R
  'dsidle_(replica_directory|masstree_single_process)_tests' --output-on-failure`
  （2/2 通过）。

## 2026-07-24 — M5 架构检查点：自包含叶副本编码

- 代码检查点：`34f955484e835493c13d1aca3ad1644de608549f`。
- 新增 `masstree_replica.hh` 的进程本地 leaf 编码：按稳定 permutation 复制 ikey、
  ksuf 字节和每个普通 value 的完整 row bytes；layer 条目只保留 layer root 的
  NodeRef，绝不复制虚拟地址或把 layer 当 value。Lookup 从该本地缓冲返回 copied
  row / layer NodeRef，不再解引用 SWCC ValueRef/ksuf。
- Masstree 测试从真实 canonical leaf 创建编码并验证 copied row 的内容。该缓冲
  尚未由后台 promote 发布到 ReplicaDirectory，也尚未接入 Get 命中。
- 验证：RelWithDebInfo 重编并运行 `dsidle_masstree_single_process_tests`（1/1
  通过）。

## 2026-07-24 — M4 完成：SMO、Scan 与安全回收

- 组成检查点：`85ce90a`（scan 对拍）、`42387e8`（Scan∥Delete）、`ae5f1b3`
  （同步双进程高冲突 split）、`b3d1a75`（共享 epoch/owner-shard limbo）、
  `8872b8e`（NodeControl 成对回收）、`77166a4`（线程退出 drain）、
  `93743a3`（叶链 SWCC 发布）、`25dc071`（reclaim 状态验证）。
- 退出条件证据：高冲突 SMO 由两个独立 attach worker 的同步 64+64 插入并逐项
  GET/完整 Scan 对拍；Scan∥Delete 在扫描叶链期间验证 epoch ACTIVE；节点体与
  控制项的 RCU 生命周期、owner-shard remote-free 和 worker limbo drain 均有
  状态断言。长共同前缀键覆盖 layer 路径。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（7/7 通过）。后续进入 M5 副本和策略。

## 2026-07-24 — M5 架构检查点：本 VM access_count 绑定

- 代码检查点：`3683ec8e04601859143ee7d9b0213aa29ea686af`。
- ReplicaDirectory 可绑定到当前进程线程；Masstree 的 root/internal 遍历按
  NodeControl slab index 更新本 VM 的稠密 `access_count`，不使用 hash map。
  NodeControl 从 free 链重新 Reserve 时，目录会失效该 NodeRef 的旧副本并将
  counter 清零，防止 generation 复用沿用旧热点。
- 验证：重编 Masstree 测试并运行 `ctest --test-dir /tmp/dsidle-m1-build -R
  'dsidle_(replica_directory|masstree_single_process)_tests' --output-on-failure`
  （2/2 通过）。副本复制/命中和 SIDLE 五 worker 尚待接入。

## 2026-07-24 — M5 架构检查点：分段副本目录

- 代码检查点：`d2e3d51137b2ff71e191e73bad1d528c974a12e1`。
- 新增进程本地 `ReplicaDirectory`：按 NodeControl slab index、每段 4096 槽
  惰性分配；slot 含 seqlock、读者计数、generation/version、bytes/kind 与
  `local_ptr`。读者在取得指针前先登记，发布/失效在 odd seq 下等待本地读者
  离开后才交还旧缓冲，避免目录 `local_ptr` UAF；目录不把虚拟地址写回共享池。
- 新增单测覆盖发布、generation/version 失配、稳定读句柄、替换与失效。该
  检查点尚未把副本复制/命中接入 Masstree 或 SIDLE worker。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R dsidle_replica_directory_tests --output-on-failure`
  （1/1 通过）。

## 2026-07-24 — M4 测试检查点：节点 epoch reclaim

- 代码检查点：`25dc071e816fa3f0448e766d98ec9b94c71fe8c6`。
- Masstree 专项直接构造一个 canonical leaf 并沿真实 `deallocate_rcu` 路径回收；
  断言其 NodeControl 在 drain 前为 RETIRING、drain 后为 FREE，证明 SWCC body
  与控制项不会在 RCU 安全点之前复用。
- 验证：RelWithDebInfo 的 `dsidle_masstree_single_process_tests` CTest（1/1
  通过）。

## 2026-07-24 — M4 架构检查点：叶链 SWCC 发布顺序

- 代码检查点：`93743a32c2e8e7de17b323ba51aaee580d1b190a`。
- `btree_leaflink` 的并发版本现在对 `next_/prev_` 逐字段执行读前 invalidate、
  普通 store 后 flush/fence。split/change/unlink 的原有锁和操作顺序保持不变，
  没有在 SWCC 链字上恢复 CAS/RMW；节点版本解锁仍是最终 HWCC 发布点。
- 验证：RelWithDebInfo 重编 `dsidle_masstree_single_process_tests`，并运行同名
  CTest（1/1 通过，覆盖同步双进程 split 与 Scan∥Delete）。

## 2026-07-24 — M4 测试检查点：跨进程高冲突 split

- 代码检查点：`ae5f1b323d92840a47018007226e190b7ddc03d1`。
- Masstree 跨进程测试新增同步启动的两个 attach worker；每侧插入 64 个不重叠
  key，均超过叶容量并与另一侧重叠执行 split/root 发布。worker 退出前 drain
  自身 limbo；父进程随后逐项 GET，并由既有完整 Scan 的 `std::map` 对拍覆盖
  两侧插入集。
- 验证：`cmake --build /tmp/dsidle-m1-build --target
  dsidle_masstree_single_process_tests --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R dsidle_masstree_single_process_tests --output-on-failure`
  （1/1 通过）。

## 2026-07-24 — M4 架构检查点：线程退出 limbo drain

- 代码检查点：`77166a46f0ab9fdaa622932d0eff11e20ac13a92`。
- `threadinfo::rcu_drain()` 实现 worker join 前的唯一回收路径：撤销自身 epoch
  槽、反复推进/回收 limbo，直至本线程队列为空；30 秒内无法完成则 hard fail，
  不会把 limbo 悄然移交给其他线程。结束时再次写 INACTIVE。
- Masstree 跨进程测试在原映射关闭和重挂载结束前显式 drain 两个本地 threadinfo，
  并验证共享槽最小值回到 INACTIVE。
- 验证：`cmake --build /tmp/dsidle-m1-build --target
  dsidle_masstree_single_process_tests --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R dsidle_masstree_single_process_tests --output-on-failure`
  （1/1 通过）。

## 2026-07-24 — M4 架构检查点：NodeControl 成对 retire/reclaim

- 代码检查点：`8872b8eda8b3e08988f30a5689a6372e3c0618b8`。
- leaf/internode 摘树后进入 limbo 时，配对 NodeControl 立即转为 RETIRING 并
  记录相同 retire epoch。limbo 条目额外保存 NodeRef；仅在该节点 SWCC 体已按
  owner shard 投递 remote-free 后，才将控制项 Release 回 HWCC slab，避免
  NodeRef 在规范节点体仍受 RCU 保护时复用。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`（6/6 通过）。
  M4 仍缺线程退出前的 limbo drain 与高冲突 SMO/reclaim 压力测试。

## 2026-07-24 — M4 架构检查点：共享 epoch RCU 与 owner-shard limbo

- 代码检查点：`b3d1a75d76faa7d49606ed28009f191579c5bba3`。
- HWCC 诊断区首行现保存共享单调 epoch 时钟；Masstree 的 Get、Put、Delete
  与正/反向 Scan 均在完整 API 范围内发布/撤销对应 VM×thread epoch 槽。每个
  threadinfo 在第 50 个操作退出后才推进时钟并扫描全局最小活跃 epoch，避免把
  min 扫描放到每次读写热路径。
- limbo 的池对象记录现为 SWCC offset + tag/class + owner shard；回收时按 offset
  反查原分配 shard，并在 epoch 安全后投递到该 shard 的 remote-free 链。分配器
  专项覆盖跨 shard owner 识别与回收复用；Scan∥Delete 子进程确认父扫描的 epoch
  槽在整段叶链扫描期间保持 ACTIVE。
- 验证：`cmake --build /tmp/dsidle-m1-build --target
  dsidle_masstree_single_process_tests --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R 'dsidle_(epoch|shard_allocator|masstree_single_process)_tests'
  --output-on-failure`（3/3 通过）。M4 仍缺高冲突 SMO、成对 NodeControl retire/
  reclaim 与线程退出 limbo 排空，尚未完成。

## 2026-07-24 — M0：回滚与 D-SIDLE 骨架

- 代码检查点：`7c8f4b0378e655df3acb6bb6d26e1544eca98820`。
- 已建立 `dsidle/` 配置骨架、根 `experiment_config.jsonc` 与 CTest 配置契约；
  根 CMake 仅构建 D-SIDLE 骨架，已从构建图摘除基线 DAX/memkind、ART 与旧 benchmark。
- 验证：`cmake -S . -B /tmp/dsidle-m0-clang-build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build /tmp/dsidle-m0-clang-build --parallel && ctest --test-dir /tmp/dsidle-m0-clang-build --output-on-failure`（1/1 通过）。
- 环境说明：验收配置使用 Clang 18；默认 GCC 11 不支持合同要求的 `-flto=full`，因此 CMake 明确要求 Clang，未降低优化或 LTO 级别。

## 2026-07-24 — M1 架构检查点：共享池首次 attach

- 代码检查点：`fe3da1cd064d64fa0a24519aee0744aa5f5d6e04`。
- 新增双区域 `SharedPool`：PoolHeader/RootControl 固定于 HWCC 起点，布局仅允许 `hwcc.offset=0`、相接 SWCC，并在 create/attach 时校验 ABI、容量和跨进程 64 位 lock-free 原子。
- 新增 64B 对齐的 `NodeControl`/`RootControl` 与 64 位带类型 offset 基元；未接入基线 Masstree 前不改动其行为。
- 验证：`cmake -S . -B /tmp/dsidle-m1-build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`（2/2 通过；共享池测试含 fork 后 attach、SWCC 载荷与 HWCC 根版本可见性）。

## 2026-07-24 — M1 架构检查点：SWCC shard 分配器

- 代码检查点：`f74df50d344d6eed51379eab5a2e27d7dbe70e7d`。
- 已实现固定 size-class 的每 VM SWCC shard bump 区、HWCC local/remote-free 栈头及 offset 空闲链；remote-free 发布会逐行 flush 并 fence，单次分配最多收割 64 个对象。
- 验证：RelWithDebInfo 构建与 CTest（3/3 通过）。

## 2026-07-24 — M1：池与分配器完成

- 代码检查点：`aa41e30f51189bc67f74a1dff7a24b074f1c928a`。
- 新增 `dsidle_shared_pool --init-pool`：对已由宿主创建/清零的 backing 写入
  PoolHeader、RootControl、NodeControl slab、每 VM shard 控制区、共享 epoch 槽和
  诊断区；按配置打印 HWCC 预算并在容量不足时 hard fail。该路径只改固定元数据，
  不会误触完整 SWCC 区而重复预填充。
- NodeControl slab 使用 HWCC 空闲链，发布前写 canonical SWCC offset、递增
  generation，且要求 RETIRING 后才可归还；shard allocator 改为使用该固定布局。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（4/4 通过）；其中 shard 专项由子进程
  attach 后 remote-free，父进程 harvest 并复用同一 SWCC offset。另以 16MiB
  临时 layout 执行 `dsidle_shared_pool --init-pool --node-control-capacity 64
  --max-threads-per-vm 2`，成功输出 HWCC 预算。

## 2026-07-24 — M2 架构检查点：HWCC 版本与根访问器

- 代码检查点：`49ceccec1286abc21b296610455b2fdeb44ea760`。
- `NodeVersionAccessor` 将基线 32-bit Masstree nodeversion 的锁、插入、分裂、
  删除、根和 migration 位布局原样映射到 `NodeControl.version_and_state`；稳定
  读取同时取得 generation 与 canonical SWCC offset。新增 `RootControlAccessor`
  执行版本化 root-ref/root-generation 发布与双检读取。
- CTest target 均显式以 `-UNDEBUG` 编译，使 RelWithDebInfo 下的断言真实执行；
  新增 NodeControl/RootControl 单测，覆盖 slab generation 复用、root 发布以及
  插入/分裂版本跃迁。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（5/5 通过）。

## 2026-07-24 — M2 架构检查点：按进程解析共享 offset

- 代码检查点：`a07a3d78765bcd155dd080811c121f0d0e0db08f`。
- SharedPool create/attach 现在登记本进程（线程）的映射基址；该基址不进入共享
  backing，仅为后续 Masstree `NodeRef`/`SwccOffset` 解引用提供不同基址安全的
  本地解析点。close 会清除对应登记，未 attach 即解析会 hard fail。
- 验证：RelWithDebInfo 构建与 CTest（5/5 通过）。

## 2026-07-24 — M2 架构检查点：SWCC size-class slab

- 代码检查点：`fc9030281ab5b8ddb4f615d585b05f8d997fdd0f`。
- shard 分配器扩展为 64B 到 2MiB 的 16 个 2 的幂 size class；每 VM、每 class
  有独立 HWCC 控制行与 SWCC bump 区/本地 free 链/remote-free 链。`SwccShardAllocator`
  按对象大小选择 class，保持对象引用为 offset；生产 `--init-pool` 初始化全部
  class，单 class 初始化仅保留给专项测试。
- 验证：RelWithDebInfo CTest（5/5 通过）；分配器专项覆盖 97B、17KB、900KB
  三类分配，以及 remote-free harvest 后按原 offset 复用。

## 2026-07-24 — M2 架构检查点：原生 Masstree 接入 SWCC

- 代码检查点：`3dd6c5530e90a154ea507be5c20eaaed4dbdbd66`。
- 新增 `dsidle_masstree`，直接编译仓库内的 Masstree 源码；旧 CXL/memkind
  allocator 实现不进入构建图。运行时将当前进程绑定到一个 SWCC shard，Masstree
  canonical node、value 和 key-suffix 分配均经 D-SIDLE SWCC size-class 分配器，
  不存在 DAX/memkind 回退。SIDLE 前端的 canonical 分配也固定为 SWCC，策略选择
  留待 M5 用于副本。
- 新增原生 Masstree 单进程 PUT/GET CTest，验证其在共享池上完成创建、插入和读取。
  此检查点仍保留基线的进程内裸指针字段及临时本地 RCU epoch 兼容层；NodeRef
  偏移化、NodeControl 实际接管 nodeversion 和跨进程重挂载尚未完成，不能视作 M2
  出口。
- 验证：RelWithDebInfo CTest（6/6 通过）。

## 2026-07-24 — M2 测试检查点：Masstree 单进程对拍

- 代码检查点：`2dd49a36af6fdebf7c3c82e7cd4623b3b30c26d2`。
- 原生 Masstree 测试扩大为 64 个固定 8B key 的乱序写入、覆盖写及逐项 GET，
  使用 `std::map` 保存期望值；该规模会经过叶/内部节点分裂，验证值内容而非
  基线 `run_replace` 的返回枚举。SWCC OOM 诊断现在带 block、offset 和 limit，
  便于定位实际 shard/class 耗尽。
- 验证：RelWithDebInfo CTest（6/6 通过）。仍缺 offset/NodeRef 重写与重挂载，
  因此未达到 M2 退出条件。

## 2026-07-24 — M2 架构检查点：NodeControl 发布序

- 代码检查点：`9ca7a8b733850d6b0ce05a3379a7fd78a756b26c`。
- NodeControl slab 不再把刚取得的槽直接标成 PUBLISHED。调用方必须先 `Reserve`
  （ALLOCATING，写 canonical offset / generation）、初始化并 flush 配对 SWCC
  节点，再以 `Publish(initial_version)` 进行 release 发布；`Retire` 后才允许
  `Release` 回空闲链。`NodeVersionAccessor` 拒绝读取非 PUBLISHED 槽。
- 单测覆盖 leaf 初始位、版本稳定读取、insert/split 位跃迁、retire/release 及槽
  generation 复用；RelWithDebInfo CTest（6/6 通过）。此为接入 Masstree 构造器的
  必要前置，尚未使其节点字段偏移化。

## 2026-07-24 — M2 架构检查点：Masstree 节点成对控制项

- 代码检查点：`6d91ff56c944ddae260c08b162561e9a945b45c1`。
- 原生 leaf/internode 工厂现在为每个 SWCC 节点体 reserve 配对 NodeControl，将
  `NodeRef` 写入节点自身，flush 节点体后以正确的 leaf/non-leaf 初始 version
  发布控制行。当前线程的 SharedPool 绑定成为这条成对分配路径的唯一来源。
- Masstree 单测验证根节点的 NodeRef 已解析至其实际 SWCC offset；RelWithDebInfo
  CTest（6/6 通过）。树边、root、value 和 ksuf 仍未改写为 offset，nodeversion
  也仍由基线字段承载，故本检查点不等同完成偏移化。

## 2026-07-24 — M2 架构检查点：NodeRef 根发布

- 代码检查点：`c0ce0dd4e84c00701864ee640916d180438ccf58`。
- `basic_table` 不再持有持久化 `root_` 裸指针，而保存 NodeRef；`root()` 仅在
  栈上解析 canonical SWCC 地址。初始化和 `fix_root()` 均以 NodeControl 的
  generation 发布 RootControl，scan/print/destroy 等原有直接 root 使用点改为
  瞬时解析。
- Masstree 单测验证 RootControl 的 `{NodeRef,generation}` 与根 NodeControl/SWCC
  offset 一致；RelWithDebInfo CTest（6/6 通过）。child/parent/leaf/value/ksuf
  及 nodeversion 仍待改造，M2 未完成。

## 2026-07-24 — M2 架构检查点：内部 child NodeRef

- 代码检查点：`d2bbd22786391662f6fd49e119068b393ff67134`。
- `internode::child_[]` 已由裸指针改为 8B NodeRef 链接；链接在 SWCC 中只保存
  HWCC control offset，读取时才解析瞬时 canonical 地址。查找、split、复制和
  统计保留原有控制流，仅在模板实参推导无法进行隐式解析的位置增加显式栈上转换。
- 验证：64-key 分裂的 Masstree `std::map` 对拍以及完整 RelWithDebInfo CTest
  （6/6 通过）。parent、leaf link、value/ksuf 和 nodeversion 尚待改造。

## 2026-07-24 — M2 架构检查点：parent NodeRef

- 代码检查点：`6d0612c81b8fa7e9fadca59f6fbc691fd7bb3edf`。
- leaf 与 internode 的 `parent_` 均改为 NodeRef 链接。基线 destroy RCU callback
  原本复用 parent 裸指针作为工作队列 next 字段，现同样复用 NodeRef，避免在销毁
  路径把瞬时虚拟地址回写到 SWCC。
- 验证：RelWithDebInfo CTest（6/6 通过）；child/parent 树边均经 NodeRef 解析，
  但 leaf link、value/ksuf 与 nodeversion 仍待改造。

## 2026-07-24 — M2 架构检查点：leaf link NodeRef

- 代码检查点：`18664f9b91963edbcf3551367c0f75936f90f8f0`。
- leaf 的 `next_`（低位保留基线标记语义）与 `prev_` 均已改为 NodeRef；split、
  remove、scan、print 与链回收路径只在使用点解析瞬时地址。SWCC 链字不再执行
  CAS：M2 的单进程锁前提下使用普通写，跨 VM 的持锁 flush/fence 发布协议留待
  M4 完整实现。
- 验证：RelWithDebInfo CTest（6/6 通过）。value/ksuf 和 nodeversion 仍待改造，
  M2 未完成。

## 2026-07-24 — M2 架构检查点：外部 ksuf SWCC offset

- 代码检查点：`ecb6d8051d83f5a3443b18185ef0e492e7f86699`。
- leaf 的外部 `stringbag` 指针 `ksuf_` 已换为 `SwccOffset<stringbag>`；分配、
  重新分配、读取、预取和 RCU 延迟释放都只在调用点解析瞬时地址。新增共同前缀的
  160B 长键对拍，覆盖 layer / 外部 suffix 容器的写入与读取。
- 验证：RelWithDebInfo CTest（6/6 通过）。leafvalue/value 与 nodeversion 仍待
  改造，M2 未完成。

## 2026-07-24 — M2 架构检查点：leafvalue offset

- 代码检查点：`215f7539679124fb8eeafddc1291670d0bece975`。
- `leafvalue` 的 64-bit 槽不再存 value/layer 虚拟地址：普通 value 解释为
  `SwccOffset<row_type>`，layer 解释为 NodeRef。`tcursor` 及 kvrow/log/checkpoint
  的更新路径改为读取临时指针、完成原有更新逻辑后以 `set_value` 写回 offset，
  消除了 `R*&` 对共享字段的裸指针回写。
- 验证：随机/分裂/覆盖写、长共同前缀键的单进程 `std::map` 对拍和完整
  RelWithDebInfo CTest（6/6 通过）。nodeversion 仍待实际迁入 NodeControl，M2
  未完成。

## 2026-07-24 — M2 架构检查点：nodeversion → NodeControl

- 代码检查点：`690a6a9632d0aac258049a04ea04c7063cc441bd`。
- 保留基线 nodeversion 的全部位布局、stable/lock/unlock/mark 接口与快照语义；
  绑定 NodeRef 的活节点将版本 load/store/CAS 重定向至配对 NodeControl 的
  `version_and_state` HWCC 原子。nodeversion 拷贝仍是栈上稳定快照，不持有第二个
  可写句柄；未绑定的 single-thread 值保持原本地行为。
- 验证：分裂、覆盖写、长共同前缀的 Masstree 对拍与完整 RelWithDebInfo CTest
  （6/6 通过）。M2 剩余重挂载验证与针对不同映射基址的端到端证明。

## 2026-07-24 — M2 完成：不同基址重挂载

- 代码检查点：`7eb683af6789d17a33011ddca0a8c25cb85ece89`。
- `basic_table::attach()` 从固定 RootControl 采用 `{NodeRef,generation}` 并验证
  对应 NodeControl。测试关闭初始映射、以匿名 guard 占住旧虚拟基址，再通过
  `AttachAt` 把同一池固定映射到另一个地址；新 table attach 后逐项读取完整
  `std::map` 期望集（含分裂、覆盖写与共同前缀长键）。
- 审计 root、child、parent、leaf link、ksuf、leafvalue 的持久化字段均不再为
  裸指针；nodeversion 活节点状态位位于 NodeControl。
- 验证：RelWithDebInfo CTest（6/6 通过）。至此满足 M2“单进程 std::map 对拍 +
  重挂载 + commit/log”退出条件；后续进入 M3 跨进程预填充树的 Get/覆盖 Put/Delete。

## 2026-07-24 — M3 检查点：跨进程预填充树读取

- 代码检查点：`3692216f8d7d371b3ef50cedd0a49e79a614b3f2`。
- Masstree 测试现 fork 子进程：子进程关闭继承映射、重新 attach backing、绑定
  SWCC shard、独立创建 threadinfo/table 并从 RootControl attach；随后读取父进程
  预填充的完整期望集（分裂、覆盖写、长共同前缀键）。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 的覆盖 Put/Delete 和 SWCC 写入
  可见性发布仍待实现。

## 2026-07-24 — M3 检查点：跨进程覆盖 PUT / DELETE

- 代码检查点：`a36c79b829806facb9ee0ce83ed39d849b66d133`。
- 子进程独立 attach 后覆盖既有 key，父进程验证新值；另一子进程删除既有 key，
  父进程验证后续 Get 不再命中。两者均在预填充树上执行，不引入 split。
- 新增 `swcc_visibility.h`：value 槽写回前 flush 新 value，nodeversion unlock
  在 HWCC release 发布前 flush 对应 canonical 节点的基线 512B 节点包络，确立
  `SWCC 写/flush → HWCC version release` 顺序。Delete 专项同时补齐测试翻译单元
  对原生 `masstree_remove.hh` 的依赖。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 仍缺更严格的多进程线性化/规范
  值契约验证，M4 前不执行 SMO。

## 2026-07-24 — M3 检查点：SWCC 读侧 invalidate

- 代码检查点：`cb1f8d32ab9f7363df15dca1d786e9aa08515d6d`。
- `NodeVersionAccessor` 驱动的 Masstree `stable()` 在 HWCC 稳定版读取后精确
  invalidate 该 canonical 节点的 512B 基线包络；leafvalue 读取 value 前先
  invalidate value 首行并据 size invalidate 完整对象。写侧已有 flush/fence 后
  才 release 版本，因此读路径按 `稳定版本 → invalidate → SWCC 读` 执行。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 仍需以并发用例验证线性化，不进入
  M4 的 SMO/scan/reclaim。

## 2026-07-24 — M3 检查点：并发跨进程覆盖写

- 代码检查点：`440c4500442c919207c1afeb36c759832bdb0c3a`。
- 两个子进程分别重新 attach、经 pipe 屏障同时对同一预填充 key 执行覆盖写；父
  进程等待两者结束后读取该 key，断言结果恰为两个完整获胜值之一。该专项保持在
  既有叶内更新，不触发 split，覆盖 NodeControl CAS 锁与 release 版本发布。
- 验证：RelWithDebInfo CTest（6/6 通过）。此为 M3 线性化基础验证；更高冲突
  SMO、scan 和回收仍属 M4。

## 2026-07-24 — M3 完成：跨进程预填充树操作

- 退出证据：子进程重新 attach 后读取预填充完整树；独立子进程覆盖 PUT、DELETE
  后父进程观察到新值/缺失；两个同步起跑子进程竞争同一既有 key 后父进程只读到
  一个完整获胜值。所有 M3 写均为既有 leaf 内更新，不触发 split。
- 协议证据：写路径为新 value flush、canonical node flush、HWCC nodeversion
  release；读路径为 stable HWCC version、SWCC invalidate、value invalidate、
  基线 Masstree 原有 has_changed 复检。引用均经 offset/NodeRef 解析。
- 验证：RelWithDebInfo CTest（6/6 通过）。后续进入 M4 的 SMO、叶链、Scan 与
  epoch/limbo 回收；M3 的预填充树范围不替代这些测试。

## 2026-07-24 — M4 检查点：跨叶 Scan 对拍

- 代码检查点：`85ce90a4a5979ff63287ac092789f3c3ab14136b`。
- 新增直接调用原生 `basic_table::scan` 的 collector，对删除后的完整树逐条收集
  key/value，并与 `std::map` 的有序结果严格对拍。该树已包含多次 split、layer/
  长 key、跨进程覆盖写和 DELETE，因此覆盖 leaf link NodeRef 行走与 value offset
  读取的跨叶路径。
- 验证：RelWithDebInfo CTest（6/6 通过）。并发 Scan∥Delete、SMO 发布顺序及
  epoch/limbo 回收仍待 M4 后续实现。

## 2026-07-24 — M4 检查点：跨进程 split 可达性

- 代码检查点：`54f3832429d039ff3ea2643f0e018aaa9fade2be`。
- 独立 attach 的子进程向既有共享树插入 48 个新 key，触发额外 leaf/internal
  split；父进程随后逐项 Get 所有新增值并对全树执行 Scan/`std::map` 对拍。
  该路径验证 NodeRef child/parent、RootControl 和 leaf link 在跨进程 SMO 后仍
  能正确解析并行走。
- 验证：RelWithDebInfo CTest（6/6 通过）。并发 Scan∥Delete 与 epoch/limbo
  回收仍待 M4 后续实现。

## 2026-07-24 — M4 检查点：跨进程 Scan ∥ Delete

- 代码检查点：`42387e8824f9c9f6cb74baf5fcf0d65d5d80eb7e`。
- 父进程的原生版本化 Scan 在首个 leaf 回调时经 pipe 放行子进程，子进程独立
  attach 并删除一个既有 split key；扫描在删除并发期间安全结束，等待子进程后
  父进程验证该 key 不再可读。测试刻意延长首叶回调以形成真实重叠窗口。
- 验证：RelWithDebInfo CTest（6/6 通过）。epoch/limbo 回收仍待实现。

## 2026-07-24 — M4 检查点：共享 epoch 槽

- 代码检查点：`565d8e8ba2ba380b5e2b4352aa7bc66605203ac7`。
- epoch 单测现创建共享池并 fork：子进程关闭继承映射、重新 attach 后将自身 HWCC
  epoch 槽设为 17；父进程的 SharedEpochTable 观察到最小活跃 epoch=17，离开后
  最小值恢复为 INACTIVE。该测试同时保留本地 EpochTable 的 INACTIVE 语义验证。
- 验证：RelWithDebInfo CTest（6/6 通过）。limbo 记录的 offset 化和按 epoch
  remote-free 回收仍待接入。

## 2026-07-24 — M4 检查点：limbo SWCC offset

- 代码检查点：`638b5eb1f183513c1b8db7d873c262dec9fe9d77`。
- kvthread limbo 对 memtag_pool_mask 的 canonical 对象不再记录 `void*`，而记录
  SWCC offset；`free_rcu` 仅在实际回收时解析 offset。基线本地 callback 对象
  （`memtag(-1)`）保持进程本地地址，不作为共享规范数据。report 路径按同一
  offset 比较，避免诊断时重新依赖虚拟地址。
- 验证：RelWithDebInfo CTest（6/6 通过）。共享 epoch 与 limbo 的 threadinfo
  生命周期绑定、owner-shard 批量 remote-free 仍待 M4 后续接入。

## 2026-07-24 — M5 检查点：副本预算与代际队列

- `ReplicaDirectory` 现在是每 VM 本地副本字节数的唯一记账点：预算化发布在替换
  时按净增预留，淘汰/失效按旧副本字节扣减；预算不足会拒绝发布且保持旧快照。
  leaf/internode 提升均走此入口，因此不再存在绕过 `replica_budget_mb` 的完整
  本地副本。
- 新增 `QueuedNodeRef{NodeRef,generation}` 以及 MoodyCamel `replica_queue`；D-SIDLE
  策略候选不再以跨 VM 无效的 canonical 虚拟地址作为载荷，执行前可据 generation
  丢弃复用控制项的陈旧任务。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_replica_directory_tests --parallel`
  后执行 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R dsidle_replica_directory_tests`
  （1/1 通过）。

## 2026-07-24 — M5 检查点：副本执行器

- 新增 Masstree 副本执行器：提升任务先校验 `NodeControl` 的 PUBLISHED 状态和
  generation，再从候选叶向祖先发布稳定副本，直到已有有效本地副本；淘汰仅失效
  本 VM 目录项且根不可淘汰。它不持有 canonical 锁，不改 parent/link，也绝不
  对 canonical 节点执行 RCU free。
- 单进程专项直接验证 generation 不匹配任务被拒绝、提升后可 acquire、淘汰后
  缓存消失；保留随后规范写会使副本失效的既有检查。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_masstree_single_process_tests --parallel`
  后执行 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  'dsidle_(replica_directory_tests|masstree_single_process_tests)'`（2/2 通过）。

## 2026-07-24 — M5 检查点：本地访问冷却

- 副本目录的 slab-index 稠密本地访问计数新增原子减半操作；SIDLE cooler 将据此
  保持原始 `access_time >>= 1` 行为，同时不会写入 canonical SWCC leaf 元数据。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_replica_directory_tests --parallel`
  后执行 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  dsidle_replica_directory_tests`（1/1 通过）。

## 2026-07-24 — M5 检查点：副本五 worker

- 新增 D-SIDLE 的五角色 worker 组：trigger 按原 `basic_interval × 5` 遍历规范
  叶、从本 VM 稠密访问计数产生 generation-qualified 候选；promotion/demotion
  executor 仅执行本地副本动作；cooler 减半本地计数；adjuster 根据副本预算水位
  调整原阈值对象。root pin 在每轮 trigger 跟随 RootControl。
- 验证：相关 RelWithDebInfo 构建后 `ctest --test-dir /tmp/dsidle-m1-build
  --output-on-failure -R 'dsidle_(replica_directory_tests|masstree_single_process_tests)'`
  （2/2 通过）。

## 2026-07-24 — M5 检查点：MasstreeKV 副本运行时入口

- `MasstreeKV` 构造器改为显式接收 `SharedPool` 与 `replica_budget_mb`，并拥有本
  VM `ReplicaDirectory`；foreground `worker_enter` 绑定该目录。原
  `init_migration_worker` 现启动副本五 worker，旧
  `masstree_leaf_migration/internode_migration` 回调不再位于运行入口。
- 验证：`clang++-18 -std=c++17 -fsyntax-only -include
  third_party/masstree-beta/config.h -I. -Isrc -Ithird_party/masstree-beta
  -Ithird_party/sidle_utils -Ithird_party/art -x c++ src/kv/masstree.h` 通过（仅有
  既有第三方告警）。

## 2026-07-24 — M5 修正：worker 规范遍历

- 副本 worker 不再调用会在当前 offset 化树上卡住的旧 `masstree_leaf_traverse`；改为
  在 epoch 临界区内读取稳定 internode 子 `NodeRef`、二次验版后 DFS 到 canonical
  leaf。遍历仍只读规范树，并将候选操作限制在本地副本目录。
- 验证：RelWithDebInfo `dsidle_masstree_single_process_tests`（1/1 通过）。

## 2026-07-24 — M5 检查点：trigger 到副本执行器

- 单进程专项对 canonical leaf 注入本 VM 高访问计数，直接运行副本 trigger 与
  promotion executor，断言产生的 `QueuedNodeRef` 最终发布匹配 generation/version
  的本地副本；随后运行 cooler 并淘汰该副本。该路径不触及 canonical parent/link
  或 RCU。
- 验证：RelWithDebInfo 构建后执行
  `timeout 15s /tmp/dsidle-m1-build/dsidle_masstree_single_process_tests`（通过）。

## 2026-07-24 — M5 检查点：副本目录并发发布/淘汰

- 目录专项令 reader 在持有 `ReadHandle` 时持续读本地副本，同时 writer 连续 1,024
  次发布替换并立即释放被目录交还的旧缓冲；reader 只观察到完整已初始化内容。
  该用例覆盖 slot seqlock 与 reader count 对 publish/free 的 UAF 防护。
- 验证：RelWithDebInfo `dsidle_replica_directory_tests`（1/1 通过）。

## 2026-07-24 — M5 修正：跨 VM 副本惰性失效

- 覆盖写与删除在持叶锁时标记 canonical nodeversion 的插入位，因而 release unlock
  会发布新稳定版本；跨 VM 的旧副本无需被写者触碰，读路径的 canonical 版本双检
  即会拒绝它。
- 单进程中以两个独立 `ReplicaDirectory` 模拟 VM A/B：B 预先保留旧 self-contained
  leaf，A 写后仅失效自身槽；B 的旧槽仍存在，但 B 的 `unlocked_tcursor` 返回新值，
  而非旧副本行。
- 验证：RelWithDebInfo 构建后 `timeout 15s
  /tmp/dsidle-m1-build/dsidle_masstree_single_process_tests`（通过）。

## 2026-07-24 — M5 完成验证：基线策略对拍

- 按计划创建基线 `1f1fa8ae9459935963a67a9beef6f30683c3c47b` 的 detached
  worktree（`/tmp/dsidle-baseline-policy`），对完整
  `third_party/sidle_utils/sidle_worker.hh` 与 `sidle_policy.hh` 执行 `diff -u`；
  两者均无输出，即直方图、hot/warm/cold 分类、16 桶指数映射、水位、阈值调整、
  `queue_waiting_threshold=10`、cooler 和三态 adjuster 与基线字节级一致。
- 当前 M5 对这些选择结果的唯一变化是：候选载荷从旧虚拟地址迁移语义替换为
  `QueuedNodeRef{NodeRef,generation}` 的本地副本 promote/evict；其行为已由
  trigger→executor 专项覆盖。对拍 worktree 随后已移除。

## 2026-07-24 — M6 架构检查点：延迟模拟基础模块

- 从 cxlkv `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` 的
  `src/utils/{include,src}/latency_simulator.*` 开始移植；本仓新增
  `dsidle/latency_simulator.{h,cc}` 并接入 `dsidle` 静态库。当前基础具备
  relaxed fast gate、4ms TSC 校准、嵌套 scope、忙等、行范围统计和配置校验。
- 此检查点尚未声称 M6 完成：fixed-hit-rate/per-thread-LRU、全字段配置与
  Masstree/allocator 插入点仍待后续提交。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle --parallel`（通过）。

## 2026-07-24 — M6 检查点：完整延迟配置 schema

- `ExperimentConfig` 现持有完整 `latency_sim::Config`，JSONC 解析器要求计划列出的
  全部延迟字段并解析 cache model；默认 `experiment_config.jsonc` 明确提供全零
  `--no-latency` 档。缺字段/非法 cache model 均 hard fail。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_config_tests --parallel` 后
  `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R dsidle_config_tests`
  （1/1 通过）。

## 2026-07-24 — M6 检查点：SWCC flush 计费薄入口

- 延迟模拟头新增统一的 SWCC/HWCC 访问 wrapper；`FlushSwccRange` 通过
  `RecordSwccFlush` 计入 SWCC flush 行访问，关闭时仅执行 relaxed gate 分支。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_masstree --parallel`
  （通过，既有第三方告警不变）。

## 2026-07-24 — M6 检查点：NodeControl HWCC 原子计费

- `NodeVersionAccessor` 的 stable、try_lock 和 unlock_release 经统一 wrapper 分别
  记录 HWCC atomic load/RMW/store；关闭模拟时仍是单个 relaxed gate 分支。
- 验证：RelWithDebInfo `dsidle_node_control_tests`（1/1 通过）。

## 2026-07-24 — M6 检查点：延迟 scope 与统计单测

- 新增 `dsidle_latency_simulator_tests`：覆盖嵌套 scope 仅在最外层 delay、跨两个
  SWCC cacheline 的计费，以及 HWCC atomic load 的 raw/miss/delayed 统计恒等式。
- 验证：重新 CMake configure 后构建该目标，并执行
  `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  dsidle_latency_simulator_tests`（1/1 通过）。

## 2026-07-26 — 一致性修正：canonical 节点首次读取

- 代码提交：`a3f23f0`。
- `ResolveCanonicalNode` 在返回 SWCC canonical 节点体前先失效首个 cacheline，
  避免随后 `nodeversion` 从陈旧节点体读取 `control_ref_`；保留原
  Masstree/SIDLE 遍历与版本状态机不变。
- `dsidle_node_control_tests` 新增真实 canonical offset 的发布、解析和值检查。
- 验证：`cmake --build build -j2 --target dsidle_node_control_tests` 后执行
  `ctest --test-dir build -R '^dsidle_node_control_tests$' --output-on-failure`
  （1/1 通过）。

## 2026-07-26 — 一致性修正：节点写锁后的 SWCC 视图

- 代码提交：`de269e3`。
- 保留原 `nodeversion::lock/try_lock` 的 HWCC 版本锁状态机，在成功获得锁后、首次
  读取或修改 canonical 节点体前失效完整 SWCC 节点信封；同步修正独立
  `NodeVersionAccessor`，避免写者用陈旧缓存覆盖其他 VM 已发布的数据。
- 验证：构建 `dsidle_node_control_tests` 与
  `dsidle_masstree_single_process_tests` 后执行
  `ctest --test-dir build -R
  '^dsidle_(node_control|masstree_single_process)_tests$' --output-on-failure`
  （2/2 通过）。

## 2026-07-26 — 一致性修正：external key suffix

- 代码提交：`1f3eb6e`。
- 保留原 Masstree `stringbag`、节点锁、版本重试与 RCU 回收方式，但禁止对已发布
  external suffix bag 原地 `assign`：live leaf 更新改为 copy-on-write，新 bag
  完整写入并 flush 后才把 SWCC offset 装入节点体，旧 bag 继续走原 RCU 延迟释放。
- external suffix 读路径先失效 bag，再按完整容量记录 SWCC read；新节点尚未发布时
  仍可复用其私有 bag，避免无必要复制。
- 专项将根叶的内嵌 suffix 空间设为 0，连续插入两个 32B key，断言第二次插入发布
  新 bag 且旧 RCU suffix 字节保持不变。
- 验证：`cmake --build build -j2 --target
  dsidle_masstree_single_process_tests` 后执行
  `ctest --test-dir build -R '^dsidle_masstree_single_process_tests$'
  --output-on-failure`（1/1 通过）。

## 2026-07-26 — 一致性修正：并发叶链更新

- 代码提交：`006d6c8`。
- 保留原 Masstree `link_split/change_link/unlink` 的相邻叶检查、写入顺序和
  spin callback；将原进程内指针 CAS 所表达的“next link 修改权”放入对应节点已有
  HWCC `NodeControl` 的剩余空间。拿锁后失效并复核 SWCC link，写入和 flush 完成后
  才 release，避免此前普通 store 造成跨 VM lost update。
- 没有对 SWCC link 执行 CAS/RMW，也没有要求同时持有相邻叶的主 nodeversion 锁；
  `NodeControl` 仍保持单 cacheline。
- `dsidle_node_control_tests` 覆盖 link lock 的互斥与释放；包含两个跨进程并发
  split、叶链 scan 和删除的 Masstree 专项连续执行 10 次。
- 验证：构建 `dsidle_node_control_tests` 与
  `dsidle_masstree_single_process_tests`，执行
  `ctest --test-dir build -R
  '^dsidle_(node_control|masstree_single_process)_tests$' --repeat
  until-fail:10 --output-on-failure`（两项各 10 次通过）。

## 2026-07-26 — 一致性修正：完整节点与 root 发布顺序

- 代码提交：`532d7cd`。
- leaf/internode 构造完成（包括可选 debug 字段）后按实际分配大小写入并 flush
  完整 SWCC 节点体，再将 `NodeControl` 从 ALLOCATING 发布为 PUBLISHED；删除了
  只 flush 首 cacheline 的实现。
- 未加锁的新 root/layer 在通过 HWCC `root_bit` 发布最终状态前完整 flush 节点体；
  已加锁的 split 子节点仍复用原 `nodeversion::unlock` 发布路径。无
  `NodeControl` 的原始本地节点不增加 flush。
- 验证：`dsidle_masstree_single_process_tests`（包含初始 root attach、并发
  root split、跨进程 attach/read 与完整 scan）连续 10 次通过，最终改动后再执行
  1 次通过。

## 2026-07-26 — 一致性修正：RootControl seqlock

- 代码提交：`0c6428a`。
- `RootControl` writer 先用 HWCC CAS 将版本置为奇数，写入 `{root_ref,
  root_generation}` 后再 release-store 偶数版本；reader 遇奇数重试，并只接受前后
  相等的偶数版本。由此多个 VM 同时 `fix_root()` 时不会返回撕裂配对。
- 专项使用两个 writer 各发布 10,000 次不同 ref/generation 配对，reader 持续验证
  配对完整性和偶数版本。
- 验证：`dsidle_node_control_tests` 连续 50 次通过；
  `dsidle_masstree_single_process_tests` 1 次通过。

## 2026-07-26 — 一致性修正：RETIRING 节点的 RCU reader

- 代码提交：`9176578`。
- `ResolveCanonicalNode` 与只读 stable-version gateway 允许解析 RETIRING
  NodeControl；共享 epoch 仍保证其 canonical body 在最后一个旧 reader 离开前
  不会释放或复用。由此 optimistic reader 可观察 deleted/version 并按原 Masstree
  规则重试。
- 新 writer 的 HWCC `try_lock` 仍只接受 PUBLISHED，FREE/ALLOCATING/RETIRING
  均拒绝。
- 专项断言 RETIRING body/version 可读、写锁被拒绝；节点控制测试和包含并发删除
  与 scan 的 Masstree 专项各连续 10 次通过。

## 2026-07-26 — 一致性修正：副本后台工作的共享 epoch

- 代码提交：`1ee4749`。
- promotion、demotion 与 root-pin refresh 从 generation 校验、canonical 解析、
  snapshot copy 到目录 publish/evict 的完整区间均进入原 `threadinfo::rcu_scope`，
  防止 NodeControl 检查后 canonical body 被另一 VM 回收。
- promotion/demotion executor 使用独立后台 epoch slot，cooler slot 相应顺延；
  正式 trace runner 的最低槽位检查由前台 worker `+2` 改为实际使用的 `+4`。
  adjuster 不解引用 canonical 节点，不占 epoch slot。
- 验证：构建 `dsidle_masstree_single_process_tests` 与
  `dsidle_e2e_trace_runner`，Masstree 专项连续 10 次通过。

## 2026-07-26 — 生命周期修正：run_get1 返回值

- 代码提交：`84b1220`。
- 保留原 `run_get1(..., Str&)` 调用形式，但在 RCU scope 内将返回列复制到
  `query` 自己持有的 `lcdf::String`；返回 `Str` 的有效期延长到同一 query 的下一次
  GET，不再暴露 canonical value 或 replica buffer 的裸地址。
- 调试中首次误用 `lcdf::String::make_stable`，专项立即发现返回地址仍位于 SWCC。
  根因是该 API 表示“调用者保证外部字节永久稳定”，并不复制；已改为真正拥有字节
  的 `lcdf::String(ptr,len)` 构造并从专项起点重跑。
- 专项在 GET 返回后执行 128 次覆盖和 RCU drain，断言旧返回值仍为原字节且地址
  不位于共享池。
- 验证：构建 Masstree 专项、E2E suite runner 与 trace runner；
  `dsidle_masstree_single_process_tests` 连续 10 次通过。

## 2026-07-26 — 策略修正：惰性目录段的首次热度

- 代码提交：`43e5568`。
- `ReplicaDirectory::RecordAccess` 在目标 slab-index 的本地段尚不存在时通过既有
  `Ensure` 惰性创建该段，而不是静默丢弃访问；仍不预分配完整 NodeControl 容量。
- 专项直接访问第二个 4,096-slot segment，验证首次访问前为 0、记录后为 1。
- 验证：`dsidle_replica_directory_tests` 与
  `dsidle_masstree_single_process_tests`（2/2 通过）。

## 2026-07-26 — 副本修正：版本失配后的预算回收

- 代码提交：`5057bb1`。
- `ReplicaDirectory::Acquire` 发现本地 snapshot 与调用者 canonical
  generation/version 失配后，二次读取当前 NodeControl；只有 NodeControl 仍精确
  等于调用者快照时才机会式清除失配槽、等待规则允许时释放 buffer 并扣减
  `LocalBytes`。旧 reader 因此不会误删较新副本，也不依赖 root/migration 状态位的
  数值单调性。
- 调试中首版惰性回收会等待其他 `ReadHandle`，专项在“持旧 handle 再请求新
  generation”处挂起。根因是 foreground miss 自己同步等待合法 reader；已改为
  有 reader 时立即放弃、由后续 miss 重试，并终止了该次卡住的本地测试进程。
- 远端 VM 模拟测试现断言：canonical 更新后的首次读拒绝旧副本，并使该 VM
  `LocalBytes` 回到 0。
- 验证：`dsidle_replica_directory_tests` 与
  `dsidle_masstree_single_process_tests` 各连续 10 次通过。

## 2026-07-26 — 策略与一致性修正：canonical hotness

- 代码提交：`9aa01fc`。
- 恢复原 SIDLE 默认访问调用语义：lookup 只在最终 leaf 执行一次
  `record_access()`，删除 D-SIDLE 遍历额外给每层 internal/leaf 计数的逻辑。
- 有 NodeControl 且绑定本地目录时，leaf 热度写入本 VM `ReplicaDirectory`；不再对
  SWCC `sidle_meta.access_time` 原地自增。无 NodeControl 的原始本地路径继续执行
  原计数，`CAL_NODE_HOTNESS` 可选路径也采用同一分流。
- 验证：构建 Masstree 专项与正式 trace runner；
  `dsidle_masstree_single_process_tests` 连续 10 次通过。

## 2026-07-26 — 副本策略修正：root pin

- 代码提交：`c30d95b`。
- root pin 复用原 leaf/internal replica snapshot 编码和目录发布，只将该次发布设为
  不受普通 leaf budget 拒绝；其他 promotion 默认仍走 `TryPublish` 预算检查。
- trigger 在遍历和排普通候选前先刷新 root pin，避免根与 leaf 竞争后才尝试置顶。
  root bytes 仍计入 `LocalBytes`，因此超出 pin 后普通候选会自然停止。
- 专项将目录预算设为 0，断言 root refresh 仍成功且本地字节非 0，随后恢复预算；
  Masstree 专项连续 10 次通过。

## 2026-07-26 — 原 SIDLE 策略恢复：递归 promotion/demotion

- 代码提交：`025a188`。
- 删除“跨 VM parent 是 raw pointer、只能提升单节点”的假设；当前 parent/child
  均由 NodeRef-backed link 表示。promotion 恢复为从候选 leaf 沿祖先递归，遇到
  已有有效本地副本才停止。
- demotion 恢复原控制含义：先移除 cold leaf，再依据
  `demotion_depth_threshold`、depth 1/root 固定、以及“父仍有有效本地 child”
  选择 proceed/delay/stop；delay 仍回到 generation-qualified 本地队列。
- 迁移动作仅替换为本 VM replica publish/evict，不锁、重连或释放 canonical
  Masstree 节点；整个递归仍位于共享 epoch。
- 专项先显式移除 root replica，再从 leaf promotion 断言 leaf 与 root 均被发布；
  Masstree 专项连续 10 次通过。

## 2026-07-26 — 原 SIDLE 策略恢复：tri-state threshold adjuster

- 代码提交：`2d03af4`。
- 恢复原策略的 `tight/normal/sufficient` 三态控制：tight 时暂停 promotion、
  收紧本地分配和 demotion depth，并安排原常量指定的三轮强制 cold demotion；
  恢复后对称还原阈值，sufficient 时才逐步放宽。
- 保留现有五角色 worker，不另造策略栈；强制 demotion 复用 trigger 与
  generation-qualified 队列，adjuster 不接触 canonical 节点。
- 调试中首次构建发现遍历 lambda 未捕获强制 demotion 标志，修正后重新构建。
  此外发现 CTest 正则没有匹配已注册测试，未将 “No tests were found” 当作成功，
  改为直接执行专项二进制。
- 验证：构建 Masstree 专项与正式 trace runner；
  `dsidle_masstree_single_process_tests` 实际连续执行 10 次通过。

## 2026-07-26 — 正式入口修正：SIDLE policy bootstrap

- 代码提交：`8f39e58`。
- `ExperimentConfig` 现在保存并校验 `hot_percentage_seed`（0..100）；trace/suite
  runner 在任何建树、attach 或 foreground 启动前，用原
  `sidle::strategy_manager` 完成唯一策略初始化。
- replica workers 直接复用该 manager 持有的原 `sidle_threshold`，不再另行默认
  构造一份阈值；建树参数也由硬编码 80 改为配置种子。这样 split 不再可能解引用
  null strategy manager，配置也不再是空壳。
- 将原本遗漏的 `sidle_frontend.cc` 纳入 Masstree 库，并删除策略构造时的无条件
  `[DEBUG]` 正常路径输出。同步将本地 CTest 池的 epoch fixture 从过时的
  `foreground+2` 改为运行时真实要求的 `foreground+4`。
- 调试中首次 smoke 正确拒绝了旧 fixture 的不足槽位；没有放宽生产校验，修正
  fixture 后从该项检查起重跑。
- 验证：RelWithDebInfo 构建 config/trace/suite targets；
  `dsidle_config_tests` 与 `dsidle_e2e_trace_runner_smoke` 2/2 通过。

## 2026-07-26 — 公平性修正：e2e08/e2e09 字节合同

- 代码提交：`0cfbfda`。
- 删除 `suite.prefix[0] == '0'` 启发式，使用显式 `SuiteKind::k08/k09`
  分派 key、value 与 deterministic read seed。
- e2e09 现在与 cxlkv 一致使用 `u` 开头的 32 B key、generation-aware
  SplitMix64 1000 B value、`0xE2090000` read seed；同时修正 value byte 提取遗漏
  `& 0xff` 的第二处内容差异。e2e08 保持 `k`/base36/`0xE2080000`。
- 审计参考中“实际 key/value 长度畸形”的判断不准确：旧实现的长度一直是
  32/1000 B，错误是整套内容生成与 seed 合同。
- 验证：构建 suite runner；用独立硬编码向量检查两套 key、08 value、09 三组
  value FNV64 与两组 read index，全部逐字节/逐值匹配；随后
  `dsidle_e2e09_suite_smoke` 四进程 fill/update/read 通过。
- 临时字节测试首个链接命令误把 LTO 静态库当作 C++ 输入，第二次又遗漏
  `-flto=full`；这两次是测试命令错误，修正命令后合同测试通过。

## 2026-07-26 — 公平性修正：公开 Masstree Scan 语义

- 代码提交：`2136159`。
- 删除正式 trace runner 的 count-only `ScanCounter`；YCSB-E 现在直接复用原
  `query::run_scan` / `query_json_scanner`，因此 live row 才消耗 limit，并实际复制
  key、通过 `emit_fields1` 物化 value。核心 `masstree_scan.hh` 状态机未重写。
- 原 scanner 以独立 `unlimited_` 状态支持 `limit==0`，避免有符号计数下溢；
  scan、scan_versions、reverse scan 的公开前置条件同步允许 0。
- 专项新增 limit=3 和 limit=0 两种扫描，分别断言三条结果与全部 live rows；
  smoke trace 自身包含 SCAN，因此同时覆盖正式 runner 边界。
- 验证：构建 Masstree 专项与 trace runner；Masstree 专项连续 10 次通过，
  `dsidle_e2e_trace_runner_smoke` 通过。

## 2026-07-26 — 原 SIDLE 语义恢复：Scan 命中 leaf replica

- 代码提交：`c9cead5`。
- 保留原 `masstree_scan.hh` 状态机、leaf-link、retry 和 layer traversal；仅在每次
  `visit_leaf` 时按同一 canonical generation/version 尝试获取一个本地 snapshot
  handle，并在该叶的 value 回调中优先读取 snapshot，miss 时回退 canonical。
- handle 覆盖整片 leaf 而不是每条记录重复 acquire，保持原 SIDLE “local leaf
  连续扫描”的性能形态；跨叶、retry、上下层切换都会先释放旧 handle。
- 专项对已 promotion 的 leaf 从对应 key 发起 Scan，断言回调拿到的 row 地址位于
  SWCC 池外，证明不是只测试结果相同而实际仍走 canonical。
- 调试中首次编译发现 switch case 内局部 value 初始化会被其他 case 跳转跨过；
  将 scan_emit 保持为独立作用域后从构建起重跑。
- 验证：构建 Masstree 专项与 trace runner；副本地址断言及 Masstree 专项连续
  10 次通过，正式 trace smoke 通过。

## 2026-07-26 — E2E 修正：运行完整 D-SIDLE 策略路径

- 代码提交：`6ee7e86`。
- suite runner 每个 VM 只创建一个进程级 `ReplicaDirectory`，所有 foreground
  与原五角色 replica workers 共享它；删除每 foreground 线程临时创建、相位结束
  即丢弃的目录。
- 08/09 在 root attach 后启动与正式 trace runner 相同的 trigger、
  promotion、demotion、cooler、adjuster，并使用同一配置 budget/threshold；
  epoch slot fixture 同步为真实 `foreground+4`。
- suite 增加统一 `DSIDLE_MEMORY_STATS`，便于验收确认不是只完成 canonical
  正确性而未产生本地副本。
- 调试中首次构建发现 suite 缺少 replica worker 头文件，补入原现有头文件后从
  构建起重跑。
- 验证：四进程 e2e08 fill/read smoke 通过；四个 read 进程最终
  `replica_bytes` 分别为 564762、376054、454012、358364，均实际激活副本。

## 2026-07-26 — 公平性修正：E2E phase 计时边界

- 代码提交：`bd24d2c`。
- foreground 线程先完成 allocator、directory、threadinfo 注册，再在本地门闩
  等待；所有 VM 的 ready barrier 返回后才统一开表和释放线程。
- 本地最后一个 foreground worker 完成时立即停表；线程 join、replica workers
  stop 和跨 VM 结束 barrier 全部位于 workload duration 之外。
- 起止 barrier 依 cxlkv 格式单独输出 `E2E_BARRIER_TIME_US`；结构化 timing 的
  `trace_first/trace_workers` 也改为真实 node 分片和 foreground 数，不再固定
  `0/1`。
- 调试中首次编译把 barrier 工厂函数名误当类型；改用其真实返回类型
  `SharedPhaseBarrierView` 后从构建起重跑。
- 验证：四进程 e2e08 fill/read smoke 通过；read node0 示例为
  25,000 ops / 278,184 us，起止 barrier 均单独成行且不计入该 duration。

## 2026-07-26 — 公平性修正：trace batch replay

- 代码提交：`187e753`。
- `--batch-ops` 现在与 cxlkv 一样控制 `TracePrefetcher` 每次解析/暂存的最大命令
  数，再按原顺序逐条回放；删除原 `(void)batch_ops` 空壳。
- 注释/空行跳过、严格行号错误、固定 key/value、PUT RNG 消耗顺序和最短时长
  重放语义保持不变。
- 验证：构建 trace runner；显式 `--batch-ops 1` smoke 回放 5 ops 且结构化输出
  确认 `batch_ops=1`，默认 4096 的正式 trace smoke 亦通过。

## 2026-07-26 — 公平性修正：trace/YCSB 计时边界

- 代码提交：`c0b5bdf`。
- 所有 foreground 先绑定 allocator/directory、创建 threadinfo，再等待本地门闩；
  所有 VM ready 后执行计时窗外的 shared phase barrier，开表后才统一释放。
- 最后一个 foreground 完成时立即停表；heartbeat 启动等待、thread join 与五个
  replica workers 的 stop 均不进入 workload duration。补上 runner 原先依赖析构
  的隐式 worker stop。
- 起始 barrier 依 cxlkv 格式单独输出 `E2E_BARRIER_TIME_US`。
- 验证中首次组合命令短暂观测到 target `Permission denied`，随即检查确认文件为
  0755 ELF、重新直接执行和重链均正常，判断为并发链接时测试启动的瞬态测试编排
  问题；随后单 VM smoke 与双 VM smoke 从头分别通过。双 VM node0 示例：
  start barrier 56 us，5 ops workload 125 us，两者分开报告。

## 2026-07-26 — 公平性修正：08/09 foreground affinity

- 代码提交：`5b09238`。
- suite 的 foreground worker 现在与 cxlkv `vm_test_pool_common` 一样按
  `worker_index` 绑定 guest logical CPU；只修改双方本来就有相同合同的 08/09
  路径。
- YCSB trace 路径未单边加 affinity，因为 cxlkv 对应 trace pool 当前也未显式
  绑核；heartbeat 逐 op atomic 同理暂不单边改成 TLS，以免制造不对称加速。
- 验证：构建 suite runner，四进程 e2e08 fill/read smoke 通过。

## 2026-07-26 — 延迟模型修正：canonical 节点访问完整覆盖

- 代码提交：`caaf7d5`。
- 保留原 Masstree 的 512B 节点信封和 stable/lock/unlock 顺序；在实际热路径
  `nodeversion` 的 HWCC load/store/CAS 上补齐原子访问计费，不再只给未被主路径
  使用的辅助访问器计费。
- `nodeversion::stable()`、写锁取得以及辅助 `NodeVersionAccessor` 现在在访问
  separator、child、permutation 和 payload metadata 前失效并计费完整 canonical
  节点；解锁发布前同时计 SWCC write 与 flush。`ResolveCanonicalNode` 仍只负责
  首行 `control_ref_` 可见性，避免改变原控制流。
- RootControl/NodeControl 的普通 HWCC 字段读写与 allocation-state 原子读取纳入
  对应 read/write/atomic 类别；SWCC 与 HWCC 仍使用各自独立的可配置延迟。
- 修正 external value 读取在获知长度前读取陈旧 header、且首行被重复 clflush
  的问题：先失效首行读取长度，再只失效余下范围并按完整 value 计一次 read。
- 验证：完整构建通过；`node_control`、`latency_simulator`、
  `masstree_single_process` 三个专项测试通过。

## 2026-07-26 — SWCC 修正：多列 value metadata 可见性

- 代码提交：`5a9e355`。
- external value 读取先失效首行并取得列数；当 offset table 超过首行时，先失效
  table 余部再调用原 `size()`，最后仅失效尚未覆盖的 value body。这样多列
  value 不会为了确定总长度而先读取陈旧的跨行 offset，也不会重复失效已覆盖行。
- 固定单列 YCSB 路径仍只有原有的首行 header 步骤，没有为对比负载增加额外
  cache-line 操作。
- 验证：重建并通过 `dsidle_masstree_single_process_tests`。

## 2026-07-26 — 共享分配器修正：ABA、READY 发布与异常回滚

- 代码提交：`f7b225c`。
- SWCC size-class free head 与 NodeControl slab free head 共用 64-bit
  offset+34-bit generation 编码；每次 pop/push 都推进 tag，禁止回绕，消除并发
  A→B→A 令陈旧 CAS 再次成立的问题。为保持单字 HWCC 原子，池上限明确为 64GiB。
- pool ABI 升至 v2，初始化状态拆成 INITIALIZING/READY；固定 HWCC metadata 和
  全部 16 个 SWCC size class 完成并同步后才 release-publish READY，attach 会
  拒绝半初始化池，并可按配置精确核对 total/HWCC/SWCC layout。
- 新增只处理 ALLOCATING 槽的 `NodeControlSlab::Cancel`；leaf/internode
  `make_with_cxl_policy` 用小型 RAII guard 成对持有 SWCC body 与 control，
  Reserve/构造/flush/Publish 任一步异常都会归还尚未发布的两项资源。
- 未改变原 16 个 size class 的容量权重，也未把 RCU free 链改造成新批量策略；
  这两项会扩大持久布局或原回收控制流，不属于本轮缺陷的最小修复。
- 验证：shared-pool、allocator、epoch、node-control、Masstree 五项专项通过；
  allocator 专项包含受控 ABA 对拍及 8 线程 16,000 次并发分配/释放。
