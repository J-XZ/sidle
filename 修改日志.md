# D-SIDLE 修改日志

## 2026-07-24 — M7 检查点：08/09 的 100k 跨进程数据面套件

- 新增 `dsidle_e2e_suite_runner` 与 `--suite 08|09` 编排：08 使用固定 8B key/8B
  value，09 使用固定 32B key/1000B value；两者均填充 100,000 条记录。
- 由于 Masstree writer version lock 不支持多个进程并发构建同一棵树，填充阶段由
  node 0 串行执行；所有四个进程仍通过共享 phase barrier 参与，随后跨节点分片读回
  每条记录、node 1 删除每十条、全节点验证 delete miss，并由 node 3 扫描验证余下
  90,000 条。每阶段输出时间与明确的 verify 成功标记。
- 新增 4 VM 08/09 配置及 CTest smoke；其中 09 为了容纳真实 100k×1KB 值使用
  8 GiB 稀疏共享映射。YCSB trace CTest 的断言改为跨所有 worker 文件查找 GET/PUT，
  避免随机工作分片使 worker0 偶尔只含一种操作而产生伪失败。
- 验证：RelWithDebInfo `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`
  （19/19 通过）；六个脚本 `shellcheck`（通过）。

## 2026-07-24 — M7 YCSB 两相位 rounds suite

- `run_dsidle_e2e_rounds.sh` 增加唯一 `--suite` 接口和 `ycsb` 两相位编排：每轮只初始化一次池，load phase 仅首节点 bootstrap，workloada phase 全节点 attach 已发布根；load/run 配置必须拥有相同共享池拓扑。补充套件默认 3 轮，主套件默认 10 轮。
- 验证：原 2/4 节点 rounds CTest 通过；两节点 YCSB suite smoke 验证 load 与 workloada 两相位统计行通过。

## 2026-07-24 — M6/M7 综合回归

- 验证：RelWithDebInfo 全量构建；`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`，16/16 通过，覆盖 trace/YCSB、2/4 节点 rounds、VM dry-run、配置与延迟模拟。
- 未将此回归视为真实 QEMU 或 10 轮 e2e 验收；镜像构建仍需在持久终端中完成。

## 2026-07-24 — M7 Bash 静态检查

- 安装 shellcheck 0.8.0-2，并检查 `dsidle_make_vm_img.sh`、`dsidle_init_vms.sh`、`dsidle_kill_vms.sh`、`dsidle_check_vms.sh`、`dsidle_run_ycsb_experiment.sh` 和 `scripts/run_dsidle_e2e_rounds.sh`。
- 验证：shellcheck 零诊断。

## 2026-07-24 — M7 独立 VM 镜像配置

- 新增本仓 `image/mkosi.default` 与 post-install 脚本，定义可引导 Ubuntu jammy raw `root.img`、SSH 和 guest 构建/NUMA 依赖；镜像脚本可从配置注入 `vm.local_ssh_pub_key`，并支持 `--force` 交由 mkosi 覆盖输出。
- 验证：`bash -n`、镜像脚本 dry-run，以及 `mkosi -C image summary`；摘要确认 Ubuntu、`gpt_ext4`、`root.img`、Bootable、包集和 post-install 脚本。Debian bookworm 首次实际构建因宿主 archive keyring 无法验证 Release 签名而在安装前失败，未保留镜像；Ubuntu jammy 已通过签名和包下载阶段，但当前受控终端会在长构建会话结束时清理子进程，未形成 `root.img`，须在持久终端重跑。

## 2026-07-24 — M7 YCSB 多轮汇总字段

- 汇总器补齐 `avg_ops_sum`、`avg_duration_sec` 和基于它们的 `ops_per_sec`，CSV/Markdown 与 JSON 同步输出，避免用单轮字段隐含多轮平均。
- 验证：Python 语法检查与 YCSB 汇总 CTest 通过。

## 2026-07-24 — M6 后台副本延迟作用域

- replica worker 的 trigger、promotion/demotion executor 与 cooler 批次均增加 `ScopeGuard(kMerge)`；Trigger/Cool 的析构在 RCU stop 后发生。
- 验证：`merge_enabled=false` 时后台 scope 不产生 delayed_ns；延迟和单进程 Masstree CTest（2/2 通过）。

## 2026-07-24 — M7 配置全字段合同

- 配置解析增加所有对象层级的必填/未知键 hard fail，覆盖根、共享池、VM、host CPU、e2e、D-SIDLE 与 latency 全字段；新增未知根键拒绝测试。
- 验证：config、runner 冒烟和四节点 rounds CTest（3/3 通过）。

## 2026-07-24 — M6 TSC 注入精度与降级保护

- 启用延迟注入时要求 TSC 校准成功；移除 `sleep_for` 降级，避免在非 TSC 路径将睡眠误报为性能注入。增加测试专用 TSC 自旋入口。
- 验证：100 次 1000ns 忙等取最小样本，误差在 ±20%；仅 SWCC 与仅 HWCC profile 各自只有对应池的 delayed_ns 非零；RelWithDebInfo 延迟 CTest 通过。

## 2026-07-24 — M7 四节点 runner 冒烟

- 新增 4 VM 语义的 4 进程共享池 fixture 与 load/GET/PUT/DELETE/SCAN worker trace；统一 rounds 脚本通过单轮 `--smoke` 回放并验证 node 3 统计行。fixture 将 SWCC 设为 224MiB，以满足四 shard allocator 的容量下限。
- 这是 host 多进程冒烟，非 QEMU 4 VM 验收。

## 2026-07-24 — M7 YCSB phase 配置绑定

- YCSB 准备脚本为 load 与每个所选 workload 物化独立 `experiment_config_ycsb_<phase>.jsonc`，将 `dsidle.trace_dir` 精确绑定到该 phase 的 worker 目录，并在 `run_meta.json` 记录映射。
- 验证：YCSB trace 生成 CTest 断言 A phase 配置的 trace 路径及 GET/PUT trace 展开，通过。

## 2026-07-24 — M7 VM 启动与只读检查路径

- `dsidle_init_vms.sh --execute` 现实现共享 backing 预故障/池初始化、每 VM 独立 raw 磁盘、NUMA 绑定 QEMU、ivshmem、pidfile 与 taskset；严格预检布局、NUMA、CPU 集合和可用内存。所有破坏性步骤仍要求显式 `--execute`。
- `dsidle_check_vms.sh` 实际模式验证 pid/QEMU cmdline、ivshmem backing、CPU 亲和性、SSH guest 设备和 `numa_maps`；dry-run 只输出检查计划。
- 验证：四脚本 `bash -n`；默认配置 init/check/make 的 dry-run 通过；新增 CTest 覆盖 VM 脚本 dry-run。

## 2026-07-24 — M7 统一 e2e rounds 编排

- 新增 `scripts/run_dsidle_e2e_rounds.sh`：默认最低 10 轮；仅显式 `--smoke` 可运行开发单轮。实际路径要求 `--execute`，每轮重置精确 backing、初始化池、并发启动所有节点并写入节点日志、配置 SHA、git SHA 与退出状态；dry-run 只打印命令。
- 验证：两节点共享池、单轮 smoke CTest 通过，两个节点均输出 `E2E_TRACE_TIME_US`。

## 2026-07-24 — M7 YCSB trace 生成接入

- `--prepare-only` 现调用本仓固定子模块生成 load 和所选 A–E 的 CXLKV 格式 trace，验证 4×1 worker 的 load/A/E 文件集合和 A 的 `GET`+`PUT` 展开；新增 CTest 覆盖该路径。初始化时需 `git submodule update --init --recursive` 以取得 YCSB-cpp 的嵌套直方图依赖。
- VM 回放仍未实现，普通执行在 trace 生成后明确失败。

## 2026-07-24 — M7 YCSB-cpp 依赖固定

- 按计划 SHA 初始化本仓 `third_party/YCSB-cpp` 子模块，来源为公开 `cxlkv_trace_ycsb_5m_20260621` 分支；生成器将由本仓脚本调用，未引用兄弟仓运行时资源。

## 2026-07-24 — M7 YCSB 参数矩阵复核

- 复核 `f`、`workloada`、大写、空项、重复项和 `load` 等非法 workload：均以失败退出，且不创建请求的输出目录。
- 验证：RelWithDebInfo `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`，12/12 通过。当前环境未安装 `shellcheck`，故该外部静态检查尚未执行。

## 2026-07-24 — M7 YCSB 准备指南

- 新增 `YCSB指南.md`，仅记录已执行的帮助、准备和汇总命令，并明确 YCSB-cpp 子模块/实际 VM 回放尚未就绪，避免将准备产物误述为实验结果。

## 2026-07-24 — M7 YCSB 编排准备入口

- 新增 `dsidle_run_ycsb_experiment.sh` 的严格参数和 workload 矩阵校验，以及 `--prepare-only` 可复现元数据输出；实际 trace 生成和 VM replay 尚未启用。
- 验证：脚本 `bash -n`；YCSB 汇总器和准备模式 CTest（2/2 通过）。

## 2026-07-24 — M7 YCSB 配置物化

- 准备入口补齐计划中的超时、共享 NUMA/大小/预留、缓存冲刷及跳过阶段选项；输出派生的四 VM 配置、配置 SHA256、git SHA 和完整复现命令。派生配置固定 1024MiB HWCC、其余 SWCC，并强制关闭模拟 cache-hit 模型。
- 验证：`--prepare-only` 以显式 NUMA、4096MiB、`--no-latency` 和全部跳过开关运行；Python 断言配置布局、摘要和延迟字段；准备模式 CTest 通过。

## 2026-07-24 — M6 延迟模拟器缓存语义

- 补全 CXLKV `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` 对应的 4ms 一次性 TSC 校准、作用域末尾批量 busy-spin、固定命中率和每线程组相联 LRU。
- 保留 Sidle 的 SWCC/HWCC 插桩封装，并在 `搬运清单.md` 记录来源、版本和差异。
- 验证：RelWithDebInfo 全量构建；`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`，8/8 通过。

## 2026-07-24 — M6 SWCC 分配与值路径插桩

- 在 shard allocator 的自由链读写、零初始化，以及 `leafvalue` 的值读取/发布处使用统一的 SWCC wrapper 记账。
- 验证：RelWithDebInfo 构建；allocator 与单进程 Masstree CTest（2/2 通过）。

## 2026-07-24 — M6 缓存模型验证

- 延迟模拟器测试增加固定命中率和每线程组相联 LRU 用例，验证 raw/hit/miss/delayed 的恒等关系与命中附加延迟。
- 验证：`dsidle_latency_simulator_tests` 通过。

## 2026-07-24 — M6 CXLKV 注入对比档

- 新增 `configs/latency/cxlkv_e2e11_reference.jsonc`：25ns SWCC、117ns HWCC、每线程 LRU；该档仅用于注入对比，默认 `experiment_config.jsonc` 仍保持全零、关闭延迟。
- 验证：配置解析测试使用该文件通过。

## 2026-07-24 — M6 HWCC 控制面插桩

- RootControl、epoch 时钟/槽和 NodeControl 的插入、分裂标记均经 HWCC 原子薄封装记账。
- 验证：epoch、node-control 与单进程 Masstree CTest（3/3 通过）。

## 2026-07-24 — M6 作用域与统计输出接口

- 增加 `ScopeGuard`，供 runner 在一个逻辑操作完成且退出锁/RCU 后统一补齐延迟；增加 cxlkv 同字段的 `LATENCY_SIM_STATS` 输出和重置接口。
- 验证：延迟模拟器测试覆盖作用域守卫和统计行，CTest 通过。

## 2026-07-24 — M7 trace runner 初版

- 新增 `dsidle_e2e_trace_runner`：严格解析 cxlkv trace 行、定长 key/随机定长 value、PUT/GET/DELETE/SCAN 回放、每秒心跳、相位统计与延迟统计输出。
- runner 通过 `--bootstrap` 创建共享根，否则从 RootControl attach；多 VM 相位屏障和 VM 启动编排仍待后续阶段实现。
- 验证：RelWithDebInfo 目标构建成功；`--help` 参数契约可执行。

## 2026-07-24 — M7 trace runner 共享池冒烟

- 增加最小 128MiB 共享池配置与 PUT/GET/SCAN/DELETE trace；CTest 先精确创建 backing、初始化池，再以 `--bootstrap` 回放。
- 验证：`dsidle_e2e_trace_runner_smoke` 通过，并实际输出 `E2E_TRACE_TIME_US` 与 `DSIDLE_MEMORY_STATS`。

## 2026-07-24 — M7 ivshmem 相位屏障

- 在共享池诊断区增加可重用 HWCC phase barrier；bootstrap 节点先发布根，所有 VM 经过屏障后非 bootstrap 节点才从 RootControl attach。屏障在 replay 计时之前。
- 验证：跨 `fork` attach 的 epoch 测试覆盖两参与者屏障；runner smoke 通过。

## 2026-07-24 — M7 VM 脚本 dry-run/preflight

- 新增四个根目录 VM 脚本：镜像、初始化、精确 pid 停止和只读检查；所有会改变 VM/backing 的路径均要求 `--execute`，当前初始化脚本先交付配置 layout/CPU 预检和 QEMU dry-run。
- 验证：四脚本 `bash -n`，以及默认配置的所有 dry-run 通过。

## 2026-07-24 — M6 规范节点解引用记账

- `ResolveCanonicalNode` 在 canonical NodeRef 解引用时记录首个 SWCC cacheline 读取，覆盖规范节点遍历的统一入口。
- 验证：node-control 与单进程 Masstree CTest（2/2 通过）。

## 2026-07-24 — M6 延迟注入构建保护

- runner 在 Debug 构建中拒绝 `latency_inject.enabled=true`，防止将调试构建误用于延迟性能数据。
- 验证：RelWithDebInfo runner smoke 通过。

## 2026-07-24 — M7 两 VM runner 冒烟

- 新增两个独立 runner 进程共享同一池的 CTest；验证 bootstrap 根发布、phase barrier 与另一 VM 的 RootControl attach，两个节点都输出 `E2E_TRACE_TIME_US`。
- 验证：`dsidle_e2e_trace_runner_2vm_smoke` 通过。

## 2026-07-24 — M7 YCSB 汇总器

- 新增 `scripts/summarize_ycsb_experiment.py`，从 runner 的 `E2E_TRACE_TIME_US` 行生成 JSON、CSV 和 Markdown；吞吐以每轮节点最大耗时计算。
- 验证：Python 语法编译和双节点日志 fixture CTest 通过。

## 2026-07-24 — M6 延迟插入审计

- 新增 `延迟插入审计报告.md`，列出 wrapper、接入点、不计费的 process-local 数据与仍需逐字段扩展的边界。

基线：`1f1fa8ae9459935963a67a9beef6f30683c3c47b`（final release）。

本日志从 M0 开始维护；每个里程碑和架构检查点均记录提交、测试与差异。

## 2026-07-24 — M5 架构检查点：根副本跟随

- 代码检查点：`19e4e8bd71bcc4edb520cab9fac54e49dc253be7`。
- 新增每 VM `root_replica_pin`：从 RootControl 读取当前 root ref/version，按根
  类型 promote leaf 或 internode 副本；RootControl 变动时释放旧根槽并钉住新根。
  即使 root ref 未变，本地副本被写后失效也会按当前 version 重建。根 pin 不改
  规范 parent/link，也不持有跨进程虚拟地址。
- 测试在结构性并发插入前后刷新 pin，并断言其 ref 始终等于 RootControl 当前 ref。
- 验证：RelWithDebInfo replica-directory 与 Masstree 专项 CTest（2/2 通过）。

## 2026-07-24 — M5 架构检查点：内部节点副本命中

- 代码检查点：`303ca3d382ac9c2e95eb42edeeb06c7f1290be0d`。
- 新增紧凑 internode 副本（排序 ikey + child NodeRef），按 stable-copy/二次验版
  发布为 `kInternal`。`reach_leaf` 在 `{generation,version}` 匹配时从本地副本
  计算下一 child NodeRef，随后保持原 canonical 版本双检；不缓存子树，也不把
  本地地址写入规范树。
- 测试发布真实根副本、验证 child NodeRef 对拍，并执行查找断言内部副本命中计数
  递增；leaf 副本和值副本专项继续通过。
- 验证：RelWithDebInfo 的 replica-directory 与 Masstree 专项 CTest（2/2
  通过）。

## 2026-07-24 — M5 架构检查点：本 VM 写后副本失效

- 代码检查点：`34e2f8316b38680a0d6510187060ea937136d4a7`。
- `run_put`、`run_replace`、`run_remove` 在原有叶版本发布完成后，仅失效当前
  进程的 ReplicaDirectory 槽并在本地 reader 离开后释放旧缓冲；不会跨 VM 修改
  其他目录。远端写仍通过 canonical generation/version 失配惰性失效。
- 测试在实际副本命中后执行本地 Replace，断言旧 `{generation,version}` 无法再
  acquire，随后继续以 `std::map` 对拍。
- 验证：RelWithDebInfo 的 replica-directory 与 Masstree 专项 CTest（2/2
  通过）。

## 2026-07-24 — M5 架构检查点：Get 副本命中双检

- 代码检查点：`348c46c8713ca1aeef28a375c95039c359be1947`。
- `unlocked_tcursor` 在完整 epoch scope 内按 canonical `{NodeRef,generation,
  version}` acquire 本地副本，执行叶查找后再次验证 canonical leaf 未变化；只有
  验版成功才返回复制的 row。目录 `ReadHandle` 随 cursor 存活到 query 调用者已
  取/拷贝 value，失效不会在窗口内释放 `local_ptr`。layer 副本按 NodeRef 回到
  下层规范树。测试直接断言 query cursor 命中副本。
- `basic_table::get` 因其 ABI 返回裸 value 指针而无法延续读句柄，明确保留规范
  SWCC 路径，避免 UAF。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build -R
  'dsidle_(replica_directory|masstree_single_process)_tests' --output-on-failure`
  （2/2 通过）。

## 2026-07-24 — M5 架构检查点：稳定 leaf promote

- 代码检查点：`c86dd1b3c5baba3f31033ab8f5125c6dade9e569`。
- `leaf_replica::Promote` 实现 canonical leaf 的 stable-copy protocol：取得
  `{NodeRef,generation,version}` 快照，构造自包含副本，二次验版后才以
  `{generation,cached_version,bytes,kind}` 发布到本地 ReplicaDirectory；版本
  变化则丢弃副本。替换旧缓冲仅在目录读者退出后发生。
- 单测从真实 leaf promote，按相同 generation/version acquire，使用本地 row
  bytes 查值，并安全 invalidate。Get 热路径命中尚待接入。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build -R
  'dsidle_(replica_directory|masstree_single_process)_tests' --output-on-failure`
  （2/2 通过）。

## 2026-07-24 — M5 架构检查点：自包含叶副本编码

- 代码检查点：`34f955484e835493c13d1aca3ad1644de608549f`。
- 新增 `masstree_replica.hh` 的进程本地 leaf 编码：按稳定 permutation 复制 ikey、
  ksuf 字节和每个普通 value 的完整 row bytes；layer 条目只保留 layer root 的
  NodeRef，绝不复制虚拟地址或把 layer 当 value。Lookup 从该本地缓冲返回 copied
  row / layer NodeRef，不再解引用 SWCC ValueRef/ksuf。
- Masstree 测试从真实 canonical leaf 创建编码并验证 copied row 的内容。该缓冲
  尚未由后台 promote 发布到 ReplicaDirectory，也尚未接入 Get 命中。
- 验证：RelWithDebInfo 重编并运行 `dsidle_masstree_single_process_tests`（1/1
  通过）。

## 2026-07-24 — M4 完成：SMO、Scan 与安全回收

- 组成检查点：`85ce90a`（scan 对拍）、`42387e8`（Scan∥Delete）、`ae5f1b3`
  （同步双进程高冲突 split）、`b3d1a75`（共享 epoch/owner-shard limbo）、
  `8872b8e`（NodeControl 成对回收）、`77166a4`（线程退出 drain）、
  `93743a3`（叶链 SWCC 发布）、`25dc071`（reclaim 状态验证）。
- 退出条件证据：高冲突 SMO 由两个独立 attach worker 的同步 64+64 插入并逐项
  GET/完整 Scan 对拍；Scan∥Delete 在扫描叶链期间验证 epoch ACTIVE；节点体与
  控制项的 RCU 生命周期、owner-shard remote-free 和 worker limbo drain 均有
  状态断言。长共同前缀键覆盖 layer 路径。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（7/7 通过）。后续进入 M5 副本和策略。

## 2026-07-24 — M5 架构检查点：本 VM access_count 绑定

- 代码检查点：`3683ec8e04601859143ee7d9b0213aa29ea686af`。
- ReplicaDirectory 可绑定到当前进程线程；Masstree 的 root/internal 遍历按
  NodeControl slab index 更新本 VM 的稠密 `access_count`，不使用 hash map。
  NodeControl 从 free 链重新 Reserve 时，目录会失效该 NodeRef 的旧副本并将
  counter 清零，防止 generation 复用沿用旧热点。
- 验证：重编 Masstree 测试并运行 `ctest --test-dir /tmp/dsidle-m1-build -R
  'dsidle_(replica_directory|masstree_single_process)_tests' --output-on-failure`
  （2/2 通过）。副本复制/命中和 SIDLE 五 worker 尚待接入。

## 2026-07-24 — M5 架构检查点：分段副本目录

- 代码检查点：`d2e3d51137b2ff71e191e73bad1d528c974a12e1`。
- 新增进程本地 `ReplicaDirectory`：按 NodeControl slab index、每段 4096 槽
  惰性分配；slot 含 seqlock、读者计数、generation/version、bytes/kind 与
  `local_ptr`。读者在取得指针前先登记，发布/失效在 odd seq 下等待本地读者
  离开后才交还旧缓冲，避免目录 `local_ptr` UAF；目录不把虚拟地址写回共享池。
- 新增单测覆盖发布、generation/version 失配、稳定读句柄、替换与失效。该
  检查点尚未把副本复制/命中接入 Masstree 或 SIDLE worker。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R dsidle_replica_directory_tests --output-on-failure`
  （1/1 通过）。

## 2026-07-24 — M4 测试检查点：节点 epoch reclaim

- 代码检查点：`25dc071e816fa3f0448e766d98ec9b94c71fe8c6`。
- Masstree 专项直接构造一个 canonical leaf 并沿真实 `deallocate_rcu` 路径回收；
  断言其 NodeControl 在 drain 前为 RETIRING、drain 后为 FREE，证明 SWCC body
  与控制项不会在 RCU 安全点之前复用。
- 验证：RelWithDebInfo 的 `dsidle_masstree_single_process_tests` CTest（1/1
  通过）。

## 2026-07-24 — M4 架构检查点：叶链 SWCC 发布顺序

- 代码检查点：`93743a32c2e8e7de17b323ba51aaee580d1b190a`。
- `btree_leaflink` 的并发版本现在对 `next_/prev_` 逐字段执行读前 invalidate、
  普通 store 后 flush/fence。split/change/unlink 的原有锁和操作顺序保持不变，
  没有在 SWCC 链字上恢复 CAS/RMW；节点版本解锁仍是最终 HWCC 发布点。
- 验证：RelWithDebInfo 重编 `dsidle_masstree_single_process_tests`，并运行同名
  CTest（1/1 通过，覆盖同步双进程 split 与 Scan∥Delete）。

## 2026-07-24 — M4 测试检查点：跨进程高冲突 split

- 代码检查点：`ae5f1b323d92840a47018007226e190b7ddc03d1`。
- Masstree 跨进程测试新增同步启动的两个 attach worker；每侧插入 64 个不重叠
  key，均超过叶容量并与另一侧重叠执行 split/root 发布。worker 退出前 drain
  自身 limbo；父进程随后逐项 GET，并由既有完整 Scan 的 `std::map` 对拍覆盖
  两侧插入集。
- 验证：`cmake --build /tmp/dsidle-m1-build --target
  dsidle_masstree_single_process_tests --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R dsidle_masstree_single_process_tests --output-on-failure`
  （1/1 通过）。

## 2026-07-24 — M4 架构检查点：线程退出 limbo drain

- 代码检查点：`77166a46f0ab9fdaa622932d0eff11e20ac13a92`。
- `threadinfo::rcu_drain()` 实现 worker join 前的唯一回收路径：撤销自身 epoch
  槽、反复推进/回收 limbo，直至本线程队列为空；30 秒内无法完成则 hard fail，
  不会把 limbo 悄然移交给其他线程。结束时再次写 INACTIVE。
- Masstree 跨进程测试在原映射关闭和重挂载结束前显式 drain 两个本地 threadinfo，
  并验证共享槽最小值回到 INACTIVE。
- 验证：`cmake --build /tmp/dsidle-m1-build --target
  dsidle_masstree_single_process_tests --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R dsidle_masstree_single_process_tests --output-on-failure`
  （1/1 通过）。

## 2026-07-24 — M4 架构检查点：NodeControl 成对 retire/reclaim

- 代码检查点：`8872b8eda8b3e08988f30a5689a6372e3c0618b8`。
- leaf/internode 摘树后进入 limbo 时，配对 NodeControl 立即转为 RETIRING 并
  记录相同 retire epoch。limbo 条目额外保存 NodeRef；仅在该节点 SWCC 体已按
  owner shard 投递 remote-free 后，才将控制项 Release 回 HWCC slab，避免
  NodeRef 在规范节点体仍受 RCU 保护时复用。
- 验证：`ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`（6/6 通过）。
  M4 仍缺线程退出前的 limbo drain 与高冲突 SMO/reclaim 压力测试。

## 2026-07-24 — M4 架构检查点：共享 epoch RCU 与 owner-shard limbo

- 代码检查点：`b3d1a75d76faa7d49606ed28009f191579c5bba3`。
- HWCC 诊断区首行现保存共享单调 epoch 时钟；Masstree 的 Get、Put、Delete
  与正/反向 Scan 均在完整 API 范围内发布/撤销对应 VM×thread epoch 槽。每个
  threadinfo 在第 50 个操作退出后才推进时钟并扫描全局最小活跃 epoch，避免把
  min 扫描放到每次读写热路径。
- limbo 的池对象记录现为 SWCC offset + tag/class + owner shard；回收时按 offset
  反查原分配 shard，并在 epoch 安全后投递到该 shard 的 remote-free 链。分配器
  专项覆盖跨 shard owner 识别与回收复用；Scan∥Delete 子进程确认父扫描的 epoch
  槽在整段叶链扫描期间保持 ACTIVE。
- 验证：`cmake --build /tmp/dsidle-m1-build --target
  dsidle_masstree_single_process_tests --parallel && ctest --test-dir
  /tmp/dsidle-m1-build -R 'dsidle_(epoch|shard_allocator|masstree_single_process)_tests'
  --output-on-failure`（3/3 通过）。M4 仍缺高冲突 SMO、成对 NodeControl retire/
  reclaim 与线程退出 limbo 排空，尚未完成。

## 2026-07-24 — M0：回滚与 D-SIDLE 骨架

- 代码检查点：`7c8f4b0378e655df3acb6bb6d26e1544eca98820`。
- 已建立 `dsidle/` 配置骨架、根 `experiment_config.jsonc` 与 CTest 配置契约；
  根 CMake 仅构建 D-SIDLE 骨架，已从构建图摘除基线 DAX/memkind、ART 与旧 benchmark。
- 验证：`cmake -S . -B /tmp/dsidle-m0-clang-build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build /tmp/dsidle-m0-clang-build --parallel && ctest --test-dir /tmp/dsidle-m0-clang-build --output-on-failure`（1/1 通过）。
- 环境说明：验收配置使用 Clang 18；默认 GCC 11 不支持合同要求的 `-flto=full`，因此 CMake 明确要求 Clang，未降低优化或 LTO 级别。

## 2026-07-24 — M1 架构检查点：共享池首次 attach

- 代码检查点：`fe3da1cd064d64fa0a24519aee0744aa5f5d6e04`。
- 新增双区域 `SharedPool`：PoolHeader/RootControl 固定于 HWCC 起点，布局仅允许 `hwcc.offset=0`、相接 SWCC，并在 create/attach 时校验 ABI、容量和跨进程 64 位 lock-free 原子。
- 新增 64B 对齐的 `NodeControl`/`RootControl` 与 64 位带类型 offset 基元；未接入基线 Masstree 前不改动其行为。
- 验证：`cmake -S . -B /tmp/dsidle-m1-build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir /tmp/dsidle-m1-build --output-on-failure`（2/2 通过；共享池测试含 fork 后 attach、SWCC 载荷与 HWCC 根版本可见性）。

## 2026-07-24 — M1 架构检查点：SWCC shard 分配器

- 代码检查点：`f74df50d344d6eed51379eab5a2e27d7dbe70e7d`。
- 已实现固定 size-class 的每 VM SWCC shard bump 区、HWCC local/remote-free 栈头及 offset 空闲链；remote-free 发布会逐行 flush 并 fence，单次分配最多收割 64 个对象。
- 验证：RelWithDebInfo 构建与 CTest（3/3 通过）。

## 2026-07-24 — M1：池与分配器完成

- 代码检查点：`aa41e30f51189bc67f74a1dff7a24b074f1c928a`。
- 新增 `dsidle_shared_pool --init-pool`：对已由宿主创建/清零的 backing 写入
  PoolHeader、RootControl、NodeControl slab、每 VM shard 控制区、共享 epoch 槽和
  诊断区；按配置打印 HWCC 预算并在容量不足时 hard fail。该路径只改固定元数据，
  不会误触完整 SWCC 区而重复预填充。
- NodeControl slab 使用 HWCC 空闲链，发布前写 canonical SWCC offset、递增
  generation，且要求 RETIRING 后才可归还；shard allocator 改为使用该固定布局。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（4/4 通过）；其中 shard 专项由子进程
  attach 后 remote-free，父进程 harvest 并复用同一 SWCC offset。另以 16MiB
  临时 layout 执行 `dsidle_shared_pool --init-pool --node-control-capacity 64
  --max-threads-per-vm 2`，成功输出 HWCC 预算。

## 2026-07-24 — M2 架构检查点：HWCC 版本与根访问器

- 代码检查点：`49ceccec1286abc21b296610455b2fdeb44ea760`。
- `NodeVersionAccessor` 将基线 32-bit Masstree nodeversion 的锁、插入、分裂、
  删除、根和 migration 位布局原样映射到 `NodeControl.version_and_state`；稳定
  读取同时取得 generation 与 canonical SWCC offset。新增 `RootControlAccessor`
  执行版本化 root-ref/root-generation 发布与双检读取。
- CTest target 均显式以 `-UNDEBUG` 编译，使 RelWithDebInfo 下的断言真实执行；
  新增 NodeControl/RootControl 单测，覆盖 slab generation 复用、root 发布以及
  插入/分裂版本跃迁。
- 验证：`cmake --build /tmp/dsidle-m1-build --parallel && ctest --test-dir
  /tmp/dsidle-m1-build --output-on-failure`（5/5 通过）。

## 2026-07-24 — M2 架构检查点：按进程解析共享 offset

- 代码检查点：`a07a3d78765bcd155dd080811c121f0d0e0db08f`。
- SharedPool create/attach 现在登记本进程（线程）的映射基址；该基址不进入共享
  backing，仅为后续 Masstree `NodeRef`/`SwccOffset` 解引用提供不同基址安全的
  本地解析点。close 会清除对应登记，未 attach 即解析会 hard fail。
- 验证：RelWithDebInfo 构建与 CTest（5/5 通过）。

## 2026-07-24 — M2 架构检查点：SWCC size-class slab

- 代码检查点：`fc9030281ab5b8ddb4f615d585b05f8d997fdd0f`。
- shard 分配器扩展为 64B 到 2MiB 的 16 个 2 的幂 size class；每 VM、每 class
  有独立 HWCC 控制行与 SWCC bump 区/本地 free 链/remote-free 链。`SwccShardAllocator`
  按对象大小选择 class，保持对象引用为 offset；生产 `--init-pool` 初始化全部
  class，单 class 初始化仅保留给专项测试。
- 验证：RelWithDebInfo CTest（5/5 通过）；分配器专项覆盖 97B、17KB、900KB
  三类分配，以及 remote-free harvest 后按原 offset 复用。

## 2026-07-24 — M2 架构检查点：原生 Masstree 接入 SWCC

- 代码检查点：`3dd6c5530e90a154ea507be5c20eaaed4dbdbd66`。
- 新增 `dsidle_masstree`，直接编译仓库内的 Masstree 源码；旧 CXL/memkind
  allocator 实现不进入构建图。运行时将当前进程绑定到一个 SWCC shard，Masstree
  canonical node、value 和 key-suffix 分配均经 D-SIDLE SWCC size-class 分配器，
  不存在 DAX/memkind 回退。SIDLE 前端的 canonical 分配也固定为 SWCC，策略选择
  留待 M5 用于副本。
- 新增原生 Masstree 单进程 PUT/GET CTest，验证其在共享池上完成创建、插入和读取。
  此检查点仍保留基线的进程内裸指针字段及临时本地 RCU epoch 兼容层；NodeRef
  偏移化、NodeControl 实际接管 nodeversion 和跨进程重挂载尚未完成，不能视作 M2
  出口。
- 验证：RelWithDebInfo CTest（6/6 通过）。

## 2026-07-24 — M2 测试检查点：Masstree 单进程对拍

- 代码检查点：`2dd49a36af6fdebf7c3c82e7cd4623b3b30c26d2`。
- 原生 Masstree 测试扩大为 64 个固定 8B key 的乱序写入、覆盖写及逐项 GET，
  使用 `std::map` 保存期望值；该规模会经过叶/内部节点分裂，验证值内容而非
  基线 `run_replace` 的返回枚举。SWCC OOM 诊断现在带 block、offset 和 limit，
  便于定位实际 shard/class 耗尽。
- 验证：RelWithDebInfo CTest（6/6 通过）。仍缺 offset/NodeRef 重写与重挂载，
  因此未达到 M2 退出条件。

## 2026-07-24 — M2 架构检查点：NodeControl 发布序

- 代码检查点：`9ca7a8b733850d6b0ce05a3379a7fd78a756b26c`。
- NodeControl slab 不再把刚取得的槽直接标成 PUBLISHED。调用方必须先 `Reserve`
  （ALLOCATING，写 canonical offset / generation）、初始化并 flush 配对 SWCC
  节点，再以 `Publish(initial_version)` 进行 release 发布；`Retire` 后才允许
  `Release` 回空闲链。`NodeVersionAccessor` 拒绝读取非 PUBLISHED 槽。
- 单测覆盖 leaf 初始位、版本稳定读取、insert/split 位跃迁、retire/release 及槽
  generation 复用；RelWithDebInfo CTest（6/6 通过）。此为接入 Masstree 构造器的
  必要前置，尚未使其节点字段偏移化。

## 2026-07-24 — M2 架构检查点：Masstree 节点成对控制项

- 代码检查点：`6d91ff56c944ddae260c08b162561e9a945b45c1`。
- 原生 leaf/internode 工厂现在为每个 SWCC 节点体 reserve 配对 NodeControl，将
  `NodeRef` 写入节点自身，flush 节点体后以正确的 leaf/non-leaf 初始 version
  发布控制行。当前线程的 SharedPool 绑定成为这条成对分配路径的唯一来源。
- Masstree 单测验证根节点的 NodeRef 已解析至其实际 SWCC offset；RelWithDebInfo
  CTest（6/6 通过）。树边、root、value 和 ksuf 仍未改写为 offset，nodeversion
  也仍由基线字段承载，故本检查点不等同完成偏移化。

## 2026-07-24 — M2 架构检查点：NodeRef 根发布

- 代码检查点：`c0ce0dd4e84c00701864ee640916d180438ccf58`。
- `basic_table` 不再持有持久化 `root_` 裸指针，而保存 NodeRef；`root()` 仅在
  栈上解析 canonical SWCC 地址。初始化和 `fix_root()` 均以 NodeControl 的
  generation 发布 RootControl，scan/print/destroy 等原有直接 root 使用点改为
  瞬时解析。
- Masstree 单测验证 RootControl 的 `{NodeRef,generation}` 与根 NodeControl/SWCC
  offset 一致；RelWithDebInfo CTest（6/6 通过）。child/parent/leaf/value/ksuf
  及 nodeversion 仍待改造，M2 未完成。

## 2026-07-24 — M2 架构检查点：内部 child NodeRef

- 代码检查点：`d2bbd22786391662f6fd49e119068b393ff67134`。
- `internode::child_[]` 已由裸指针改为 8B NodeRef 链接；链接在 SWCC 中只保存
  HWCC control offset，读取时才解析瞬时 canonical 地址。查找、split、复制和
  统计保留原有控制流，仅在模板实参推导无法进行隐式解析的位置增加显式栈上转换。
- 验证：64-key 分裂的 Masstree `std::map` 对拍以及完整 RelWithDebInfo CTest
  （6/6 通过）。parent、leaf link、value/ksuf 和 nodeversion 尚待改造。

## 2026-07-24 — M2 架构检查点：parent NodeRef

- 代码检查点：`6d0612c81b8fa7e9fadca59f6fbc691fd7bb3edf`。
- leaf 与 internode 的 `parent_` 均改为 NodeRef 链接。基线 destroy RCU callback
  原本复用 parent 裸指针作为工作队列 next 字段，现同样复用 NodeRef，避免在销毁
  路径把瞬时虚拟地址回写到 SWCC。
- 验证：RelWithDebInfo CTest（6/6 通过）；child/parent 树边均经 NodeRef 解析，
  但 leaf link、value/ksuf 与 nodeversion 仍待改造。

## 2026-07-24 — M2 架构检查点：leaf link NodeRef

- 代码检查点：`18664f9b91963edbcf3551367c0f75936f90f8f0`。
- leaf 的 `next_`（低位保留基线标记语义）与 `prev_` 均已改为 NodeRef；split、
  remove、scan、print 与链回收路径只在使用点解析瞬时地址。SWCC 链字不再执行
  CAS：M2 的单进程锁前提下使用普通写，跨 VM 的持锁 flush/fence 发布协议留待
  M4 完整实现。
- 验证：RelWithDebInfo CTest（6/6 通过）。value/ksuf 和 nodeversion 仍待改造，
  M2 未完成。

## 2026-07-24 — M2 架构检查点：外部 ksuf SWCC offset

- 代码检查点：`ecb6d8051d83f5a3443b18185ef0e492e7f86699`。
- leaf 的外部 `stringbag` 指针 `ksuf_` 已换为 `SwccOffset<stringbag>`；分配、
  重新分配、读取、预取和 RCU 延迟释放都只在调用点解析瞬时地址。新增共同前缀的
  160B 长键对拍，覆盖 layer / 外部 suffix 容器的写入与读取。
- 验证：RelWithDebInfo CTest（6/6 通过）。leafvalue/value 与 nodeversion 仍待
  改造，M2 未完成。

## 2026-07-24 — M2 架构检查点：leafvalue offset

- 代码检查点：`215f7539679124fb8eeafddc1291670d0bece975`。
- `leafvalue` 的 64-bit 槽不再存 value/layer 虚拟地址：普通 value 解释为
  `SwccOffset<row_type>`，layer 解释为 NodeRef。`tcursor` 及 kvrow/log/checkpoint
  的更新路径改为读取临时指针、完成原有更新逻辑后以 `set_value` 写回 offset，
  消除了 `R*&` 对共享字段的裸指针回写。
- 验证：随机/分裂/覆盖写、长共同前缀键的单进程 `std::map` 对拍和完整
  RelWithDebInfo CTest（6/6 通过）。nodeversion 仍待实际迁入 NodeControl，M2
  未完成。

## 2026-07-24 — M2 架构检查点：nodeversion → NodeControl

- 代码检查点：`690a6a9632d0aac258049a04ea04c7063cc441bd`。
- 保留基线 nodeversion 的全部位布局、stable/lock/unlock/mark 接口与快照语义；
  绑定 NodeRef 的活节点将版本 load/store/CAS 重定向至配对 NodeControl 的
  `version_and_state` HWCC 原子。nodeversion 拷贝仍是栈上稳定快照，不持有第二个
  可写句柄；未绑定的 single-thread 值保持原本地行为。
- 验证：分裂、覆盖写、长共同前缀的 Masstree 对拍与完整 RelWithDebInfo CTest
  （6/6 通过）。M2 剩余重挂载验证与针对不同映射基址的端到端证明。

## 2026-07-24 — M2 完成：不同基址重挂载

- 代码检查点：`7eb683af6789d17a33011ddca0a8c25cb85ece89`。
- `basic_table::attach()` 从固定 RootControl 采用 `{NodeRef,generation}` 并验证
  对应 NodeControl。测试关闭初始映射、以匿名 guard 占住旧虚拟基址，再通过
  `AttachAt` 把同一池固定映射到另一个地址；新 table attach 后逐项读取完整
  `std::map` 期望集（含分裂、覆盖写与共同前缀长键）。
- 审计 root、child、parent、leaf link、ksuf、leafvalue 的持久化字段均不再为
  裸指针；nodeversion 活节点状态位位于 NodeControl。
- 验证：RelWithDebInfo CTest（6/6 通过）。至此满足 M2“单进程 std::map 对拍 +
  重挂载 + commit/log”退出条件；后续进入 M3 跨进程预填充树的 Get/覆盖 Put/Delete。

## 2026-07-24 — M3 检查点：跨进程预填充树读取

- 代码检查点：`3692216f8d7d371b3ef50cedd0a49e79a614b3f2`。
- Masstree 测试现 fork 子进程：子进程关闭继承映射、重新 attach backing、绑定
  SWCC shard、独立创建 threadinfo/table 并从 RootControl attach；随后读取父进程
  预填充的完整期望集（分裂、覆盖写、长共同前缀键）。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 的覆盖 Put/Delete 和 SWCC 写入
  可见性发布仍待实现。

## 2026-07-24 — M3 检查点：跨进程覆盖 PUT / DELETE

- 代码检查点：`a36c79b829806facb9ee0ce83ed39d849b66d133`。
- 子进程独立 attach 后覆盖既有 key，父进程验证新值；另一子进程删除既有 key，
  父进程验证后续 Get 不再命中。两者均在预填充树上执行，不引入 split。
- 新增 `swcc_visibility.h`：value 槽写回前 flush 新 value，nodeversion unlock
  在 HWCC release 发布前 flush 对应 canonical 节点的基线 512B 节点包络，确立
  `SWCC 写/flush → HWCC version release` 顺序。Delete 专项同时补齐测试翻译单元
  对原生 `masstree_remove.hh` 的依赖。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 仍缺更严格的多进程线性化/规范
  值契约验证，M4 前不执行 SMO。

## 2026-07-24 — M3 检查点：SWCC 读侧 invalidate

- 代码检查点：`cb1f8d32ab9f7363df15dca1d786e9aa08515d6d`。
- `NodeVersionAccessor` 驱动的 Masstree `stable()` 在 HWCC 稳定版读取后精确
  invalidate 该 canonical 节点的 512B 基线包络；leafvalue 读取 value 前先
  invalidate value 首行并据 size invalidate 完整对象。写侧已有 flush/fence 后
  才 release 版本，因此读路径按 `稳定版本 → invalidate → SWCC 读` 执行。
- 验证：RelWithDebInfo CTest（6/6 通过）。M3 仍需以并发用例验证线性化，不进入
  M4 的 SMO/scan/reclaim。

## 2026-07-24 — M3 检查点：并发跨进程覆盖写

- 代码检查点：`440c4500442c919207c1afeb36c759832bdb0c3a`。
- 两个子进程分别重新 attach、经 pipe 屏障同时对同一预填充 key 执行覆盖写；父
  进程等待两者结束后读取该 key，断言结果恰为两个完整获胜值之一。该专项保持在
  既有叶内更新，不触发 split，覆盖 NodeControl CAS 锁与 release 版本发布。
- 验证：RelWithDebInfo CTest（6/6 通过）。此为 M3 线性化基础验证；更高冲突
  SMO、scan 和回收仍属 M4。

## 2026-07-24 — M3 完成：跨进程预填充树操作

- 退出证据：子进程重新 attach 后读取预填充完整树；独立子进程覆盖 PUT、DELETE
  后父进程观察到新值/缺失；两个同步起跑子进程竞争同一既有 key 后父进程只读到
  一个完整获胜值。所有 M3 写均为既有 leaf 内更新，不触发 split。
- 协议证据：写路径为新 value flush、canonical node flush、HWCC nodeversion
  release；读路径为 stable HWCC version、SWCC invalidate、value invalidate、
  基线 Masstree 原有 has_changed 复检。引用均经 offset/NodeRef 解析。
- 验证：RelWithDebInfo CTest（6/6 通过）。后续进入 M4 的 SMO、叶链、Scan 与
  epoch/limbo 回收；M3 的预填充树范围不替代这些测试。

## 2026-07-24 — M4 检查点：跨叶 Scan 对拍

- 代码检查点：`85ce90a4a5979ff63287ac092789f3c3ab14136b`。
- 新增直接调用原生 `basic_table::scan` 的 collector，对删除后的完整树逐条收集
  key/value，并与 `std::map` 的有序结果严格对拍。该树已包含多次 split、layer/
  长 key、跨进程覆盖写和 DELETE，因此覆盖 leaf link NodeRef 行走与 value offset
  读取的跨叶路径。
- 验证：RelWithDebInfo CTest（6/6 通过）。并发 Scan∥Delete、SMO 发布顺序及
  epoch/limbo 回收仍待 M4 后续实现。

## 2026-07-24 — M4 检查点：跨进程 split 可达性

- 代码检查点：`54f3832429d039ff3ea2643f0e018aaa9fade2be`。
- 独立 attach 的子进程向既有共享树插入 48 个新 key，触发额外 leaf/internal
  split；父进程随后逐项 Get 所有新增值并对全树执行 Scan/`std::map` 对拍。
  该路径验证 NodeRef child/parent、RootControl 和 leaf link 在跨进程 SMO 后仍
  能正确解析并行走。
- 验证：RelWithDebInfo CTest（6/6 通过）。并发 Scan∥Delete 与 epoch/limbo
  回收仍待 M4 后续实现。

## 2026-07-24 — M4 检查点：跨进程 Scan ∥ Delete

- 代码检查点：`42387e8824f9c9f6cb74baf5fcf0d65d5d80eb7e`。
- 父进程的原生版本化 Scan 在首个 leaf 回调时经 pipe 放行子进程，子进程独立
  attach 并删除一个既有 split key；扫描在删除并发期间安全结束，等待子进程后
  父进程验证该 key 不再可读。测试刻意延长首叶回调以形成真实重叠窗口。
- 验证：RelWithDebInfo CTest（6/6 通过）。epoch/limbo 回收仍待实现。

## 2026-07-24 — M4 检查点：共享 epoch 槽

- 代码检查点：`565d8e8ba2ba380b5e2b4352aa7bc66605203ac7`。
- epoch 单测现创建共享池并 fork：子进程关闭继承映射、重新 attach 后将自身 HWCC
  epoch 槽设为 17；父进程的 SharedEpochTable 观察到最小活跃 epoch=17，离开后
  最小值恢复为 INACTIVE。该测试同时保留本地 EpochTable 的 INACTIVE 语义验证。
- 验证：RelWithDebInfo CTest（6/6 通过）。limbo 记录的 offset 化和按 epoch
  remote-free 回收仍待接入。

## 2026-07-24 — M4 检查点：limbo SWCC offset

- 代码检查点：`638b5eb1f183513c1b8db7d873c262dec9fe9d77`。
- kvthread limbo 对 memtag_pool_mask 的 canonical 对象不再记录 `void*`，而记录
  SWCC offset；`free_rcu` 仅在实际回收时解析 offset。基线本地 callback 对象
  （`memtag(-1)`）保持进程本地地址，不作为共享规范数据。report 路径按同一
  offset 比较，避免诊断时重新依赖虚拟地址。
- 验证：RelWithDebInfo CTest（6/6 通过）。共享 epoch 与 limbo 的 threadinfo
  生命周期绑定、owner-shard 批量 remote-free 仍待 M4 后续接入。

## 2026-07-24 — M5 检查点：副本预算与代际队列

- `ReplicaDirectory` 现在是每 VM 本地副本字节数的唯一记账点：预算化发布在替换
  时按净增预留，淘汰/失效按旧副本字节扣减；预算不足会拒绝发布且保持旧快照。
  leaf/internode 提升均走此入口，因此不再存在绕过 `replica_budget_mb` 的完整
  本地副本。
- 新增 `QueuedNodeRef{NodeRef,generation}` 以及 MoodyCamel `replica_queue`；D-SIDLE
  策略候选不再以跨 VM 无效的 canonical 虚拟地址作为载荷，执行前可据 generation
  丢弃复用控制项的陈旧任务。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_replica_directory_tests --parallel`
  后执行 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R dsidle_replica_directory_tests`
  （1/1 通过）。

## 2026-07-24 — M5 检查点：副本执行器

- 新增 Masstree 副本执行器：提升任务先校验 `NodeControl` 的 PUBLISHED 状态和
  generation，再从候选叶向祖先发布稳定副本，直到已有有效本地副本；淘汰仅失效
  本 VM 目录项且根不可淘汰。它不持有 canonical 锁，不改 parent/link，也绝不
  对 canonical 节点执行 RCU free。
- 单进程专项直接验证 generation 不匹配任务被拒绝、提升后可 acquire、淘汰后
  缓存消失；保留随后规范写会使副本失效的既有检查。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_masstree_single_process_tests --parallel`
  后执行 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  'dsidle_(replica_directory_tests|masstree_single_process_tests)'`（2/2 通过）。

## 2026-07-24 — M5 检查点：本地访问冷却

- 副本目录的 slab-index 稠密本地访问计数新增原子减半操作；SIDLE cooler 将据此
  保持原始 `access_time >>= 1` 行为，同时不会写入 canonical SWCC leaf 元数据。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_replica_directory_tests --parallel`
  后执行 `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  dsidle_replica_directory_tests`（1/1 通过）。

## 2026-07-24 — M5 检查点：副本五 worker

- 新增 D-SIDLE 的五角色 worker 组：trigger 按原 `basic_interval × 5` 遍历规范
  叶、从本 VM 稠密访问计数产生 generation-qualified 候选；promotion/demotion
  executor 仅执行本地副本动作；cooler 减半本地计数；adjuster 根据副本预算水位
  调整原阈值对象。root pin 在每轮 trigger 跟随 RootControl。
- 验证：相关 RelWithDebInfo 构建后 `ctest --test-dir /tmp/dsidle-m1-build
  --output-on-failure -R 'dsidle_(replica_directory_tests|masstree_single_process_tests)'`
  （2/2 通过）。

## 2026-07-24 — M5 检查点：MasstreeKV 副本运行时入口

- `MasstreeKV` 构造器改为显式接收 `SharedPool` 与 `replica_budget_mb`，并拥有本
  VM `ReplicaDirectory`；foreground `worker_enter` 绑定该目录。原
  `init_migration_worker` 现启动副本五 worker，旧
  `masstree_leaf_migration/internode_migration` 回调不再位于运行入口。
- 验证：`clang++-18 -std=c++17 -fsyntax-only -include
  third_party/masstree-beta/config.h -I. -Isrc -Ithird_party/masstree-beta
  -Ithird_party/sidle_utils -Ithird_party/art -x c++ src/kv/masstree.h` 通过（仅有
  既有第三方告警）。

## 2026-07-24 — M5 修正：worker 规范遍历

- 副本 worker 不再调用会在当前 offset 化树上卡住的旧 `masstree_leaf_traverse`；改为
  在 epoch 临界区内读取稳定 internode 子 `NodeRef`、二次验版后 DFS 到 canonical
  leaf。遍历仍只读规范树，并将候选操作限制在本地副本目录。
- 验证：RelWithDebInfo `dsidle_masstree_single_process_tests`（1/1 通过）。

## 2026-07-24 — M5 检查点：trigger 到副本执行器

- 单进程专项对 canonical leaf 注入本 VM 高访问计数，直接运行副本 trigger 与
  promotion executor，断言产生的 `QueuedNodeRef` 最终发布匹配 generation/version
  的本地副本；随后运行 cooler 并淘汰该副本。该路径不触及 canonical parent/link
  或 RCU。
- 验证：RelWithDebInfo 构建后执行
  `timeout 15s /tmp/dsidle-m1-build/dsidle_masstree_single_process_tests`（通过）。

## 2026-07-24 — M5 检查点：副本目录并发发布/淘汰

- 目录专项令 reader 在持有 `ReadHandle` 时持续读本地副本，同时 writer 连续 1,024
  次发布替换并立即释放被目录交还的旧缓冲；reader 只观察到完整已初始化内容。
  该用例覆盖 slot seqlock 与 reader count 对 publish/free 的 UAF 防护。
- 验证：RelWithDebInfo `dsidle_replica_directory_tests`（1/1 通过）。

## 2026-07-24 — M5 修正：跨 VM 副本惰性失效

- 覆盖写与删除在持叶锁时标记 canonical nodeversion 的插入位，因而 release unlock
  会发布新稳定版本；跨 VM 的旧副本无需被写者触碰，读路径的 canonical 版本双检
  即会拒绝它。
- 单进程中以两个独立 `ReplicaDirectory` 模拟 VM A/B：B 预先保留旧 self-contained
  leaf，A 写后仅失效自身槽；B 的旧槽仍存在，但 B 的 `unlocked_tcursor` 返回新值，
  而非旧副本行。
- 验证：RelWithDebInfo 构建后 `timeout 15s
  /tmp/dsidle-m1-build/dsidle_masstree_single_process_tests`（通过）。

## 2026-07-24 — M5 完成验证：基线策略对拍

- 按计划创建基线 `1f1fa8ae9459935963a67a9beef6f30683c3c47b` 的 detached
  worktree（`/tmp/dsidle-baseline-policy`），对完整
  `third_party/sidle_utils/sidle_worker.hh` 与 `sidle_policy.hh` 执行 `diff -u`；
  两者均无输出，即直方图、hot/warm/cold 分类、16 桶指数映射、水位、阈值调整、
  `queue_waiting_threshold=10`、cooler 和三态 adjuster 与基线字节级一致。
- 当前 M5 对这些选择结果的唯一变化是：候选载荷从旧虚拟地址迁移语义替换为
  `QueuedNodeRef{NodeRef,generation}` 的本地副本 promote/evict；其行为已由
  trigger→executor 专项覆盖。对拍 worktree 随后已移除。

## 2026-07-24 — M6 架构检查点：延迟模拟基础模块

- 从 cxlkv `984ad91a614ae65b57d0fe53ccc174bb6e962bcd` 的
  `src/utils/{include,src}/latency_simulator.*` 开始移植；本仓新增
  `dsidle/latency_simulator.{h,cc}` 并接入 `dsidle` 静态库。当前基础具备
  relaxed fast gate、4ms TSC 校准、嵌套 scope、忙等、行范围统计和配置校验。
- 此检查点尚未声称 M6 完成：fixed-hit-rate/per-thread-LRU、全字段配置与
  Masstree/allocator 插入点仍待后续提交。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle --parallel`（通过）。

## 2026-07-24 — M6 检查点：完整延迟配置 schema

- `ExperimentConfig` 现持有完整 `latency_sim::Config`，JSONC 解析器要求计划列出的
  全部延迟字段并解析 cache model；默认 `experiment_config.jsonc` 明确提供全零
  `--no-latency` 档。缺字段/非法 cache model 均 hard fail。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_config_tests --parallel` 后
  `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R dsidle_config_tests`
  （1/1 通过）。

## 2026-07-24 — M6 检查点：SWCC flush 计费薄入口

- 延迟模拟头新增统一的 SWCC/HWCC 访问 wrapper；`FlushSwccRange` 通过
  `RecordSwccFlush` 计入 SWCC flush 行访问，关闭时仅执行 relaxed gate 分支。
- 验证：`cmake --build /tmp/dsidle-m1-build --target dsidle_masstree --parallel`
  （通过，既有第三方告警不变）。

## 2026-07-24 — M6 检查点：NodeControl HWCC 原子计费

- `NodeVersionAccessor` 的 stable、try_lock 和 unlock_release 经统一 wrapper 分别
  记录 HWCC atomic load/RMW/store；关闭模拟时仍是单个 relaxed gate 分支。
- 验证：RelWithDebInfo `dsidle_node_control_tests`（1/1 通过）。

## 2026-07-24 — M6 检查点：延迟 scope 与统计单测

- 新增 `dsidle_latency_simulator_tests`：覆盖嵌套 scope 仅在最外层 delay、跨两个
  SWCC cacheline 的计费，以及 HWCC atomic load 的 raw/miss/delayed 统计恒等式。
- 验证：重新 CMake configure 后构建该目标，并执行
  `ctest --test-dir /tmp/dsidle-m1-build --output-on-failure -R
  dsidle_latency_simulator_tests`（1/1 通过）。
